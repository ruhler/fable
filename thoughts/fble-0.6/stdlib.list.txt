Std List Library
================

Existing List API:
* List@, List, Cons, Nil
* Append, Concat, Join
* Last, Init
* Map, ForEach
* Reverse
* Eq, Has, Search, Contains, StartsWith
* Split
* RemoveFirst, RemoveAll
* Length, Get, Take, Drop
* Sort
* And standard interfaces: show, order, etc.

Let's look at some existing standard libraries, both at list and string
operations, which I suspect are both applicable to what I want for my list
library.

C99 doesn't have lists I guess. So that's easy.

C++ std::list
* Iteration: begin, end 
* Front, Back
* empty, size
* clear, insert, insert_range, erase, push_back, pop_back
* merge, remove_if, reverse, unique, sort

Java List
* add, add_at, add_all, clear
* contains, contains_all,
* equals, get, set, hashCode,
* indexOf, lastIndexOf, 
* isempty
* iterator
* remove, removeIf, removeAll, replaceAll, retainAll, 
* size, sort
* sublist

Java String
* literal, compare, concat, contains, equals
* startsWith, endsWith,  indexOf, isEmpty,
* join, lastIndexOf, replace, replaceAll,
* length, split, substring, trim

Haskell List
* map, ++, filter
* head, last, tail, init
* null, length, get, reverse
* foldl, foldl1, foldr
* and, or, any, all, sum, product, maximum, minimum
* concat, concatMap
* scanl, scanl1, scanr
* iterate, repeat, replicate, cycle
* take, drop, splitAt, takeWhile, dropWhile, span, break
* elem, notElem, lookup
* zip, zip3, zipWith, zipWith3, unzip, unzip3

Haskell String
* lines, words, unlines, unwords

Python List
* append, clear, copy, count, extend, index
* insert, pop, remove, reverse, sort
* 'in', '+', '*', len, min, max, slice
* list comprehension, list literal

Go List
* iterator: Front, Back
* InsertAfter, InsertBefore, Len, MoveAfter, MoveBefore,
* MoveToBack, MoveToFront, PushBack, PushBackList
* PushFront, PushFrontList, Remove

Keep in mind that my list implementation is 'singly linked' and we don't have
an array or vector type that provides random access by index (use a map for
that?).

Straw proposal:

  /Std/List%
    List@, List, Cons, Nil
    Append, Concat, Join, Split,
    Head, Tail, Last, Init
    IsNil (?)
    Reverse
    Map, ForEach

  /Std/List/Eq%
    Eq, Uniq

  /Std/List/Index%
    Length, Get, Set(?), Take, Drop, Sub(?), IndexOf

  /Std/List/Sort%
    Sort
    
* Join takes a list separator, not a single element separator.
* ForEach is in place of iteration, but no way to break out of iteration?
* Has, Search, Contains are all slight variations:
 - find a specific element, or one satisfying a predicate?
 - return bool if it's found, or int position, or element value?
 - similar questions for remove functions, and split, and replace.
* I'd like StartsWith, EndsWith, and HasInsideWith
 - Except pick better names. These are all about subsequences.
* RemoveFirst, RemoveAll also suffers from exact element or general predicate.
* Append adds a list. Do we want a way to add a single element to the end?
* Do we want a 'null' function? aka IsEmpty.
 - Yes we do, just not sure what to call it.
* Do we want ability to insert or delete at an indexed position?

List is also the first place we have a clear desire for some standard
interfaces:
* Eq for many of the operations... assuming they are based on equality rather
  than generic predicate.
* Ord for sort.

What would it look like to support iteration though a list that we can break
out of? For example, maybe I want to implement my own Get function on top of a
generic list iteration. How would I express it?

Sounds easy enough, just like 'ForEach', except returns a maybe value to tell
us when to continue? Or it has continue or break?

Conceptually:
  B@ b = base
  foreach x in list:
    case body(x, b):
      RETURN r: return r
      CONTINUE b: b = b
  return ???

We need to return something when we reach the end. Maybe stuff the return
value into 'b'? Which suggests at each step of the iteration we return the
accumulated state along with a flag to tell us if we should break out early.

  B@ b = base
  foreach x in list:
    (c, b) = body(x, b)
    c.?(return: return b);
  return b;

Our magic type could be a bool and a base type. Or it could be:
  @I = +(B@ continue, B@ return);

Should we instead offer general iterator support? Iterators are pretty easy to
define and give full flexibility, right?

 Iter@ = <@ T@> { Maybe@<*(T@ head, (Unit@) { Iter@<T@>; } tail); };

Basically it becomes a kind of list where you have to call a function
explicitly before you can advance to the next element. I suppose that would
be, instead of:

  @ L@ = +(*(T@ head, L@ tail) cons, Unit@ nil);

You have:
 
  @ I@ = +(*(T@ head, (Unit@) { T@; } tail) cons, Unit@ nil);

Now, even if you have an iterator, that doesn't naturally solve the 'for loop
with break' use case. We would want a similar, separate api for iterator. You
could write your own now, as a function, I suppose.

For example, for get, I might implement it in terms of iterators as...

But I don't need to use an iterator for list. I can use the list itself.
Particularly if I'm controlling the iteration. Iterators are useful when we
start to have other structures we want to make look like lists without having
to construct a full list for them, and then we want a way to break out early.

Anything fancy we can do with 'bind' to make ForEach look cooler? I suppose
instead of:

  Int@ sum = ForEach(xs, 0, (Int@ x, Int@ b)  {
    Add(x, b);
  });

We would be writing:

  Int@ sum = {
    Int@ x, Int@ b <- ForEach(xs, 0);
    Add(x, b);
  };

Not sure that's any better.

With some way to do continuation versus early return:

  Int@ sum = {
    Int@ x, Int@ b <- ForEach(xs, 0);
    Sgn(x).?(0: Return(0));
    Continue(Add(x, b));
  };

Or, maybe we squeeze all the arguments into a single type?

  Int@ sum = {
    S@<Int@> s <- ForEach(xs, 0);
    Sgn(s.x).?(0: s.return(0));
    s.continue(Add(s.x, s.b));
  };

Not sure why we would want to do that though.

Written as a function instead:

  Sum = (Int@ b, List@<Int@> l) {
    l.?(nil: b);
    Int@ x = l.cons.head;
    Sgn(x).?(0: 0);
    Sum(Add(x, b));
  }

So we could always break out early via explicit use of functions if we wanted
to.

What if we had an iteration that did something like:

  Sum = {
    Int@ s, Int@ x, List@<Int@> t <- ForEach(xs, 0);
    Sign(x).?(0: 0);
    Sum(s, x);
  };

You know, this is interesting as a way to do pattern matching. Like:

  (List@<T@>, B@, (T@, List@<T@>) { B@; }) On;

  Sum = {
    Int@ head, List@<Int@> tail <- On(xs, 0);
    Add(head, Sum(tail));
  };
  
---

Things you can do without knowing anything about the elements of a list:

  List construction:
  * List@, List, Cons, Nil (names to be reviewed?)
  * Append, Concat, Join

  List deconstruction:
  * IsCons, IsNil (names to be decided)
  * Head, Tail, Last, Init

  List reordering:
  * Reverse

List subsequencing:
  Requires you to have element by element comparison.

  * IsPrefixOf, IsInfixOf, IsSuffixOf (names to be discussed)
  * Eq (for full sequence match)
  * Maybe you want to skip to/past a prefix, infix, or remove a suffix?
  * You could split on a sequence.

  These seem like specialized use cases, worth pulling into a separate module.

List element search/access:
  * Has: Ask if any element satisfies a predicate.
  * Count: Count how many elements satisfy a predicate (general case of Length?)
  * GetFirst element that satisfies a predicate.
  * GetLast element that satisfies a predicate.
  * GetAll elements that satisfy a predicate (aka 'filter', 'remove_if')
  
  For GetFirst, GetLast, we could return a Maybe... or, what if we just return
  the point in the list with the element? If there is no such element, return
  empty list. If there is, you can easily access the element, but also easily
  jump into where in the list the element is. No need to return an index. So
  these can really be like 'strchr' style functions?

  GetFirst is like DropWhile(Not(l)).

  Split based on single element predicate sounds reasonable too. Not sure what
  to call it. There are two variations on split: split into 'prefix' and
  'rest', or split the full length of the list. We probably want to have both
  of them.

Indexing operations:
  * Length, Get, Set, Take, Drop.

  Note that Take + Drop together effectively give you a sublist. Maybe we want
  to provide that helper function: SubList(...);
  
I think a good guidance for the standard library functions is:
* They should actually be convenient to use.
  For example, Contains(eq, xs, x) isn't notably easier to use than
  Contains(xs, eq(x)), so reasonable to take the predicate form always.
  But maybe we want a nice compose function so we could do things like:
  
  HasChar = Contains

  No. Not sure what order to put the args in.
  
Let's consider the variations of take, drop, splitAt, takeWhile, dropWhile,
span, and break that Haskell has.

Take, Drop are the substring operations. Easy.

SplitAt returns back the pair with 'Take' and 'Drop'. That could be useful,
honestly.

takeWhile, dropWhile, and span are the analogous element predicate based
operations. As mentioned, dropWhile is related to GetFirst if we return the
position in the list rather than the individual list element. Seems worth
having both of these.

So, honestly, I think it's fairly clear what all functions I want to have and
how to organize them. I'm just not sure on naming. Let me do a straw pass.

List construction: /Std/List%
* List@, List, Cons, Nil
* Append, Concat, Join

List deconstruction: /Std/List%
* IsCons, IsNil
* Head, Tail, Last, Init

List reordering: /Std/List%
* Reverse

List search: /Std/List/Search%
* Has, GetFirst, GetLast, GetAll
* TakeWhile, DropWhile, Span (?), Split
* Count (?) (adds Int@ dependency, otherwise makes sense to have here)
* DropAll? DropFirst? DropLast?

List indexing: /Std/List/Index%
* Length, Get, Set, Take, Drop, SplitAt, SubList

List traversal: /Std/List%
* ForEach, Map

List sort: /Std/List/Sort%
* Sort (wants 'Ord' standard class?)

List sequencing: /Std/List/Eq%
* IsInfixOf, IsPrefixOf, IsSublistOf, Split
* Uniq (here only because of Eq?)

Yeah, I think that covers everything. The slightly unfortunate bits:
* Count is the only one of list search that pulls in Int@.
  Not sure what to do about this. Better to avoid pulling in the dependency on
  Int@ here if we can. But it doesn't also make sense in list indexing.
  Maybe: /Std/List/Search/Count%?

* Uniq is only one besides list sequencing that needs Eq, but it doesn't feel
  like a list sequencing thing. Well, we can group it all into 'Eq', where
  'Eq' is both used for sublist equality and element equality.

---

On to the question of names.

Why 'Cons' and 'Nil'?

'cons' comes from lisp, meaning construct. It's nice and short, but in no way
obvious or intuitive. Do I have any better ideas?

How about for nil?
* nil means a quantity of no importance.
* null means of no binding force or utility.
* Empty is the obvious name to use, but it's a bit long.

I like 'head' and 'tail' and 'last'. Those make sense to me. 'init' is a bit
more of a stretch.

We could use 'Empty' and 'NonEmpty'. It's clear from a descriptive point of
view, but it's pretty wordy. I think I could deal with 'Empty', but it would
be nice to have a verb to replace 'Cons'/'NonEmpty'. 'Prepend'? 'Prefix'?
Prefix sounds more like adding a string of elements to the front rather than a
single element. Prepend is apparently jargon as opposite of append.

If we think of the list as a stack, then Cons is the same as Push, or
PushFont. Head is like Pop. Or, Insert? InsertFront? Or, 'Put'? Like, put onto
a list?

Here's an example to play with:

  Cons(Dying@(head.x, head.y, nwait), tail);
  Put(Dying@(head.x, head.y, nwait), tail);
  Prepend(Dying@(head.x, head.y, nwait), tail);
  Push(Dying@(head.x, head.y, nwait), tail);

    dying: Empty<Dying@>,
    dying: Nil<Dying@>,
    dying: None<Dying@>,
 
  dying.?(empty: ...);    # works well I think.
  dying.?(some: ...);

That's interesting to use 'some' instead of 'cons'. It reads fairly well. It's
a list with some elements. "composed of a quantity or number which is not
stated;" sounds like an accurate description. I like that.

'head' and 'tail' could be replaced with 'first' and 'rest'?

  dying.some.first
  dying.some.rest

Except 'rest' is a little bit easy to confuse with resting.

  dying.some.head
  dying.some.tail

---

Here's what I'm thinking:
  'empty'
  'items': *('head', 'tail')

Empty<...>
Items(head, tail)
List[...]

Let's give that a try, see how it feels to me. It's a bit of a tedious change,
so we'll see how far I get.

Reactions:
* So far everything feels nice except 'Items' is a little weird.
* 'elems' might be a good alternative to 'items', but why? 'items' is at least
  a complete word.
* 'items' feels slightly weird in a union select. It doesn't have the
  immediate implication to me of 'non-empty'. Maybe I'll get used to it?
* 'Empty' might have name conflicts with an empty map. Annoying?
  Maybe use List<...>[] in that case? Or empty string? Maybe EmptyList<...>?
  But then, may as well use the literal instead?
* I feel like 'Items' is growing on me, but 'Empty' not so much. Because
  Empty<Foo@> reads like it's making an Empty Foo@, not an empty list of Foo@.
  There's nothing about 'Empty' that suggests 'list'.
  I have existing 'Empty' for: Env@, Fifo@, Markup@.

---

Empty, EmptyList, List.Empty, NoItems, List<>[].

Items. Insert?

Insert sounds like a decent generic name for forming a list. Analogous to Map
Insert. If I don't mind having overlapping names for things, maybe I should
Have List.Empty and List.Insert?

I don't know. Maybe let's move ahead for now and revisit this later.

* IsCons, IsNil
  Rename to IsEmpty and IsItems? Again, 'items' feels weird in this context.

'NonEmpty'? List.NonEmpty(head, tail). List.IsNotEmpty. NotEmpty?


List search: /Std/List/Search%
* Has, GetFirst, GetLast, GetAll
* TakeWhile, DropWhile, Span (?), Split
* Count (?) (adds Int@ dependency, otherwise makes sense to have here)
* DropAll? DropFirst? DropLast?

Has: given a predicate, check if the list has anything that satisfies the
predicate, without returning whatever does or does not satisfy it.

I don't currently have any uses of my existing 'has' function.

Here's an example with 'Contains':

  Contains(/Core/String/Eq%.Eq, args, Str|'--help').?(true: {
    Unit@ _ <- m.do(O.PutStr(stdio.out, /Invaders/Usage%));
    m.return(True);
  });

Which would be rewritten to...

  Has(/Core/String/Eq%.Eq(Str|'--help'), args)

(List@<Pos@>, Pos@) { Bool@; } InPosS = /Core/List/Eq%.Contains(EqPos);

(List@<Pos@>, Pos@) { Bool@; } InPosS = (List@<Pos@> l, Pos@ x) {
  /Core/List/Eq%.Has(EqPos(x), l);
};

Both seem reasonable to me. Now, which argument comes first, the list or the
predicate?

If we put the predicate second, we can use it with bind syntax: uh...

(List@<X@>, (X@) { Bool@; }) ...

{
  Pos@ p <- Has(list);
  Eq(Pos, x);
}

Hard to imagine that being useful for much.

  Has(/Core/String/Eq%.Eq(Str|'--help'), args)
  Has(args, /Core/String/Eq%.Eq(Str|'--help'))

If we had a flip function:
  
{
  Pos@ p <- Flip(Has, list);
  Eq(Pos, x);
}

Which may not work with type inference.

---

I don't imagine much of a use of bind syntax for predicate functions. I think
it's much more natural to put the predicate before the list in these cases.
Though I suppose when we get to 'ForEach' and 'Map' it might be different, and
we may want to be more consistent.

I propose we call 'Has': 'Any'.

  Any(Eq(3), nums);
  Any(IsOdd, xs);

For searching for an element:
  Search(IsOdd, xs);
  SearchR(IsOdd, xs);

Returns the list at the point of the element found, or empty list if not
found. Search goes forward, SearchR goes from the back.

Filtering:
  KeepIf(IsOdd, xs);
  RemoveIf(IsOdd, xs);

Feels most natural to me. Because I never remember if 'filter' keeps elements
or gets rid of them.

Apparently I have no existing uses of 'Has', or 'Search'. No use of
'RemoveAll', not use of 'RemoveFirst'. Maybe those were there from a time when
I was using list for Set, and then I switched to a specific Set API?


How about TakeWhile, DropWhile? Those sound like reasonable names to me.

For Count, we could use CountIf? But again, I want to separate this to avoid
the potential false dependency on Int@.

How about 'span' and 'break'? Those are just TakeWhile, DropWhile/Search
combined into a single function. It's only for performance, to avoid double
traversing the list. I don't know. Not sure.

Split is useful, the name makes sense. But I like it to be a full split of the
list, reverse of join. What would we call a partial split? And note, our
partial split would want to include the delimiter element presumably, unlike
our full Split.

List search: /Std/List/Search%
* Has -> Any, GetFirst -> Search, GetLast -> SearchR, GetAll -> KeepIf
* TakeWhile -> TakeWhile, DropWhile -> DropWhile, Span (?), Split -> Split
* Count -> CountIf (?) (adds Int@ dependency, otherwise makes sense to have here)
* DropAll -> RemoveIf, DropFirst? DropLast?

How do we feel about location of the predicate in general? First or second
argument?

Places I put it first today:
* RemoveFirst, RemoveAll,

Places I put it second today:
* Map, ForEach, Has, Search

Motivations for having it first: it reads better to me(?). For example:
  Any(Eq(4), xs) vs. Any(xs, Eq(4));
  Search(xs, IsOdd);
  Map(Mul2, xs); Map(xs, Mul2);

Okay, not so convinced to put it first based on that.

Motivation for having it first: the predicate is more general than the list
  For example, seems more likely we would want to say:
   AnyIsOdd = Any(IsOdd);
   Mul2s = Map(Mul2);

  Less likely to say:
   F = Any(xs);

Motivation for having it second:
  We can use it with bind syntax:
     Int@ x <- Map(list);
     Add(x, 5);

  The map function at least introduces a type argument?
    But that shouldn't really matter here.

  Nicer to call directly, like in for loops:
    ForEach(0, xs, (Int@ i, B@ b) { 
      ...
      });

Thus the big conflict is between partial function application on one hand and
use of bind syntax on the other.

I'm leaning towards having the predicate be second?

I don't know.

   AnyIsOdd = Any(IsOdd);
   Mul2s = Map(Mul2);

   AnyIsOdd = (List@ l) { Any(l, IsOdd); };
   AnyIsOdd = Flip(Any<Bool@>, IsOdd);

If we put the predicate second, what makes sense and what doesn't?
* Any(l, IsOdd), Search(l, IsOdd), SearchR(l, IsOdd)
* KeepIf(l, IsOdd), RemoveIf(l, IsOdd)

Like, KeepIf should be KeepFrom? KeepFrom(l, IsOdd), RemoveFrom(l, IsOdd);

Keep(l, IsOdd); Remove(l, IsOdd)? Yeah, why not just have the short names?

Split is tricky. Should it take a subsequence or an individual character? Tcl
takes individual characters. Python takes a subsequence. Java takes a regex.
Go takes a subsequence.

My current use of Split is all by character property: IsSpace or equal to a
certain character. Maybe it's worth having both? We could put them in two
different submodules.

* TakeWhile -> TakeWhile, DropWhile -> DropWhile, Span (?), Break (?)
* DropFirst? DropLast?

TakeWhile is a mouthful. Any simpler name we could use? DropWhile too. They
are related to Search and SearchR. In that...

  Search(l, p) = DropWhile(l, Not(p))

Better to just use Search(l, Not(p))? What's an example of when I would use
DropWhile? Maybe... DropWhile(l, IsSpace); Then Search(l, Not(IsSpace)) seems
reasonable to me. But it reads differently. It's extra indirection.

Skip(l, IsSpace)? That sounds nice. Skip any leading characters satisfying the
predicate. aka. DropWhile.

TakeWhile... would be like trying to get a word. TakeWhile(l, Not(IsSpace)).
How would I say that? Maybe... TakeUntil(l, IsSpace)? Or just...
Until(l, IsSpace); And While(l, IsSpace)?

What does that leave? The DropWhileNot, but that's search.

In other words, think of it like this:
* take while: While
* take while not: Until
* drop while: Skip
* drop while not: Search

Then what is SearchR about? That's take back while not? Or, reverse take while
not? Not sure.

Anyway, the remaining ones to consider for search:
* Span, Break, DropLast

Span and Break sound good to me. Span splits the string into what satisfies
your predicate and everything that follows. Break splits the string into what
doesn't satisfy your predicate and everything that follows. I just need a data
structure to return the two values:

  <@ T@> { *(List@<T@> ???, List@<T@> ???); }

Three names are needed:
* Name for the data type.
* Name for the first field.
* Name for the second field.

How would I describe these things? Let's see. Span and break are splitting the
string into a 'prefix'... well, actually a 'head' and a 'tail'? Or a 'prefix'
and a 'tail'?

When would you use something like this in practice? Probably GetWord:

  Span(input, IsWord).span and .tail. 

Or:
 
  Break(input, IsSpace).span and .tail.

So, proposal is:
  @<@> Span@ = <@ T@> { *(List@<T@> span, List@<T@> tail); };

Good. Swell.

That leaves us with DropLast, or anything else we might want to do at the end
of the list.

We have SearchR, which is a kind of 'drop while not', but searching for the
last occurrence of a string instead of the first. Or, working backwards, take
from the end up to *and including* the first item seen from the back.

We have 6 variations for working on the front. How about the back? SearchR
already is something new in those regards...
* Take everything at the tail that satisfies a predicate.
  Like SearchR.tail. Not sure how it would be used?
* Drop everything at the tail that satisfies a predicate.
  Like trimming whitespace from the end of a string. We should provide a
  function for this.
* Drop everything at the tail up to an element that satisfies a predicate.
  Not sure what the use case would be.
* Take everything at the tail that goes up to an element that satisfies a
  predicate.
  Like SearchR, but exclusive of the element instead of inclusive. Not sure
  what the use case would be.
* The Span/Break analogies which split the list and provide both the front and
  the back.

Honestly I don't know the use case for SearchR. The only use case that makes a
lot of sense to me for working at the back of lists is trimming whitespace
from the end.

Apparently python has:
* rpartition: you give the separator, it gives you the before, middle, and
  after, searching from the back.
* rsplit - split, but reverses the results.
* rstrip - remove trailing whitespace.
* strip - remove leading and trailing whitespace.

I'm thinking:
* Trim - remove anything satisfying a predicate from the end
* Strip - remove anything satisfying a predicate from beginning and end

See how far we get with those.

Otherwise it would be things like:
* SkipR - skip over the last occurrence of something in a string?
* UntilR - take up to but excluding the last occurrence of something?
* WhileR - take up to and including the last occurrence of something?

I don't see those as worth having. How important is SearchR? It's like strrchr
in c. They have it, so seems reasonable for us to have it too.

Thus I complete my proposed list searching functions:
 
List search: /Std/List/Search%
* Any, Keep, Remove, Split
* Count (not 'Length', so plain word should be fine)
* Search, SearchR, Skip, Until, While
* Trim, Strip,
* Span@, Span, Break

---

List indexing: /Std/List/Index%
* Length, Get, Set, Take, Drop, SplitAt, SubList

These are similar to search, but using indices.

Length, Get, Set are all fine.
Take and Drop sound like fine names to me.

Instead of SplitAt, how about Split? Oh. No. That's not good.

How about Break? Reuse the same name and Span@ from list search? Or Span.

And for SubList, I like Range? Yeah. I like that.

Anything we want in terms of dropping or taking from the end? We could allow
negative indices to say from the back? I dunno.

List indexing: /Std/List/Index%
* Length, Get, Set, Take, Drop, Span, Range

---

* IsCons, IsNil ==> HasItems, IsEmpty.

---

List sequencing: /Std/List/Eq%
* IsInfixOf, IsPrefixOf, IsSublistOf, Split
* Uniq (here only because of Eq?)

Uniq is fine.

Actually, seems easy to me:
* StartsWith, EndsWith, Contains, Split, Uniq.

We could also have the search variations:
* Search, SearchR, Until
* Span@, Span, Break make sense if you are thinking of it as a string
  delimiter. Like: Span(Eq, l, Str|', '); Seems a little more far fetched to
  me. But also no harm, right?

While - doesn't make any sense. It's more of a 'drop prefix', which would make
    sense. How about: Drop? Or.. actually, Skip makes sense here.
Trim - is basically drop the end if it matches. That makes sense to me.
Strip - drop the front and the end. A little odd, but okay?

List sequencing: /Std/List/Eq%
* StartsWith, EndsWith, Contains, Split, Uniq
* Eq, Ne
* Skip, Trim, Strip, Break

In this case, I feel like 'Eq' should come as the first argument, because it's
more likely to be associated with the type. Unlike the predicate previously,
which is more likely to be associated with the call.

Do we put the needle first or the haystack, as it were? Let's do haystack.
That's consistent with strstr and putting the list arg before the predicate.

So, some examples:
  StartsWith(Eq, l, Str|'hello');
  Contains(Eq, l, Str|'hello');
  Skip(Eq, l, Str|'hello');
  Skip(Trim, l, StrE|'\r\n');

Sounds reasonable. Does 'Eq' still make sense for the module? I don't think
this requires an Eq@ class quite yet, to be honest. Eq@ is not anything more
special than a plain predicate. Hmm... Makes sense to standardize though? But
there's nothing to standardize here. It's not like Int@ or List@ where
everyone has to agree. There is literally only one way you could describe Eq@.

---

Cool. I think I have my full list proposal now. Here it is:

  /Std/List%: (done)
  * List@, List, Items, Empty
  * Append, Concat, Join
  * Head, Tail, Last, Init
  * Reverse
  * ForEach, Map

  /Std/List/Search%: (done)
  * Any, Keep, Remove, Split
  * Search, SearchR, Skip, Until, While
  * Trim, Strip,
  * Span@, Span, Break

  /Std/List/Count%: (done)
  * Count

  /Std/List/Index%: (done)
  * Length, Get, Set, Take, Drop, Range, Span@, Span

  /Std/List/Sort%: (done)
  * Sort

  /Std/List/Eq%: (done)
  * HasItems, IsEmpty
  * Eq
  * StartsWith, EndsWith, Contains,
  * Skip, Trim, Strip,
  * Span@, Break
  * Split
  * Replace

Upon review: 'replace' is the only thing missing here?
* Eq could have a Replace function easily enough. Done.
* Do we want to replace elements as well as substrings? You can do that with
  Map more easily than if we had a Replace function.
  Map(... { P(x).?(true: y, false: x); });

Good. Good improvement over what we have today anyway. Let's move forward with
this. I'm adding a fair bit, so I'll want to expand the test coverage.

Also, maybe worth thinking about Eq@ and Ord@ briefly.

---

Uh, IsEmpty and HasItems depend on Bool@. So they should be moved elsewhere,
right? Into Eq% module, no? Or Search%?

---

Question: Take and Drop should take the index as the second argument, right?
Not the first?

---

Trouble: how do document list, which has a nested type? It's a mix of @struct
and @union, without a separate name for the different parts. Hmm...

How to document the type parameter? Tricky.

Should Empty be documented as a value or a function, given it depends on type
parameter T@?

---

Does it make more sense for ForEach to go (A@, B@) { B@; },
or (B@, A@) { B@; }?

Somehow, writing my example, it feels better to me to put the base as the
first argument.

That will be a tedious change to make though.

I think I should make the change. But then we can ask if we should base the
base element before the list, for consistency?

---

Uniq function, couple questions:
1. Does it only drop duplicate uniq elements, like the uniq command?
2. Does it keep the first or the last of duplicate elements?

c++ has: "Removes all except the first element from every consecutive group of
   equivalent elements from the range"

That's not what I was thinking. My intention was to remove duplicates wherever
they showed up. Like using a List for a set. Maybe best not to have this then?
Or to use the more traditional 'uniq' behavior? Or maybe rename it to 'Dedup'?
We could call it 'Set' as well?

I'm starting to think maybe best not to have these functions. If I don't use
them, and nobody else has them?

---

Question: For Eq Strip... what if there is overlap at front and back? I'm kind
of thinking maybe we don't want strip as a function. Can you even imagine any
use for it?

Skip I can definitely imagine a use of. For example, in split? Not sure about
trim. But Split should give us a hint as to how to treat strip, because it has
similar challenges.

For example, let's say we want our delimiter to be aba, and the string is
'ababa'. Skip is easy: 'ba'. Trim is easy: 'ab'. What about split and Strip?
For split, I should think it would be: aba is first delim, ba is what's left.
That is, take the front off first. Use the same for Strip?

Or just don't have this function. Let people write it themselves if they want
to, and then they have control over the behavior. I think that's better.

But I still want split. What does split do in this case in, say, python?
Python gives back ['', 'ba'] in this case. Seems funny to me that it returns a
leading empty string.

Java docs for split are more precise. They only return leading '' when there
is a positive width match at the beginning of the string. If python and java
both have split with sequences as the delimiter, I feel like we ought to as
well, even if I can't think of a good case for it.

---

What should 'Split' on an empty list return? An empty list, or a single empty
list? Python returns a single empty list.
