
// Test simple process calls.
struct Unit();
union Bool(Unit true, Unit false);

proc sub(Bool <~ get ; ; Bool) {
  get~();
};

/// Expect: Bool:true(Unit())
proc main( ; ; Bool) {
  Bool <~> myget, put;
  put~(Bool:true(Unit()));
  sub(myget ; );
};

