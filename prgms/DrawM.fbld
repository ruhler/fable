module DrawM(DrawI) {
  import @ { UnitM; UBNatM; };
  import UnitM { Unit; };
  import UBNatM { Nat; };

  union Color(
    Unit black,
    Unit red,
    Unit green,
    Unit yellow,
    Unit blue,
    Unit magenta,
    Unit cyan,
    Unit white
  );

  struct Draw(Nat x, Nat y, Nat w, Nat h, Color color);
};
