# @@fble-test@@ compile-error 10:14
@ Unit@ = *();
Unit@ Unit = Unit@();

@ Bool@ = +(Unit@ true, Unit@ false);

@ Tok@ = %(/SpecTests/'9.3-AbstractCast'/Type/ValueMaltyped%);

# The value zzz is not defined.
Tok@.<Bool@>(zzz).true;
