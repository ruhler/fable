Fble 0.6 TODO
=============
Things to consider doing for the fble-0.6 release.

* fble-pprof 'unable to serve' after ctrl-c to quit just before.

* Add test that /Std/Stream%.ForEach iterates in the right order.

* Change IntP Data traversal order to be natural order.

* Figure out why we don't propagate an error
 - If /Core/Map/Data% Delete function is left undefined.
 - The tests print @[, then skip over to the next test?

* Remove /Std/Stream/Text% and /Std/Stream/Binary%?
  Now that I'm using Stream@ type more generically elsewhere in the library?

* Improve the performance of fble-pprof.
  Taking more than a minute for a 5MB profile feels way too slow.

* Update all the sample apps for latest features, styles, idioms.

* Should we provide Monad instances for Maybe@ and Lazy@ somewhere? List@?

* Figure out a real solution to error handling in general?
 - Examples to consider: file read/write, parsing an int.

* Should struct copy be allowed to change the type of fields it updates?
 Sample use case: Convert a byte Channel@ to a char Channel@ using
   s.@(put: (Char@ c) { s.put(Pos(c)); });

* Document somewhere that files are closed automatically.

* Double check all the old documentation referring to Output@, which is now
  called Stream@?

* Add back character type functions isspace, isalnum, etc?
* Add Lt, Le, Gt, Ge, etc for Char@?

* Add maybe.c helper functions for working with maybe types at native level.

* Make sure setlocal gets called for compiled stdio code
 - If we aren't explicitly registering the stdio functions.
 - For example: add an FFI function and call it at the module level?

* A lot of the /Std/* documentation isn't very helpful.
  Consider rewriting it to be an explanation for how to use the functions
  instead of a description of what they are.

* Re-open stdin, stdout, stderr in binary in /Std/Io/File/Binary%.
* Reconsider if Read, Write, Append should report error messages?

* Add spec tests for import expressions.
 - Add tests for unused warnings.
 - Add test for missing type in bind expression.
 - Add test for extra (.field) in bind expression.
 - Add test for duplicate variable names.
 - Add test for duplicate field names.

* See if there is more opportunity to typecheck more stuff before erroring
  out?

* Document/test/do something with the fact that "(.", ":=", and ".=" are
  treated specially in the parser, but other such constructs aren't.
  - And it makes a difference in what whitespace you can or cannot use in an
    fble program.

* Clean up bnf syntax in spec.
 - What do I use for list versus optional? Why sometimes [] sometimes {}?

* Add test that LSFR won't seed itself to 0.

* Why is the invaders ship dying animation so messed up on linux console?

* Why don't interpreted cli tests output when running them during build?
 - I have to go to the generated file to see the outputs?

* Remove kinds from let expressions in all the examples in the spec and
  tutorials and everywhere for the new syntax.

* Remove Builtin from spec and spec tests now that we have FFI?
 - Like, do we need builtin support anymore? Is there any value for it?

* Figure out how to organize std packages
 - Add network to std package?
 - Separate io and FFI related stuff into separate std-io package?
 - Add app to std package?

* Somethings wrong with the Snake window sizing.

* Debug jank issues in fble-invaders?
  --fps shows a few slow frames.

* fble-app capture input so we don't see a bunch of it on the console after
  the game is done?
  - Quick search on the internet didn't bring anything obvious up.
  - This would be bad for a password protected field, for example...

* Switch back to LFSR for snake?
  - Because it's faster and good enough?

* Double check benchmark deadlock conditions
  - for example, for snake, now that we changed the random number generator
    used.

* Write a script to help find unused modules?
 - Because I suspect I have some unused modules in my packages...

* Pick better example for docs in /Std/Monad%
 - Now that we no longer have Stdio@.

* Double check error message for over application of a function.
  It shouldn't say "can't apply arguments to something of type (Foo@) { ...}"
  Because that's not true, right? Better would be "too many arguments?"

* Add tests for /Std/Func%.

* Figure out what FbleStringValueAccess should do in terms of unicode.

* Add tests for Output ToString and ToNull
 - Assuming we don't already have them.

* Verify all api is listed in include/build.tcl.

* Add spec tests for full test coverage.

* Test coverage for FFI
 - Verify names matter.
 - Verify module paths matter.
 - Test what happens if you can't find the foreign function?

* Test unicode for character based native input and output streams.
* Add tests for /Core/Char/Stream/IStream%.GetLine.

* Bug in type inference?
 - See /SpecTests/6.4-TypeInference/Regression/Getline%
 - /Std/Stream% is probably the relevant module now.

* Fix issue with opengl with dummy driver.
 - Remove broken --driver dummy option to FbleAppMain?
 - See since removed commented out code in pkgs/invaders/build.tcl
 - Or just remove --driver option entirely if it isn't going to work?

* Think about api to break out of a ForEach loop mid way through.
 - Because this comes up a lot, and I tend to just write 'TODO' in my code
   where I should probably implement a proper interface.
* Should we have a generic Container interface?
 - As a way to standardize ForEach and perhaps other loop constructs across
   all containers? Would that add anything of value?

* Add test for /Core/Native/Env%.GetVar.

* Add test for StrE with \q escape.

* Update tutorials for new bind syntax.

* Print function names along with error location on stack trace?

* Optimize /Std/Int/Show% implementation by pulling out int values to top
  level rather than calling Int|* over and over?

* Add function to show an integer base 16? Base 2? Base anything?

* Figure out why we see the same errors reported many times when we build.
 - And fix it.

* Cover all escape chars in StrE tests.
* Better implementation of ReadHex in /Std/String/Escape.
* Replace uses of 'Concat' with 'Format' where appropriate.
* Add vim syntax highlighting for \ and % escape sequences?
* Double check @module[...] values are correct in /Std/...*.fble files.

* Double check performance of ReverseComplement, specifically the
  ComplementCode function. Maybe use a map instead?
  Similar for /Invaders/Graphics%.String.

* Does private type access restrict access to literals?
  Like, if Digit@ was private, could I still construct a list of Digit@ via a
  literal? Hopefully the answer is no? It probably works fine, but maybe worth
  adding a spec test for.

* Optimize encoding of FbleNewLiteralValue prgm?

* Update tutorial on literals.
  - Particularly about 'longest' versus 'first' match.
* Update tutorials for string literals: Foo(Str|...) now instead of Foo|...;
* Update tutorials for character library.
* Update tutorials for use of Concat instead of Strs.

* Implement Cmp@ for Unit, Bool, Int, Char, List.

* Figure out how to properly document List@ type
 - Mix of poly, struct, and union all in one.
* How to document a poly? @func? @value?
* How to document methods of interface structs?
* How to document an interface function type, like Eq@?
* /Std/List/Eq% docs could be more precise and include examples.
* How to document internal functions, e.g. /Std/List/Sort%.Insert?
  Make sure this doesn't get shown in reference documentation.
  e.g. /Std/Int/Lit% internal helper functions.

* Should I add @example as a doc comment field? Seems useful?
* Reorder arguments in body function passed to ForEach?

* Restrict Int@ type internals to /Std/Int%.
* Restrict /Std/Int/IntP* modules to /Std/Int%.
* Update references to /Core/Int in tutorials.
* Update references to /Core/List in tutorials.

* Clarify in spec whether poly apply to an undefined value results in an error
  or not.
  And add spec tests for that case.

* Rename /Core/Unit% to /Std/Unit% in tutorials, if there are any references.

* Revamp the standard library.
 - Redesign from the ground up, looking at existing standard libraries for
   guidance.
 - Test and document (and tutorialize?) all functions.
 - Implement rosettacode.org tasks under pkgs/rosetta to test it out.

* Optimize C and interpreter backends like we did aarch64?
  By inlining logic to access fble values instead of calling library
  functions.

* Design a package management / versioning scheme?
 - see thoughts/fble-0.5/modular_compilation.txt for context.

* Clean up program loading
 - see thoughts/fble-0.5/modular_compilation.txt for context.
 - Clarify the spec with respect to builtins referring to other builtins and
   the recursive module requirements in case of difference between builtins,
   .fble files, and header files.
 - Add tests for:
   a. Try to compile a builtin module.
   b. builtin A -> builtin B, .fble C -> builtin B.
   c. builtin A, A.fble.@ -> B.fble, B.fble -> /C%,
      C.fble has bugs, C.fble.@ doesn't. This should not fail to execute.
 - Update the implementation to pass the tests.
 - Consider separating the FbleModule* for execution and for type for a given
   module path.
 - Add option for loading modules from .so files?
 - Have fble-cli check that the function has the right type?
 - Load minimum dependencies needed for type checking when possible?
  
* Make programs run twice as fast.
 - Use native stack for stack allocations.
 - Inline FbleCall into aarch64 backend.
 - Specialize call logic based on what we can figure out statically in terms
   of number of args expected and tail/non-tail possibilities.
 - Avoid redundant undef checks on values based on static info.
 - Use a special ref value instead of NULL to represent undefined values?
 - Something to reduce overhead of use of 'Do'
   See thoughts/fble-0.6/stdlib.map.txt for analysis and discussion.

* Implement deflate algorithm.
 - Change --profile to produce gzip compressed profiles (doesn't need to be
   compressed yet, can use uncompressed blocks).
 - Change fble-pprof to decompress profiles.

* Fix potential GC gen overflow?
 - See thoughts/fble-0.3/stack_alloc.txt for details.
 - See thoughts/fble-0.5/gc.txt for more thoughts.

* Remove now unused FBLE_PROFILE_ENTER_OP, EXIT_OP, and REPLACE_OPs?
 - Or wait to see if we want them for code coverage?

* New apps
 - IEEE floating point support.
 - More benchmark games.
 - Implement an image viewer.
 - bitcoin miner
 - gzip, gunzip.

* Fble Language Features
 - Add support for mutable data structures?
 - Add some way to catch or recover from errors at runtime?
   Vital for composition.
   See thoughts/fble-0.6/stdlib.test.txt, thoughts/fble-0.6/exceptions.txt,
   thoughts/fble-0.6/stdlib.stdio.txt for discussion.
 - Some way to implement a web server that can handle multiple requests
   concurrently?
 - Think seriously about compatibility as the language and libraries evolve.

* Fbld Language
 - modularity
 - Better support for structs in fbld language, so its easier to write decent
   performing fbld code.
 - Have an 'if' primitive instead of 'ifeq' and 'ifneq'?
 - The @plain builtin is really strange. I had trouble coming up with a test
   case for it. Consider if there's some way we can get rid of that or make it
   more sensible.
 - Clarify in spec if @if[a@x][b@x] ... is an error or not.
   Or redesign semantics of fbld to avoid that question entirely.

* Fbld C implementation
 - Change FbldMarkup type to more explicitly distinguish between the different
   kinds of markups.
 - Figure out how to associate a location with empty markup.
 - Avoid constructing markups with singleton sequences?

* Implement a custom debugger for fble
 - That works on all backends.
 - That supports single step through code.
 - That supports field access.
