
// Test process calls that takes regular arguments.
struct Unit();
union Bool(Unit true, Unit false);

proc sub( ; Bool x; Bool) {
  ?(x ; $(Bool:false(Unit())), $(Bool:true(Unit())));
};

/// Expect: Bool:true(Unit())
proc main( ; ; Bool) {
  sub( ; Bool:false(Unit()));
};

