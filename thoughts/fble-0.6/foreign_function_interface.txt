Foreign Function Interface
==========================

Revisiting the question of having FFI baked into the language.

See thoughts/fble-0.4/foreign_function_interface.txt for background.

I'm thinking about it differently today though. The main goals for me with FFI
now are:
* Eliminate the need to write tedious, error prone code for defining profile
  blocks and FbleExecutables in my native modules.
* Allow easier mixing of foreign and pure code in the same module.

Concrete examples:
* I'm pretty sure I have my profile block ids wrong in my implementation of
  pkgs/core/stdio.fble.c, or I did, or I broke things and didn't notice.
* It's a pain to add new profile blocks and adjust everything based on
  position.
* It's a pain to define the module paths needed for the .path field of an
  FblePreloadedModule.
* It's a pain to have FooImpl and Foo wrapper function for every native
  function.
* For native getenv, I wanted to define Env@ type in the same module as
  getenv, and I wanted to provide a Lookup function that translated from the
  list of pairs to a lookup function in the same module.

Looking at things now, I'm not sure the details quite work out, but the idea
this time is what if we focus on only having to write the run functions for
things, and let the compiler do all the rest.

The idea is the compiler needs to know the function type, including number of
actual arguments, and the name of the symbol, e.g. FbleIStreamImpl. It writes
the wrapper function and profiling things as needed. Probably best to have the
syntax be a function type/call like thing:

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ? FbleStdin <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; };

You can't just use a type or type synonym. We need to know the difference
between taking an argument and returning a function.

static FbleValue* FbleStdin(
    FbleValueHeap* heap, FbleProfileThread* profile,
    FbleFunction* function, FbleValue** args);

The problem's that show up right away:
* FbleStdin needs to be a 3 argument function, because it assumes a type for
  M@ that we aren't allowed to assume on the fble side. Or rather, that I
  don't want to be allowed to assume on the fble side.
* FbleStdin wants to return a function with a static value, but if we can do
  that, we either need to let native code call into fble code, or we're back
  to square 1 with defining our own FbleValue* functions which require manual
  block ids.

Oh well. It was worth a try.

---

We don't need to assume any type for M@. Just use the passed Monad instance
and call return on it. Easy. That's probably a better way to do things anyway.

We don't have to return a function with a static value if we do partial
applies all on the fble side. For example, have 'stdin' return a File@, and
have GetChar take that File@ as an argument. On the fble side, we return a
char IStream by calling stdin, partially applying it to GetChar, and returning
that.

One concern I have with FFI is that people could abuse it. For example, once I
implement some FFI function, what's to prevent anyone from using it and
subverting any type protections?

The answer: have the compiler include the module name in the FFI label. For
example, if my function is 'Foo' and I instantiate the FFI function literal
from module /A/B%, then the FFI function we try to call is /A/B%.Foo. But if
we instantiate an FFI function from some other module /C%, then the FFI
function we try to call is /C%.Foo, which is a completely different function.

For Stdin as above, it would be nice to define a private File@ type that is
constructed and deconstructed entirely on the native side. Now we can do
whatever we want on the native side, so we can use any type at all on the fble
side. For example, Unit@. And we can restrict that Unit@ to the module in
question, so other people can't try to construct wrong things. But that
doesn't prevent us from accidentally constructing the wrong thing.

A suggested workaround: define a single element struct type instead, with some
unique field name to avoid accidentally mistaking two different types.

 @ File@ = *(Unit@ file);

I don't want to define File@ as an Int@ pointer value, because we don't want
people to create them out of thin air.

In terms of syntax, what we need abstractly is:
* The fble type of the function.
* The name of the FFI function.
* The (implicit) name of the module instantiating the FFI function.
* The number of arguments expected by the function.

We could as easily specify the number of arguments separately from the type of
the function. I think that makes sense.

What kind of syntax would make sense? It's something that takes a type as an
argument. So put the type first? Or use <...>. Those are the two precedents
for variations on types. I suppose fields are 'type name'.

Let's play around a bit, shall we?

Say I want an FFI function for:

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } Stdin;

What's a natural way to write it?

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ? FbleStdin<<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }>

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } ? FbleStdin;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ? FbleStdin: <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; };

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } Stdin ?= FbleStdin;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ?FbleStdin<@<Stdin>>;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ?(FbleStdin <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; });

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = (,)(FbleStdin <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; });

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = !FbleStdin(,) { <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; };};

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = !FbleStdin(,)<<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }>

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = !FbleStdin(,)<@<Stdin>>;


<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } Stdin ?= FbleStdin(,);

! <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } FbleStdin(,);

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = !(<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; } FbleStdin);

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = !FbleStdin(,) { <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }; } FbleStdin);

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ^FbleStdin(,) { <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }; } FbleStdin);

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ?FbleStdin(,) { <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }; } FbleStdin);

You can't distinguish between 0 and 1 using the (,,,) syntax.

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = <<@>@ M@>(Native@<M@>, Monad@<M@>) FbleStdio { M@<IStream@<M@>>; };

I guess you can't call a zero argument function, so maybe that's fine?

I feel like I'm leaning towards something like...

    ?<name>(,,) { <type>; }

Where (,,) means it takes 3 arguments. (,) would be 2. () 1.

Because this looks funny with the '?'. It looks like a function value sort of
with the (,,) { ... }, and we specify the type clearly enough, right?

The shortened version looks pretty nice too:

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ?FbleStdin(,) { @<Stdin>; };

Cool. Sounds like a decent initial proposal for syntax.

How could we implement it? Something like func value, but different.

Today the interpreter uses FbleNewInterpretedFuncValue to create the function
value. We just need number of args, number of statics is 0, oh. max_call_args.
What to do about that? And the run function.

The block id is part of the instruction. That's good. We can reuse that same
thing for an FbleForeignFuncValueInstr.

What to do about max_call_args? Force it to 0? We can't call a function?

For example, I'm going to want to call Monad.return...

Then I start to wonder why I can't specify these on the C side. The number of
args and the number of call args, but preferably not the profile block id?

To construct a function value, the backend needs:
* executable
 - num_args we want to supply on the C side.
 - num_statics is 0
 - max_call_args we want to supply on the C side.
 - run we want to supply on the C side.
* profile block id - say this comes from the instruction.
* statics - say this is always empty.

In short, seems like I want to expose an FbleExecutable in C code rather than
a run function? How would that look?

static FbleValue* IStream(
    FbleValueHeap* heap, FbleProfileThread* profile,
    FbleFunction* function, FbleValue** args);

FbleExecutable _Fble_2f_Core_2f_Stdio_2f_Native_25_IStream_ = {
  .num_args = 1,
  .num_statics = 0,
  .max_call_args = 0,
  .run = &IStreamImpl,
};

A bit more tedious than if we could just specify the function. But it really
does make sense to supply those as part of the C side of things.

What does that mean for our concrete syntax?

No worries?

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = ?FbleStdin { @<Stdin>; };

Or take advantage of it?

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = @<Stdin>.|FbleStdin;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }|FbleStdin;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = @<Stdin>|FbleStdin;

In other words, share concrete syntax with 'literal', only in this case the
expr is a function type instead of a function value?

Sure. Why not? No new syntax. That makes things easy from that point of view.

Or, use '.' access, but with function type? Like, a function type access?

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = <<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }.FbleStdin;

<<@>@ M@>(Native@<M@>, Monad@<M@>) { M@<IStream@<M@>>; }
Stdin = @<Stdin>.FbleStdin;

I think the '|' stands out much better, to be honest.

Cool. What do you think? Should I try it out? Draft it up? See if it works,
how it works, how it feels? Or should we punt to the next release?

The type heap knows the current module. So, I propose we have typecheck.c get
the module and insert it into our FBLE_FOREIGN_FUNC_VALUE_TC, which will store
the module path there along with the name. Then have an
FBLE_FOREIGN_FUNC_INSTR, and the backend deals with it just fine.

It should be easy to implement in C and aarch64 backends. Mangle the module
name together with the symbol name, assume that gives you a symbol which
points to an external FbleExecutable or FbleExecutable*? FbleExecutable is
probably easier to implement.

For the interpreter, we want to pass a list of triples of module path, name,
and FbleExecutable or pointer to FbleExecutable. Do we have a mechanism for
that?

Or do we do that in the linker? No, we don't want to crawl through all the
instructions. How can we pass that context to the interpreter?

Use dlsym magic? Or have some global registry? Actually, a global registry
sort of makes sense? Except what if we have a single C program that loads two
different fble code hierarchies? Is that a thing that could happen?

Right now we pass builtin modules to the linker to look for.

Or we could add another argument to RunFunction which is the list of
registered foreign functions. We could even reuse that for aarch64 and C
backends if we don't want to do symbol mangling. Or stuff it into the
FbleValueHeap?

Honestly, FbleValueHeap doesn't sound like a terrible idea. We already have
tail_call_sentinel, tail_call_buffer, and tail_call_argc.

Okay? Then here's what I'm proposing:
* Add an API to register a foreign function with an FbleValueHeap. You pass
  the module name, the function name, and an FbleExecutable (borrowed pointer,
  or copied value, or whatever).
* For all backends, the implementation should call FbleForignFuncValue which
  looks up the module path and function name in the value heap for you. Easy.

Except that requires we populate all the foreign functions we might want in
the FbleValueHeap. Which is annoying for things I don't want to have to do
that, like currently sockets. Which suggests maybe we want to support the name
mangling approach too?

It would be very convenient if we could use name mangling always, instead of
have to provide all the builtins explicitly to the interpreter.

It's like I want some linker magic that let's every module linked in to add to
a shared data structure by the shear fact of its being linked in.

---

To get dlsym like behavior on linux:
* dlsym(NULL, "Foo") seems to work as long as I pass -export-dynamic to gcc.

If I can get it to work on windows too, then we can just go this approach for
the interpreter, right? Maybe same with builtins?

I guess it doesn't solve the bulitins problem if fble-cli is my interpreter
binary and doesn't have the foreign functions linked in. That would need to be
solved with dynamic loading.

---

Turns out it's not hard to do dlsym equivalent on windows. It's just a
different name, an extra call in place of NULL, and different linker flag.

I vote we try adding ffi this release:
* To greatly simplify the native implementations of things like stdio.
* To allow mixing of native and pure fble code in the same module.
* To establish use of dlsym for searching for symbols, which we could use to
  replace explicit builtin module lists when linking eventually.

---

It doesn't make sense to use dlsym at this point. For compiled code we should
just directly reference the symbol. For interpreter we should register a list
and pass through the FbleValueHeap.

Since compiled code can directly reference its FFI code, we never need special
dlsym support for it. We would use the dlsym to load the module itself, but
internally nothing else is needed.

The only case where we might want to dlsym FFI code is if you want to ship
foreign functions for use with interpreted code that you don't want to
compile. Then we need to arrange for where to look for that native code,
perhaps in a specially named .so file. But that's beyond what I need right
now.

I can't figure out how to annotate a symbol as dlsym-able in gcc without using
a blanket -rdynamic flag, which I don't want to deal with. I don't want to
deal with the headache of a non-C99 solution when there's a perfectly fine C99
solution available.

Anyway, let me continue my draft using FbleNewForeignFuncValue helper function
in all the backends, then switch aarch64 and C backends to directly reference
the magic symbol? Or maybe go straight to the magic symbol, because it
simplifies the aarch64 and C backends in theory because we don't need to
define static module paths and string values.

---

There's going to be a question about whether missing foreign function is a
runtime error or a compile (link time) error. Because I think I'm going to end
up with it being a link time (compile time) error for C and aarch64 backend,
but a runtime error for the interpreter. But I can't write a spec test for
that.

Maybe I should use dlsym after all to turn C and aarch64 backend approach into
a runtime error instead of compile time? But surely you rather have it be a
compile error if you can?

Well, the user is in charge of writing the function. So maybe they should be
in charge of registering it too if they want, even for C and aarch64 backends?

---

When registering a foreign function explicitly, it's pretty tedious to
construct and then free the ModulePath. Maybe more reason to reconsider my
decision to register explicitly instead of using dlsym approach?

But maybe it won't be so bad if I just need one module path per module, and I
can use const char* and parse the module path. And if I really want to, I can
change the register function to take const char* and parse the module path
itself. So that's a bit of a lame excuse.

At this point, I think there are two branches we can split down:
1. Switch over everyone to use FFI instead of builtin modules.
2. Figure out the best way to register/lookup foreign functions.

Probably they can be done independently. I assume any changes in (2) will be
minor.

What do you think? Shall we try it out for /Core/Env/Native%? Define GetVar as
a foreign function? Register it with fble-cli and fble-app, and wherever else
we need to?

In terms of whether foreign not found is a compile, link, or runtime error, I
think it's okay to say it's platform dependent. I would like to return the
error at link time if at all possible, because it's better to catch the error
as early as possible. Is there not some existing precedent for the error for
failing to find a builtin module?

How do I feel about the term 'Native', native interface, versus 'Foreign',
ffi? Should we switch to /Core/Env/FFI%? /Core/Env/Foreign?

I want it to be clear from the name it is doing some magic under the covers.
But I also would like consistency in terminology. Let's stick with
/Core/Env/Native% for now.

Maybe I can register the GetVar function with FFI first, then swap
/Core/Env/Native% impl to use FFI, then remove the old builtin module code on
the C side. That should be a good test of how convenient FFI is compared to
builtin modules to use.

---

GetVar implementation is much nicer with FFI. I eliminate a ton of boiler
plate, as planned.

It is unpleasant that I don't get a static error if I get the FFI wrong. It's
hard to know where all I need to register things for it to work properly.

Let's figure this out properly, because I'm afraid to switch things over to
FFI for fear I'll make a mistake and not know about it because we don't have
enough test coverage to figure out I messed up somewhere with finding the FFI
function.

Let's understand symbols better.

Specifically:
* What effect does -rdynamic have on what symbols are exported?
* Is there a way to limit to just the symbols I want to expose?

Let's focus on linux to start, because I honestly think windows will be easier
in this case, based on my experiments.

What I know so far in theory:
* -rdynamic or -export-dynamic option to gcc is needed to see all symbols via
  dlsym.
* There are function attributes like __attribute__((visibility("default")))
  you can put on functions. For windows, the dllexport one works to make a
  specific function visible without any extra options to gcc needed. With
  linux, I haven't gotten a function attribute to make a difference.

In practice I'll be compiling my .o files separately from my final binary. We
can consider these kinds of functions (or symbols in general):
* static private. Shouldn't be exported for any external use.
* not marked static, used in final link.
* not marked static, not used in final link.
* explicitly marked for export, not used in final link, but available to
  dlsym.

First thing to check: can I see the dynamic symbols list in the final
executable, and see the impact of -rdynamic there rather than via dlsym?

Yes. Absolutely:
* objdump -T foo

Without -rdynamic, I'm getting:
* l: .init, .data
* And a bunch of D, DF *UND*
  - abort, dlsym, __libc_start_main_ are DF
  - others like __gmon_start__, __cxa_finalize

Let's see what the various fields mean from objdump:
* l: local
* g: global
* w: weak
* d: debug symbol
* D: dynamic symbol
* F: name of a function
* *UND*: symbols is referenced, but not defined in the file.

So, .init, .data are debug. Otherwise we have some references to dynamic
functions defined externally and some dynamic non-functions defined
externally.

When I add -rdynamic, I get a bunch more stuff included:
* Global symbols defined locally including the ones I want exported, but also
  a bunch of start/ends of sections.

To put this to the test, let me work on a specific object file and try static
functions and a couple non-static functions.

Note that -rdynamic and -export-dynamic appear to have the same results.

If I compile to a .o file, I can no longer run objdump. But I can run nm. I
get something like:

0000000000000038 T Bar
0000000000000018 T Foo
0000000000000000 t FooImpl
0000000000000000 D P

Meanings are:
* D - Data section.
* T, t - text section
* U undefined
* v weak

Lowercase for local, uppercase for global. So we can see here: Bar, Foo, P are
all non-static, and FooImpl is static.

We can run 'nm -a' to get debug symbols too.

We can run 'nm -D' to list dynamic symbols?

Okay, so I split my sample program into separate .o files. What do I need at
each level to get something useful for dlsym?
* I need -rdynamic on the final link. Not on any object files.

Note I can run 'objdump -t' on foo.o to get the list of symbols. The three
symbols I care about show up as global there.

Interestingly, Bar shows up in my final executable, even if I don't use it and
don't export it as a dynamic symbol. Perhaps it would be different if I passed
foo.o into the link as a library.

Okay. What's next? My goal, ideally, is to get the dynamic symbol table, but
only with 'Foo' and not 'Bar'. Somehow, magically.

In theory I can use the --dynamic-list option to ld.

Internet suggests I can:
* Make all symbols in files hidden by default, and explicitly export the ones
  I want.

-fvisibility=hidden + __attribute__((__visibility__("default")))

See explanation of how to do it at:
 https://www.gnu.org/software/gnulib/manual/html_node/Exported-Symbols-of-Shared-Libraries.html

Looks like I could use --version-script for the linker to specify a glob. That
might be nice, if we glob export everything that's _Fble_*. No need to do
anything special at all otherwise?

Sample VERSION file:
 CODEABI_1.0 {
         global: *entry_point*;
         local: *;
 };

Or:

 {
     global:
         JNI_*;
         Java_*;
     local:
         *;
 };

-Wl,--version-script,foo

It sounds like attribute based approach is better, because gcc can do some
compile time optimizations, it can be made to work with windows, and I like
explicitly annotating things. Let me try to get that to work.

Yeah, that works fine. When compiling the object file, specify
-fvisibility=hidden. On the functions I want to export, specify
__attribute__((__visibility__("default"))). On the final link, specify
-export-dynamic or -rdynamic.

Let me try the linker script approach, just for the fun of it.

Example that works:

Foo_VER {
  global: Fo*;
  local: *;
};

I still need -export-dynamic to make it work with dlsym though.

How bad would it be for complexity of symbol table if I used -export-dynamic,
for example on a compiled fble program such as fble-invaders? Let's find out.

We add maybe 70KB to the size of fble-invaders.
Number of exported symbols jumps from 80 to 323:
* All the compiled modules.
* All the Fble library API.

That's most of it. Also some other global variables I have that I typically
would consider internal.

So think about it. Decide what I want to do. We can start by exporting
everything the easy way, then limit what we export as a follow on
optimization.

---

Options we can consider:
* Dynamic loading
 - works for interpreter and compiled code
 - errors only at runtime
* Static linking
 - only works for compiled code
* Explicit registration
 - works for interpreter and compiled code
 - errors only at runtime
* Some mix of the above depending.

It would be much nicer to not have to explicitly register things. To just make
them available with the right symbol, particularly in the interpreter case.
Just in terms of quality of life.

Remember that foreign functions are tied to their module. If we are compiling
the module in question, there is no reason to export the symbol as a dynamic
symbol. There is no reason to look up the symbol dynamically.

It's only for the interpreter case things get tedious.

I do worry about this fble-cli + external package with some native code in
interpreter mode. Today things will just silently fail. For example, if I try
running the network programs in interpreter mode. We get an error like:

$ ./out/pkgs/core/fble-cli -I pkgs/std -I pkgs/core -I pkgs/network -m /Network/Apps/Echo% -- -p 8123
pkgs/network/Network/Apps/Echo.fble:114:32: error: module /Network/Sockets/Native% not found

I wouldn't know that's about to happen unless I tried it.

Is it worth trying to solve that problem right now?

One option to make things easier to register would be to define an
FbleForeignFunction type, which is like FbleExecutable, but without
num_statics, and with extra const char* fields for module path and name. Then
I can register as easily as builtins.

Or... we could go all the way and say, for code where you have some native
component and it isn't linked into the executable, you need to have a .so file
next to your .fble file. If you do, we try to load that and read symbols from
it. Otherwise... error.

It would be nice if we got ffi errors at link time instead of use time. That
way we never see anything for the module working unless the ffi function would
work. Practically I'm not sure that makes much difference though.

If I defined FbleForeignFunction as a type, we could pass that to
FbleNewForeignFuncValue. Interpreter case just looks for a matching registered
one. Compiled case passes the statically computed one. That would be a
convenient way to share the logic for calling FbleNewFuncValue? Or is it
basically the same so it doesn't really matter?

---

It probably wouldn't be hard to list the foreign function names in an
FbleModule somewhere, so we can quickly check at link time that they are all
registered. If I'm concerned about that, or about having different behavior
for foreign functions than builtin modules today.

The only way to address the case of fble-cli trying to interpreted load some
network module that has FFI or builtin references is to supply a shared
dynamic library in the search path, dlopen it, and dlsym search for symbols of
the things we need. I'm not there yet. I don't need to be there yet. But I can
prepare for it.

There is the question of whether we want to explicitly register code or
implicitly do it via attributes, dynamic symbols, etc. Here I'm talking about
the static case for things like stdio or env ffi in fble-cli, where we are
statically linking the foreign code into the interpreter. That's a separate
case. The benefit of explicitly registering is you have the choice not to
explicitly register. The downside is it's slightly more tedious and error
prone, because you might forget to register.

Here's what I vote for now:
1. Define FbleForeignFunction type as defined above. Use that to specify
foreign functions.
2. Have interpreter validate ffi functions are present at link time, not at
run time. Update the spec accordingly.
3. Change aarch64 and c backends to map directly to the target symbol for FFI
 - Which means have a well defined name mangling. I'm happy with
  mangled /Foo/Bar%.Name, for example. I don't think that's ambiguous.
 - We can check things work by removing getenv from app.c.
4. Save dlsym, dlopen, export-dynamic, all the fun stuff for a future release.

---

It's tedious specifying the module path. If we specify it as FbleModulePath in
FbleForeignFunction, it's annoying to make a static version of that. If we
specify it as char*, it's annoying to convert that to FbleModulePath for the
comparison.

Oh well.

I suppose if we used dlsym, we could avoid that entirely by encoding the
module path in the name of the symbol.

Or, I could change FbleNewForeignFuncValue to take the module path as a
string. Then do the conversion from FbleModulePath to string in the compiler
or backend.

---

Foreign functions are slightly different from builtin modules? No. I'm
thinking about whether we have to standardize the map from module path to
mangled name. I think we do, for both builtin and foreign functions. Because
we don't know what backend is going to reference it: C or aarch64. And if we
add dlsym, interpreter will be another possible backend to worry about.

So we should standardize it. What better way than to provide a function to
convert FbleModulePath to mangled string and/or FbleModulePath + field to
mangled string? We will also need to publish it so people know what to name
their modules and foreign functions.

We have to be a little careful to avoid ambiguity I fear. What if someone
tries to put '%' in the name of the function? Then we can't tell where the
module path ends and the function name begins?

For example:

  /A/B/C%.'D%e' versus /A/B/'C%.D'%e

Do we already have this problem? I feel like yes:

  /A/'B/C'% versus /A/B/C%

So maybe let's not worry about it.

In terms of API...
* I have FbleModulePathName already.
* We could add FbleMangleName as a generic way of mangling things, and reuse
  that with FbleModulePathName. Thought it's too bad to have to do the
  intermediate allocations.
* Is there an easy way to get '/Foo/Bar%.C' as an FbleName for use with
  mangling?

Or should I just have:
  FbleString* FbleMangle(FbleModulePath* path, const char* name);

Where we are allowed to set name to NULL. And FbleMangleModulePath which
doesn't have the const char* name argument? Or 

FbleMangleModulePath? and FbleMangleForeignFunction?

Sure. Let's do that. I can put them in module.c and fble-module.h.

---

aarch64 and C backends have been switched over to statically link. It was
easier than I expected. Where does that leave us?

Do I want to do something about checking interpreter foreign function linkage
at module link/load time? What's my policy about making things like GetEnv
available to things like fble-app for interpreting? Should we just require
everything is compiled (I've had this discussion before and it always turns
out that we should continue to support interpreted code...).

If we switch everything to FFI, is there a need for builtin functions anymore?
Does that make it easier to define the foreign function code in per-library
modules that we can dynamically load and check at link time or statically link
in and rely on the symbol being defined that way? Or should I just not worry
about this yet, save it for another release?

Honestly, I think next step should be to convert as much over to FFI as we
can, now that I feel like the interface is reasonable. If we migrate off of
registering and searching for foreign functions by name in the interpreter,
we can remove the path and name fields from FbleForeignFunction, which might
be nice cleanup.

Let's migrate debug then, shall we?

---

Cool. I can start using FFI for the next stdio. Good. I assume I'll come back
for more discussion when we go to test FFI in detail, but for now, let's call
that a wrap.

---

An interesting thing happened. I tried to write:

  External@<File@> Stderr = @<Stderr>|Stderr;

Where External@ is an abstract wrapper around function type (Unit@) { A@; }.

This gave me an error, because External@<File@> isn't a function type. Two
issues here:

1. External@<File@> is a function type. There's no reason we shouldn't be able
to do FFI on it. It's just that it's abstract, so technically we shouldn't
know it's a function type.

2. Even if External@<File@> wasn't a function type, why can't we use FFI to
create it? Have a zero argument function that we call when we instantiate the
FFI. That way we can define arbitrary values using FFI. What do you think?

It should be implementable: When allocating the FFI function, if the number of
arguments is zero, execute it right away and return the result.

Maybe generalize 'foreign function interface' to just 'Foreign Interface'?

---

Let me start from the ground up. How would I implement, work back to the spec.
If it all makes sense, then switch over to foreign values instead of foreign
functions.

Assembly, C back ends:
* Today they call FbleNewFuncValue directly. Change that to
  FbleNewForeignValue, passing in FbleForeignFunction (renamed
  FbleForeign) directly.

Interpreter:
* FbleNewForeignFuncValue should change to:
  FbleLookupForeign, reuse FbleNewForeignValue.

How to handle the 'value' case: num_args is 0.
* Create a local FbleFunction, call the run function directly.

Other changes:
* FOREIGN_VALUE_INSTR

I think it's totally doable and clean. Let's do this. I can do it in small
steps. We have renames, we have reorganization of helper functions, etc.

---

Transition over to foreign values is done. Looks good to me. Code is more
consistent and simpler with our FbleNewForeignValue function taking an
FbleForeign* directly. I like this change.

---

Issue migrating sockets to FFI: How do I do initialization functions? Like
windows needs sockets to be initialized. Stdio wants setlocale to be called.

I could define FFI functions and call them when the module is loaded. Is that
a reasonable thing to do? Have module level side effects?

---

How to test foreign value not found case?

In the interpreter, it's a runtime error. In the compiler, it's a link time
error. The spec test infra doesn't have a way to handle that. The spec says it
could happen either time.

So... what do I do? Just not test it for now? Add one off infra support for
this case? I could do that, right?

---

* Do I use undefined values anymore?
  Like, since FFI, do we really need to support these?

The concept of a header file still makes sense. If somebody wants to ship
pre-compiled code. We do that with undefined values. I think it still makes
sense.

Maybe we can revisit or clean up undefined values in the future when I have
actual use cases of shipping only pre-compiled code.

Although, come to think of it... could we change the syntax for undefined
variables to be FFI? Like, if you leave a variable undefined, it's not that
it's undefined, it's that it's defined natively? And somehow magically get
that to work out with precompiled code?

This is an interesting idea.

So, instead of writing something like:

  External@<File@> GetStdin = @<GetStdin>|GetStdin;
  External@<File@> GetStdout = @<GetStdout>|GetStdout;
  External@<File@> GetStderr = @<GetStderr>|GetStderr;
  (String@, String@) { External@<Maybe@<File@>>; } Open = @<Open>|Open;
  (File@) { External@<Unit@>; } Close = @<Close>|Close;
  (File@) { External@<Unit@>; } Flush = @<Flush>|Flush;

I write something like:

  External@<File@> GetStdin;
  External@<File@> GetStdout;
  External@<File@> GetStderr;
  (String@, String@) { External@<Maybe@<File@>>; } Open;
  (File@) { External@<Unit@>; } Close;
  (File@) { External@<Unit@>; } Flush;

So, that's much simpler for defining foreign values. Right?

For pre-compiled code... as long as the compiler generates the right name for
functions... does that handle all the linkage and stuff?

I guess I don't want to have to use a foreign function for these things. But
is it really such a problem?

Or maybe the problem is it's weird to have a compiled version of the code and
a compiled version of the header file, and have them be two separate things?
Or maybe the compiled version of the code is literally just to provide the
FbleForeign values?

Am I using undefined values anywhere today? Would that use case work if it was
treated as a foreign function interface? That shouldn't be difficult to check.

Current uses:
* /Std/Undef% is kind of built on this. And there are lots of places that is
  used.
* /Pprof/Parser/Proto% Proto@ type definition.
 - But this could be easily done with a normal struct type.

Those are my only current uses of undef expressions.

Couldn't I implement /Std/Undef% as a function from Unit@ to any type A@ that
errors out? It could even take a String argument instead of Unit to explain
what's wrong. What do you think of that?

Honestly, I think I like that better as an approach. Though it's slightly
annoying to have /Std/Undef% depend on Ascii@ or String@?

I don't know. This is pretty tempting to be honest. Get rid of a rarely used,
weird feature. Get rid of builtin modules. Treat precompiled modules as just
repositories of foreign values. That makes a lot of sense, right?

Let's go one step at a time:
1. Remove use of undef for Proto@.
2. Rewrite /Std/Undef% API.
3. Remove undef expression from the language.
4. Use old undef syntax for FFI.

But at some point I should make sure the precompiled case works this way.
Like, I can have a separate header file for a precompiled module.

---

I could get rid of undef expression from the language right now. Because I
don't have any examples of its primary use case now that we've gotten rid of
builtin modules.

Reminder of the primary use case:
* I want to ship precompiled code for a package in some library libfble-foo.@
* I want to provide header .fble.@ files with the types but not the
  implementations of what's defined in libfble-foo.@

If we're sticking with separate .fble.@ header files, you don't need undef
expressions. We could write things hackily using /Std/Undef%:

 (Int@, Int@) { Int@; }
 Sub = /Std/Undef%.Undef.undef<(Int@, Int@) { Int@; }>;

Now, that's not nice to write or read.

I could also use FFI:

 (Int@, Int@) { Int@; } Sub = (Int@, Int@) { Int@; }|Sub;

Still not nice to write or read.

But if we repurpose the undef syntax for FFI, we get it nice to write and
read:

 (Int@, Int@) { Int@; } Sub;

Remember, in this case, we only use the module for its type. We throw away any
code. We never attempt to link with the FFI function. So this works just as
well as undef expression.

The more interesting question in my mind is if we could get rid of the .fble.@
header files entirely.

Note: this is a step beyond. I claim we don't lose anything by getting rid of
undef expression today. But it seems worth at least thinking about.

Let's say I have my libfble-foo.a that I compiled using normal .fble files.
Now I want to ship .fble files, but in this case, not just for the type. For
real. Except I want to rely on my libfble-foo.a entirely for the
implementation.

What would it mean to write:

 (Int@, Int@) { Int@; } Sub;

It means you need to define a foreign function. We need to link with that.

Well, what's weird is not knowing how things are structured. For example, do
we have:

 (Int@, Int@) { Int@; } Add;
 (Int@, Int@) { Int@; } Sub;
 @(Add, Sub);

Or we have:

 *((Int@, Int@) { Int@; } Add, (Int@, Int@) { Int@; } Sub) Module;
 Module;

In one case, we would have 2 FFI functions. In the other 1. But from a type
point of view they are equivalent. But certainly not in the code they generate
or that would need to be generated.

So I don't think FFI gets us in a natural way to headerless .fble code.

Also, it's nice to have a way for the compiler to check the .fble.@ file types
against the .fble files.

I have a choice here that I can make.
1. Leave header files as is. Assume they work well enough for the use case
when I one day actually have that use case.
2. Remove support for header files entirely. Revisit from scratch when I one
day actually have that use case.
3. Work out the use case properly right now.

(3) is out of scope for this release.

But is there really any point in having (1) implemented if it's not fleshed
out, and seems likely I would want to change it as soon as I start using it?
Surely it's better to restrict the language to start?

And also, couldn't you use regular .fble files for that case still anyway?
Though you lose type checking from the compiler...

Different ways I could envision dealing with this in the future:
* Don't ship any .fble or .fble.@ files, embed the type in the compiled code
  and let the compiler look it up in there.
 - But then we lose the nice documentation.
* Have the compiler generate an FFI based .fble file that works with the
  implementation it generates. Read it as a real fble file.
 - But structure ambiguity is still an issue. The fble file is mostly for
   documentation.


This is a bit of a thorny problem. Some misc thoughts:
* If I get rid of undef expression, we rule out the use case of using undef
  for todo. FFI doesn't replace that, because it will give a compile error if
  using compilation. You could use /Std/Undef%.Undef for that, but it's a bit
  tedious.
* I don't want people writing FFI functions in .fble files that they have no
  intention of providing. It's just confusing. Seems like .fble.@ is worth
  keeping around for no reason other than to clearly distinguish between
  something describing a type of a module and something describing the actual
  implementation.
* Builtin modules let you define profiling blocks. When you rely on FFI, you
  only get profiling blocks for each FFI function you define. That puts a
  limit on the structure of how you write your FFI.
* Probably the compiler should generate a type description with compiled code
  and we should type check against that, not on whatever documentation is
  provided along side the precompiled code. The type should be stored in .fble
  syntax, which is already standardized and supports sharing, rather than hard
  code some internal compile representation of the type.

Maybe I should open a separate discussion on module compilation to discuss
this.

thoughts/fble-0.6/modular_compilation.txt suggests we want .fble.@ files
moving forward. So don't get rid of them now.

It also suggests we would be okay to use FFI in those .fble.@ files for FFI
functions we don't expect to ever link.

Let me review why I didn't want to use undef syntax for FFI before and see if
I still agree with that assessment, or something has come up to change my
mind.

Before I do so, here are my biggest concerns with using undef syntax for FFI:
* Seems easy to accidentally forget to define something, and now you get a
  weird link error about FFI?
* It forces you into a statement context for FFI, and it's awkward to do:
  { Foo@ Foo; Foo; }

Though on the flip side, it's awkward to do:

  Foo@ Foo = Foo@|Foo;

And I would argue that the later is much more the common case than the former.

Here's what I had thought previously:
* Looks like I never considered using the undef syntax.

For some reason I thought I had. Maybe that was as part of introducing undef?

See thoughts/fble-0.4/modular_compilation.txt:
* My initial conclusion there was that I don't need undef expressions.
* I changed my mind because it was so much nicer to have.
* I picked let syntax as the nicest syntax for the use case.
* I relied on 'error at use' to justify statement syntax rather than
  expression syntax.

Am I willing to switch FFI syntax over to undef syntax?

I suppose the biggest question is if we want a different variable name than
ffi function. But we could always put them together into a single struct and
import, or rename some other way?
