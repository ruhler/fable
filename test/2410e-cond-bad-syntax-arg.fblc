
struct Unit();
union Fruit(Unit apple, Unit banana, Unit pear);

/// Expect: ERROR
func main( ; Fruit) {
  // An argument has bad syntax.
  ?(Fruit:pear(Unit()) ;
    Fruit:pear(Unit()), ???, Fruit:banana(Unit()));
};
