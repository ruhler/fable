
How do I want Native% / Cli% / Ffi% stuff exposed in the standard library?

What we have today:
* Native@ type, abstract.
* Cli main takes forall M@, Native@<M@>, Monad@<M@>
* I pass the Native@, Monad@ to my FFI functions, but ignore them in practice.
 - Remember, this is a way to try and make it harder for pure code to call FFI
   functions that result in stateful side effects not captured in the type of
   the fble function doing the side effects.

Considerations:
* Is 'Native' a good name to use? Is FFI better? Should something be built
  into the language itself?
* Let's take advantage of the NativeM@ approach to avoid having to pass three
  separate things everywhere.
* Any performance cost of passing unused Native@, Monad@ everywhere?
* Should we change FFI implementations to use the provided Monad@ rather than
  make assumptions about what M@ is?

What's to prevent me from doing something like:

  Native@<Foo@> n;
  getenv<Foo@>(n, m);

In pure code? Anyone can create an undefined value of any type.

It seems a little sad to make people pay performance overhead for all IO when
it's so easy to work around any value that overhead gives us.

---

I see three options.

1. Concrete IO monad.
(String@) { Io@<Maybe@<String@>>; } GetVar = @<GetVar>|GetVar;

With some concrete Monad implementation for composition. And CLI at the top
level is Io@<Exit@>.

Pros:
* Deals with concrete types, which is simple.

Cons:
* Pulls people into writing everything in the Io monad?
* If you want to generalize an Io computation to generic M@, you have to
  rewrite all the IO related calls and types.
* Tedious to lift an Io@ function up to a generic M@ function.

2. Passing a magic key.
<<@>@ M@>(NativeM@<M@>, String@) { M@<Maybe@<String@>>; } GetVar = @<GetVar>|GetVar;

We say only native code can construct/access the key, so if you have one, you
are okay.

Pros:
* People write for generic M@ by default.
* It's more complicated to write things taking NativeM@ than a generic M@.
* It's easy to transition from this generic M@ to some other generic M@ that
  is more targeted without full native access.
* Trivial to lift an Io@ function up to a generic M@ function.

Cons:
* You can fake a key using undefined expressions.
* Easy to forget to check the key is valid on the native side.

3. Combined approach:
(String@) { Io@<Maybe@<String@>>; } GetVarIo = @<GetVarIo>|GetVar;

<<@>@ M@>(NativeM@<M@>, String@) { M@<Maybe@<String@>>; }
GetVar = (NativeM@<M@> n, String@ s) {
  n.io(GetVarIo(s));
};

Pros:
* People write for generic M@ by default.
* It's more complicated to write things taking NativeM@ than a generic M@.
* It's easy to transition from this generic M@ to some other generic M@ that
  is more targeted without full native access.
* Trivial to lift an Io@ function up to a generic M@ function.
  - Because the Ffi code has already done that for you.
* Impossible to forget to check the key, because we do that on the fble side
  where types force us to do it.

Cons:
* Tedious to lift an Io@ function up to a generic M@ function.

It's tricky, because I like the idea of forcing the conversion of the fble
side, but that's exactly what makes approach (3) tedious.

Maybe let's try (3), see how it goes. Maybe it feels okay because we already
have wrapper functions most places. I feel like that's the case with
/Core/Stdio/FFI% at least. So yeah. Let's try it out.

But first, I need to come up with better names.

Two names really that I need, which will lead to three types.
1. The concrete 'locked' type.
2. The interface for unlocking the locked type.

Conceptually what's going on here is we have side effects that impact the
outside world. (1) is an outside-world side effecting computation. (2) is our
interface to the outside world: it lets us run those side effecting
computations. Like an Io computation and an IoRunner kind of thing.

Maybe Io@ and World@ and WorldM@? Io@ is input output. World@ is the thing you
are sending that input/output to?

  (String@) { Io@<Maybe@<String@>>; } GetVarIo = @<GetVarIo>|GetVar;

  <<@>@ M@>(WorldM@<M@>, String@) { M@<Maybe@<String@>>; }
  GetVar = (WorldM@<M@> n, String@ s) {
    n.run(GetVarIo(s));
  };

Hm... doesn't feel right to me. Brainstorm of other potential name sets:
* Io_@, Io@, IoM@
* Ffi@, Io@, IoM@
* 'Io!'@, Io@, IoM@
* IoS@, Io@, IoM@
* Io@, Cli@, CilM@
 - Io@, App@, AppM@
* '!'@, Io@, IoM@

We say IoM is a monad that can perform input/output. Io@ is the input/output
interface. What do you pass to the input/output interface to effect Io? An
IoComputation? 


  Io@<M@> = *(<@ A@>(Io_@<A@>) { M@<A@>; } run);
  Io@<M@> = *(<@ A@>(Ffi@<A@>) { M@<A@>; } run);
  Cli@<M@> = *(<@ A@>(Io@<A@>) { M@<A@>; } run);
  Io@<M@> = *(<@ A@>('!'@<A@>) { M@<A@>; } run);

I don't know.

---

We better not rely on any arguments to FFI functions. We better have them
produce something only native can deal with, and have someone else deal with
them externally. Otherwise too much risk of spoofing.

Proposal:

  External@<A@> = (Unit@) { A@; }.%(@/Std/Io%);

  (String@) { External@<Maybe@<String@>>; } GetVarExt = @<GetVarExt>|GetVar;

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<String@>>; }
  GetVar = (IoM@<M@> n, String@ s) {
    n.run(GetVarExt(s));
  };

The FFI function uses the External@ type directly as a function. No need to
take monad or pretend M@ is something based on Native@ or anything like that.

We don't provide any monad for composing External@, which should strongly
discourage people from writing stuff directly in the External@ monad.

I like this proposal. How hard to try it out? Should I just dive in and hope
for the best? Why not? Can we do it incrementally?

I think we can do it incrementally. Something like:
* Rename Native@ to Io@. [DONE]
* Define IoM@ and start using that. [DONE]
* Define External@. Add run function to Io@ interface. [DONE]
* Migrate FFI over to External@ one by one.
* Move /Core/Env/Native to /Core/Io/Env?
* Move /Core/Cli/* over to /Core/Io/Cli?

Easy, no?

---

IoM@ works fine. We need extra wrapper layers internally, but it's not overly
onerous. This feels much nicer in terms of being unspoofable and simplifying
the types passed to the C side and not having weird implicit assumptions about
types used on the C side.

There's still an open question about whether Io@ versus IoM@ is the right
approach for monad composition. See exploration in
thoughts/fble-0.6/monad_composition.txt.

Ignoring the monad composition question, how would I like to organize all this
native code related stuff?

Straw Proposal:
* /Std/Io%: Io@, IoM@, External@
* /Std/Io/Env%: GetVar
 - Environment variables really are an input/output external to this process
   kind of thing. Otherwise you would pass them internally.
* /Std/Io/Cli%: Exit@, Main@, Success, Failure
 - Command line interface also is an IO kind of thing, I think.

Should we include a Demo app in the standard library? Should that live
elsewhere? Is it just for testing purposes? I kind of feel like we shouldn't
include a random demo app in the standard library. If it's used for testing,
it can live in std-test? It is pretty useful to have the demo app in the app
package though, and that feels less odd to include in that package.

We'll want to decide on the stdio stuff where it might live. Remember the
different things I expect to have:
* Definition of IStream@ type.
* Implementations of GetLine, GetText, IStream.
* Definition of OStream@ type.
* Implementation of NullOutput, OStream.
* FFI implementation of stdin, stdout, stderr, open, read, write, etc.
* Demo apps.
* StringO main helper function thing.
* Conversion between char and byte streams.

Some of those are more clearly Io@ bound. Others not. I think we can organize
them appropriately once I have more clarity on the actual types involved. No
need to block moving Io and Cli stuff into /Std.

There is a question of whether std package should avoid all FFI so you can
reuse it on platforms that don't have FFI. I'm a little torn. We can have the
Io related interfaces like streams even if we don't have FFI. We could put
things into std-io package that involve IO. But is that just more effort than
it's worth right now? I don't have a concrete use case. It's an extra package
to have to deal with. On the other hand, if I'm going to have C code, it is
perhaps natural to put that all in the std-io package and have std without any
C code? Except what about things like Debug, or helper functions for
converting Int, String, Char, etc?

Let me put this on a todo list, but not worry about it for this release. It
shouldn't be too bad to separate out and otherwise reorganize packages later
on. That makes more sense to consider when asking about bringing app package
and network package into /Std. Let me put a generic todo bullet point to worry
about it later.

---

I'm happy with monad composition now. Are we ready to migrate the Io stuff to
std? Here's what it would look like:
* /Std/Io%: External@, Io@
* /Std/Io/Env%: GetVar
* /Std/Io/Cli%: Exit@, Main@, Success, Failure

Let's not worry about the demo for now. We would want to do standard output at
least in the demo app, so we can wait until after sorting out the interfaces
for that.

When I move Io over, I should also move native code:
* io.fble.{h,c}
* cli.fblie.{h,c}
* char, env, int, string

But I'm not sure I'm ready to move these over yet. So maybe keep them with the
core package for now, move them over later.
 
Let's move over those three modules now, get that out and done with.
