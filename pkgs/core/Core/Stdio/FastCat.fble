
@ Bool@ = /Std/Bool%.Bool@;
% False = /Std/Bool%.False;

@ Char@ = /Std/Char%.Char@;

@ Int@ = /Std/Int%.Int@;

<@>@ List@ = /Std/List%.List@;

<@>@ Maybe@ = /Core/Maybe%.Maybe@;

@ World@ = /Core/Monad/IO%.World@;
% m = /Core/Monad/IO%.Monad;

@ Exit@ = /Core/Stdio%.Exit@;
% Success = /Core/Stdio%.Success;

% Stdin = /Core/Stdio/IO/Builtin%.Stdin;
% Stdout = /Core/Stdio/IO/Builtin%.Stdout;

@ String@ = /Std/String%.String@;
% Str = /Std/Char/Ascii%.Str;

@ Unit@ = /Std/Unit%.Unit@;

<@>@ R@ = <@ T@> { *(World@ s, T@ x); };
<@>@ IO@ = <@ T@> (World@) { R@<T@>; };

# Implementation of fble-cat for the purposes of experimenting with
# fble performance.
/Core/Stdio/IO%.Main@ Main = (List@<String@> args) {
  (Int@) { IO@<Unit@>; } stdout_ = (Int@ s, World@ w) {
    Stdout(s, w);
  };

  IO@<Exit@> MCat = {
    Maybe@<Int@> byte := m.do(Stdin);
    byte.?(nothing: m.return(Success));
    Char@ char = /Std/Char%.Char(byte.just);
    Unit@ _ := m.do(stdout_(/Std/Char%.Pos(char)));
    MCat;
  };

  (World@) { R@<Exit@>; } Cat = (World@ w) {
    R@<Maybe@<Int@>> byte = Stdin(w);
    byte.x.?(nothing: R@(w, Success));

    Char@ char = /Std/Char%.Char(byte.x.just);
    R@<Unit@> out = Stdout(/Std/Char%.Pos(char), byte.s);
    Cat(out.s);
  };

  Bool@ monadic = {
    args.?(empty: False);
    /Std/String/Eq%.Eq(Str|'--monadic', args.items.head);
  };

  monadic.?(true: MCat, false: Cat);
};

Main;
