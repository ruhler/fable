Char@
=====

This will be a fun one.

It makes sense to support unicode characters in the standard library. So Char@
represents a unicode character.

Expected operations:
* Construct a character from a literal or unicode code point.
* Check for character equality, or ordering based on unicode code point.
* Get the unicode code point for the character.
* Standard character type classification: isspace, isupper, etc.
* Perhaps standard toupper, tolower kinds of transformations.

Existing fble API:
* Char@, with most of printable latin chars, and 'unknown' byte.
* Chars collection of literals.
* Standard interfaces: Eq, Show, etc.
* Conversion to/from ASCII integer value.
* Character types: IsUpper, IsLower, IsAlpha, IsDigit, IsAlNum, IsSpace,
  IsNewline
* Char@ can currently be used as a literal, implicitly.

I'll want to generalize to support all of unicode presumably.

C99:
* Character literal: source file encoded character literal.
  "mapped in an implementation defined manner to the execution character set".
* Escape sequences for ', ", ?, \, octal, hex
* Escape sequences for \a, \b, \f, \n, \r, \t, \v
* Conversions between char* and wchar_t (mbtowc)
* ctype.h:
  - behavior depends on current locale
  - isalnum, isalpha, isblank, iscntrl, isdigit, isgraph, islower, isprint,
    ispunct, isspace, isupper, isxdigit,
  - tolower, toupper
  - corresponding wide character versions of the above.

Unicode:
* Characters have code points.
* Major category: Letter/Mark/Number/Punctuation/Symbol/Separator/Other
* Minor category: uppercase/lowercase/titlecase/modifier/other
                  spacing/nonspacing/combining/enclosing
                  decimal/digit
                  connector
  etc.
* Basic type: Graphic
* Character: Character
* It's pretty complicated in general. I'm not sure standard library for
  Char@ is the right place to deal with these complexities a Unicode specific
  library would be better suited. Just pick the basics for Char@.

---

Okay! What are we doing for Char now, since it's been a while since I last
thought about this.

* Char@ will be a wrapper around Int@ with the unicode code point for the
  character.
* Define some literal sets:
  - ascii, latin?
* Create a character from a unicode code point. Get the unicode code point of
  a character.
* Character classes
  - but specific to a subset of characters, or all of unicode? Or related to
    your current locale?

Is that it? Basically character literals and character classes?

Java has things like:
* Convert between digit and char, for given radix
* Comparison, equality.
* Unicode database info: name, code point, directionality, category, etc.

Yeah, I think that's kind of it. So I need to figure out what literal sets I
want to provide and what classes or other info I want to supply, and how to
interpret those.

---

* In C99, character classes are locale specific?
* Go refers to 'Latin1' as a code space somewhere. What's that?
  ASCII:  U+0000..U+007F
  LATIN1: U+0080..U+00FF

I suppose we could start with 'C99' as a character set: /Std/Char/C99%, or
Just /Std/Char/C%. The spec clearly lists the characters and defines what is
upper or lower case. It includes almost exactly what I have for Char@ today.
But that's also almost the same as ASCII, so why not start with ASCII, which
is part of unicode?

/Std/Char/Ascii% can define ascii character strings and literals. It can
define character classes ... but only for ascii characters?

How feasible would it be to implement character classes properly for all of
unicode? Isn't that a whole lot of data that most people don't want or need?

---

Currently defined character literals:
* Spaces: newline, space, tab, CR
* Symbols: !"#$%&'()*+,-./:;<=>?@[\]^_`{|}~
* Digits: 0-9
* Letters: A-Z, a-z

Differences between my characters and C99:
* C99 also has: \0, vertical tab, form feed
* C99 also has: alert, backspace
* C99 doesn't have '$' or '@' or '`'.

What else is in ASCII:
* SOH (start of heading)
* STX (start of text)
* ETX (end of text)
* EOT (end of transmission
* ENQ (enquiry)
* ACK (acknowledge)
* BEL '\a' (bell)
* SO  (shift out)
* SI  (shift in)
* DLE (data link escape)
* DC1 (device control 1)
* DC2 (device control 2)
* DC3 (device control 3)
* DC4 (device control 4)
* NAK (negative ack.)
* SYN (synchronous idle)
* ETB (end of trans. blk)
* CAN (cancel)
* EM  (end of medium)
* SUB (substitute)
* ESC (escape)
* FS  (file separator)
* GS  (group separator)
* RS  (record separator)
* US  (unit separator)
* DEL
 
What do I want to include in my character literal?
* All of ascii?
* Just printable characters?
* Non-printable characters as raw bytes, as escape sequences (e.g. \n), or
  both?
* More than just ascii?

What to include in my character definition:
* The Ascii@ type, which is one big enum type.
* Conversion from Ascii@ to unicode code point for Char@.
* An Ascii char literal function that takes List@<Ascii@> to Char@.
 - We could include support for parsing unicode escape sequences and other
   escape sequences here if we wanted to.
* Optional: conversion from Char@ to Ascii@, for use in case expressions
  rather than force use of Eq?
* Implementations of character class functions: IsSpace, IsUpper, etc.?

What not to include in my character definition:
* Eq, Ord, Show. Anything that would tempt people to use it for anything other
  than character literals.

If it wasn't so tedious and I wasn't worried about the memory cost at runtime,
ideally I would have a type that supported anything you could write in
unicode? Wait, I'm sure we've been through this discussion. It's still not
terribly satisfying though.

Limiting to ascii is good, because then if somebody wants to, they could write
their program entirely in ascii. No unicode support required by their
implementation. That is, use of funny characters beyond ascii in standard
library can be restricted to specific modules you can easily avoid including
if you want.

But we still have a composability challenge. If only there was an easy way to
say, given Ascii@ and Latin1@, form a composite string literal that
supports characters from both ranges. If only we had that, then everything
would be real easy:
* Define separate modules for separate unicode code pages, lazily.
* Have a way for a user to define a string literal given a list of types from
  various code pages. For example, something like Str = StrFor[Ascii@,
  Latin1@], etc.

Can I consider this again? I know we had a long discussion about it, but that
would really be very nice. Specifically what I need is:
* Given Ascii@ and Latin1@ types, create an AsciiLatin1@ type.
* Given ascii2Char and latin12Char, create an asciilatin12Char function.

The compiler can do the first thing: create a special kind of unionized type.
It's basically the union of fields of the two types. As long as there are no
conflicts in the type of fields by name, this is doable. We can come up with
some well defined way to define the tags associated with each field.

The compiler could do the second too maybe? It's messy. It's hacky. But it's
technically not difficult and could solve my problem. Consider this for real.
Maybe I have new insight now that can get me further along than my last
attempts.

---

The fble language now supports literals with composition of letter types. We
should be able to proceed now.

What's the approach? Maybe:
* Char@ is a unicode code point.
* We define something called a CharSet@ which has a literal letter type and
  mapping from letter type to Char@. And also implementation of the IsAlpha,
  etc. functions restricted to elements of that character set. The charset
  will also provide a string literal function.
* We define a way to compose two CharSet@ together into a combined charset.
* We provide some standard subsets of unicode for charsets. Basically whatever
  code blocks I personally use: Ascii, whatever has unicode pi symbol, that
  kind of thing.
* Anybody can combine and use their own combination of CharSet to define
  string literals.
* And we can continue to share Char@ and String@ based on unicode for
  everyone.

Sound reasonable?

---

Straw Proposal:
* /Std/Char%:
  Char@,
  Char (construct from unicode),
  CodePoint (get unicode code point, name tbd)
* /Std/Char/Eq%, /Std/Char/Cmp%, maybe /Std/Char/Ord%.
* /Std/CharSet%:
  CharSet@ (interface type for character sets)
   Char@ - Letter type associated with that charset. Name tbd?
   ToChar - Convert to /Std/Char%.Char@. Name tbd.
   Char - Character literal.
   CharE - Character literal with escape sequences.
   Str - String literal
   StrE - String literal with escape sequences.
   IsAlnum, IsAlpha, etc. to test if a character belongs to the charset and
     satisfies the class. Maybe IsChar to test if it belongs to the character
     set. These operate on /Std/Char%.Char@, Not the Char@ letter type.
  CharSets - Compose two charsets together.
* /Std/CharSet/Ascii%
  CharSet@ instance for unicode ascii code page.
* /Std/CharSet/Greek%
  CharSet@ instance for unicode Greek code page. Which has unicode pi, as a
  proof of concept for how composition would work.
* /Std/String%
  String@ - list of Char@
  StrE - Converts String@ to String@ by interpreting some standard escape
         sequences, including something for unicode characters and newline?
  Other string specific things we can worry about separately from the Char@
  part of the library.

Potential character class APIs:
* IsAlnum, IsAlpha, IsCntrl, IsDigit, IsGraph, IsLower, IsPrint, IsPunct,
  IsSpace, IsUpper, IsHexDigit, IsAscii, IsBlank
* Unicode category major / minor? Or are there too many of those?
* ToUpper, ToLower

Potential escape sequences:
* \', \", \?, \\, octal, hex
* \a, \b, \f, \n, \r, \t, \v
* \u, \U

Question: How does this interact with locales? Like, we want a way for the
program to depend on a CharSet% that is passed in as an argument at runtime?
And the user has a way to maybe set a default value to be passing in? That
sounds reasonable to me. People should write their code to take a locale as
input if they want to change behavior based on that.

It sounds like Locale is character set plus more. So we could worry about that
later. A locale will pick a specific character set to use.

Hmm... locale is going to be pretty complicated. And maybe have some impact on
character sets. I need to go a little at a time though. Let's say to start my
goal is to have Char@ and the ability to write whatever character and string
literals you may want to in the source code.

Maybe unicode has more general character categories and sorting and other such
stuff that would replace locale or these Is* functions?

For the sake of making forward progress, let me start by reorganizing the
existing Char@ functions I have to match the above proposed organization. This
one is going to be a bit tricky.

Maybe:

1. Rename /Core/Char% to /Core/Ascii%.
2. Define /Core/Char@ as unicode code point.
3. Switch /Core/String% to use Char@ instead of Ascii@.

If we can get that far, we should be in much better shape.

---

Observations from splitting Char@ and Ascii@ types:
* I fear that using Eq instead of a switch is slow. But I guess there isn't an
  alternative if we are representing Char@ as an Int@ internally. For things
  like converting Char@ to digit value 0-9, we could do arithmetic on the code
  point.
* I commonly use Str[c] to make a single element string. That switches now to
  List[c].
* Fail|... turns into Fail(Str|...). That makes literal functions slightly
  less reusable than they were before. I don't want people passing around
  ascii everywhere. String@ and Char@ should be the standard types.
* I want to expose List@<Ascii@> as a type for use in other literals, like
  Bytes, or, you could imagine, Int.

---

The difference between String@ and List@<Ascii@> feels pretty painful. Also,
having to do Eq everywhere for linear search instead of a direct case on
Ascii@. Maybe worth thinking more about.

---

After reading more of the unicode spec, here's my thinking:
* /Std/Char% is just the Char@ type with conversion to/from Int@.
* /Std/Char/Lit% is an API for literals from some literal type T@
  - T@ to Char@
  - Char, CharE, Str, StrE literals, 
  - A way to compose two literals.
* /Std/Char/Ascii% is Ascii literal, and maybe the character classes of use.

But I should review the character classes to see if they are really useful. I
feel like in most cases specific character classes should be application specific.

That leaves two things:
* Figure out what to do with /Core/Char/Type% specifically.
* Figure out what to name everything.

Review of existing /Core/Char/Type% uses:
* stdio GetLine uses IsNewline. Could as easily be Eq(Chars.nl).
* Fbld @define uses IsSpace. Probably ought to be app specific?
* Fbld Loc uses IsNewline to know when to advance line counter.
* Fbld parse uses IsAlNum for IsNameChar.

In general, seems like I use IsNewline function out of convenience. Maybe move
that to /Std/Char/Eq%. The others probably ought to be app specific. Things
like fbld parsing, or dimacs. Depends on the input program. If the input
program is in ascii, makes sense perhaps to put the character class in ascii.

Ascii standard itself doesn't say anything about character classes. I think
that's from C99. Well, ascii says "Control Character" versus "Graphic
Character".

First names to figure out:
* Char@ field name.
* Functions to convert between Char@ and Int@.

Influences:
* Unicode standard clearly calls the int value 'code point'.
* Ascii doesn't give a name for code point.
* Mathematic codes are a mapping from strings in a source alphabet to strings
  in a target alphabet. They use the term 'code word'.
* Java uses 'toCodePoint'
* go calls a code point a 'rune' because they like the shorter name. Otherwise
  it's just a type alias for integer.
* Haskell uses toEnum, fromEnum or chr, ord.

I agree with go in that the notion of a character is not clear. Like, Char@ does not correspond to unicode's notion of an abstract character.

This is complicated.

---

Let's think in terms of improvement over existing library. Adding support for
unicode is an improvement. Representing Char@ as Int@ code point is an
improvement.

Brainstorm of names:
* Char@. Makes most sense. Let's go with that. Good enough for now. Can
  revisit later if needed. I don't think Rune@ conveys anything meaningful.
  CodePoint@? UnicodeCodePoint@? No. Stick with Char@ for now.

For the name of the constructor, converting Int@ to Char@:
* Char - though that is overloaded with literals.
* Unicode? Ascii? - but we can provide literals for those if we wanted?
  U|00a0, A|0065, for example.
* Let's go with Char for now.

For the name of the deconstructor, converting Char@ to Int@:
* ValueOf
* CodePoint
* Pos

The origin of the term 'code point' is interesting. They represented a code as
a bunch of tables of values. 'point' was the coordinate in that table
identifying a particular value.

* Point
* FromChar (contrast with ToChar?)
* CP
* .cp, .codepoint, .pos.

Pos is interesting. Short for 'position in the unicode tables'. It's short.

* Id - short for identifier.

Let's go with Pos and .pos to start.

Should it be public or private? If we make it public, we could use Char@ as
well, and @(pos: ...). Any reason not to make it public? It's not like we have
much other choice for how we want to represent it.

Okay? Let's move forward with this then.

---

We have a pretty tight loop between Char@ and String@, because character
literals depend on strings.

/Std/Char/Lit% is something like:
  <@>@ Lit@ = <@ T@> { *(
     (List@<T@>) { Char@; } Char,
     (List@<T@>) { String@; } Str
  ); };

Actually, I think we can derive everything automatically as long as we have
the function from T@ to Char@, right?

  <@>@ Lit@ = <@ T@>(T@) { Char@; };

I can define Char, CharE, Str, StrE all in terms of that. And also provide:

  (Lit@<A@>, Lit@<B@>) { Lit@<Pair@<A@, B@>>;} Combine;

That way you can get Char, CharE, Str, StrE out of any combination of
literals.

Ascii% module should provide Ascii@, Lit@ instance, and direct definitions of
Char, CharE, Str, StrE, as well as any desired character classes.

Anyway, /Std/Char/Lit% will depend on /Std/String%. Maybe rename Lit to
Literal, since we don't expect to be naming it explicitly that often?

---

What to do about character type classes like isalnum? Where should those live?
What all classes should I support? On which character sets should I support
these?

Options:
* Don't provide any support for character classes.
* Character class applies to specific character set.
  So, isalnum in Ascii means the character belongs to the ascii set and is
  alpha numeric.
* Full unicode support for these character class things.
* Do something locale based like wctype, iswalnum, isalnum_l.

Current use cases:
* IsNewline for GetLine implementations.
* IsSpace for pprof parser test to skip over space in my literal pprof file.
* IsSpace in sat solver for dimacs parser.
* IsNewline in fbld loc line number tracking.
* IsSpace, IsNewline, IsAlNum in fbld parser.

In all these use cases, I really ought to be using a narrower definition of
what I'm looking for. IsAlNum and IsSpace in the fbld parser, for example,
entirely depend on the fbld language spec. I'm not free to make that decision
as an fble standard library developer.

So unless there is a common definition of these things used by all these use
cases, I feel like I shouldn't provide character class support at all in the
standard library. Anyone who uses it for anything other than a specification
that has the same interpretation and definition gets no value.

I'm thinking the first task here is to understand the underlying specs for all
my use cases, switch to implementing those precisely. And then probably it
makes sense to remove the character classes entirely unless something
obviously consistent shows up that is worth standardizing, in which case it
will be obvious what definition to use for that standardization.

---

* GetLine - is \n, per /Std/String/Lines%.
 - I get just use it from there, right?
* /Pprof/Parser/Tests%
 - Space or newline.
 - Maybe just hard code them? Or search in StrE|' \n'? Or anything except hex?
* SAT DIMACs
 - Space, tab, or newline according to the spec.

---

I ended up basically inlining everything. But ... oh, it feels like it would
be so nice to have ascii definitions for Alpha, Lower, Upper, AlNum, Space,
etc... Or at least an easier way to define and check ranges of characters?

---

* Add back character type functions isspace, isalnum, etc?

For Rosetta it would certainly be nice to have IsSpace. But again, it feels
like there isn't a clear universal standard version of isspace that meets the
needs for all the applications.

Why not C99 isspace function?

 isspace()
        checks for white-space characters.  In the "C"  and  "POSIX" lo‐
        cales,  these  are: space, form-feed ('\f'), newline ('\n'), car‐
        riage return ('\r'), horizontal  tab  ('\t'),  and  vertical tab
        ('\v').

Because it depends on the locale. And I don't have a notion of locale really,
aside from assuming a unicode compatible one.

I could provide: /Std/Char/C%.IsSpace and friends? Or
/Std/Char/Posix%.IsSpace and friends? That's well defined. But that would also
encourage use of that behavior, and who knows if that's the right thing to use
for the application in question?

I could define /Std/Locale/C%, /Std/Locale/Posix%, etc. modules that define
all these locale specific behaviors. Or I could pull something from unicode
standard.

Maybe I need ICU u_isspace?

Also, internet suggests wchar_t is 16 bits on windows, so is not going to work
for all my unicode needs.

Which begs the question: should I use notion of 'locale' instead of 'unicode'
for Char@?

But posix suggests it's a bad idea to have a single global locale that
everyone has to use. Better to be explicit to what the app needs?

But we would want some way to ask the system what default locale it should
use. That comes from the system, right?

Whose job should it be to specify the current locale? Both in terms of which
locale we want to use, and what the definition of that locale is?

From ICU website:

   Note: There are several ICU whitespace functions. Comparison:

     • u_isUWhiteSpace=UCHAR_WHITE_SPACE: Unicode White_Space property; most
       of general categories "Z" (separators) + most whitespace ISO controls
       (including no-break spaces, but excluding IS1..IS4)
     • u_isWhitespace: Java isWhitespace; Z + whitespace ISO controls but
       excluding no-break spaces
     • u_isJavaSpaceChar: Java isSpaceChar; just Z (including no-break
       spaces)
     • u_isspace: Z + whitespace ISO controls (including no-break spaces)
     • u_isblank: "horizontal spaces" = TAB + Zs

So once again, I'm feel like the application really ought to define whatever
version of isspace is appropriate for it. It doesn't make sense to provide a
standard one that sort of works, but doesn't always work. Even if the standard
one is well specified to be, e.g. C locale isspace or something.

---

The more I read, the more I feel like wchar_t may not be the right solution.

There is not guarantee that wchar_t is unicode code points. On windows it
decidedly isn't. Certainly C99 doesn't tell us anything about wchar_t being
unicode or large enough for unicode.

Having Char@ refer to unicode code points still makes sense to me, I think.
The concern is more the implementations of:
* FbleNewCharValue - convert C char to fble char.
* FbleCharValueAccess
* PutChar - writing an fble character to a file.
* GetChar - reading an fble character from a file.
* FbleStringValueAccess, FbleNewStringValue
 - Used for opening files by name, reading env vars, etc.

Windows is going to have issues on the command line regardless if I don't take
special care to prevent it from turning π into p.

Let's review what go does:
* go.dev/blog/strings.
* strings are sequences of bytes.
* source code is UTF8 encoded.
* 'rune' is their term for unicode code point, represented as int32
* you can do for-each on a string that goes UTF8 char at a time
  - The string is UTF8 encoded. You see a sequence of runes.

That doesn't really help much here. Maybe with string literals, if I wanted to
make the same assumption about UTF8 source encoding, but that's a different
problem than I'm worried about right now.

I can use Char@, have it be unicode code point, provide libraries for working
with unicode in the language in fancy ways. That's all fine.

But how do I translate characters into and out of fble? How do I write
characters to a file? How do I interpret the characters of a command line
argument?

---

Let's move this discussion to thoughts/fble-0.6/unicode.txt, since it is
talking about something slightly broader than the Char@ standard library type.

---

Back to this question:

* Add back character type functions isspace, isalnum, etc?

Now that the discussion in thoughts/fble-0.6/unicode.txt has led to us keeping
the existing unicode code point based Char@ type and removing notions of
locale from the platform by assuming UTF-8.

I feel like it would be useful to define all these, specifically for a C99 "C"
locale, with suitable module name. People can use it if they want. If they
need something different, they don't use it. Maybe: /Std/Char/C/Type%? Or
/Std/Char/C%? /Std/Char/CType%? /Std/Char/Ascii/Type%?

Double check the C spec and the specs for all these functions. See if there's
any ambiguity in how they are defined for ascii. If not, then I vote we go for
/Std/Char/Ascii/Type%. That makes it pretty clear what values to expect, and
that it only covers ascii, and it fits in with the existing Ascii submodule.

---

From C99:
* isalnum = isalpha OR isdigit
* isalpha = isupper OR islower, in the "C" locale. Otherwise there could be
  more.
* isblank: Space and Tab., in "C" locale.
* iscntrl: "any control character".
* isdigit: decimal digit
* isgraph: printing character except space.
* islower: lowercase letters
* isprint: printing character including space.
* ispunct: print - (space | alnum)
* isspace: ...

I think we can do this as ascii and make sense of it that way.

Where can I see the definition of the C locale?

I'm not convinced I want all of these. I'm not convinced yet that I want any
of these. But let's break it down:
1. Which of these have clear definitions?
2. Which of these do I want, if any.

I have the list of ascii chars. For any of them, I can test the difference
between C and POSIX locale. Though I wish I could just see the definition.
Hmm...

. isalnum: a-zA-Z0-9
. isalpha: a-zA-Z
. isascii: 0 - 127
. isblank: space or tab
. iscntrl: 0 - 0x1F, 0x7F
. isdigit: 0-9
. isgraph: print not space
. islower: a-z
. isprint: 0x20 - 0x7E
. ispunct: print not alnum
. isspace: \f\n\r\t\v
. isupper: A-Z
. isxdigit: 0-9a-fA-F

So, question is, what counts as print and what counts as control.

Apparently 'C' and 'POSIX' are identical locales.

C99 spec says these are graphic characters:

   !"#%&'()*+,-./:
   ;<=>?[\]ˆ_{|}˜

So print must be graphic plus space.
Control characters are alert, backspace, \r, and \n

Oh, here we go, from C99 spec:

   167) In an implementation that uses the seven-bit US ASCII character set,
   the printing characters are those whose values lie from 0x20 (space)
   through 0x7E (tilde); the control characters are those whose values lie
   from 0 (NUL) through 0x1F (US), and the character 0x7F (DEL).

So it's well defined for ASCII, though that may differ from non-ascii C
systems. And I can double check myself easily enough by writing a program to
loop through all the ascii characters. But at least, if I'm putting it under
Ascii module, I can use this definition.

Next question: do I want any or all of these? Useful ones I've wished at some
time or another that I had:
* isalnum, isalpha, isdigit, isspace, isxdigit.
* Probably isupper and islower at some point, but I don't know what for.

These I don't have intuition for: isblank, iscntrl, isgraph, isprint, ispunct.

---

Here's my proposal:

/Std/Char/Ascii/Type% defines all the C99 standard functions listed above.

I still believe any character classification you use should be as specified
for the application in question. You should not rely on the definitions of
ascii character types for the specification of your app.

But just because these aren't intended to be used for app specifications, and
may not be useful depending on the app specifications, doesn't mean there
won't be some apps that can and would like to use them. They are well defined,
independent of locale, and clearly labelled as Ascii.

