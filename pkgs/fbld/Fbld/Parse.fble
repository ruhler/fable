
List@ .= /Std/List%;

Markup@ .= /Fbld/Markup%;
Block .= /Fbld/Parse/Block%;
Run .= /Fbld/Parse/M%;
Result@ .= /Fbld/Result%;
Text@ .= /Fbld/Text%;

# Parses fbld markup.
(List@<Text@>) { Result@<Markup@>; } Parse = (List@<Text@> inputs) {
  Run(Block, inputs);
};

@(Parse);
