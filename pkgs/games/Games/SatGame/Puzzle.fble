
@ Bool@ = /Std/Bool%.Bool@;
@ Int@ = /Std/Int%.Int@;

<@>@ List@ = /Std/List%.List@;

@ VarId@ = Int@;
@ Var@ = *(VarId@ id, Bool@ pos);

@ Clause@ = List@<Var@>;
@ Puzzle@ = List@<Clause@>;

@(Puzzle@, Clause@, Var@, VarId@);
