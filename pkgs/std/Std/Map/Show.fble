# @module[/Std/Map/Show%]
#  The Show@ instance for Map@.

Char@ .= /Std/Char%;
Format, String .= /Std/Char/Ascii/Stream%;
Map@, Entry@, ForEach .= /Std/Map%;
Show@ .= /Std/Show%;
Stream@, Puts .= /Std/Stream%;
Unit@ .= /Std/Unit%;

# @func[Show] Show a Map@ value.
#  @arg[@][K@] The key type.
#  @arg[Show@<K@>][show_key] How to show a key.
#  @arg[@][V@] The value type.
#  @arg[Show@<V@>][show_value] How to show a value.
#  @returns[Show@<Map@<K@, V@>>] How to show a map.
#
#  For example, to show a map from Int@ to Bool@:
#
#  @code[fble] @
#   Show@<Map@> Show = /Std/Map/Show%.Show(/Std/Int/Show%.Show, /Std/Bool/Show%.Show);
#
#   Map@ map = Put(Put(Empty, Int|2, False), Int|3, True);
#   Show(map);
#
#  Which in this case would show the following sequence of characters:
#
#  @code[txt] @
#   { 2 => False; 3 => True; }
<@ K@>(Show@<K@>)<@ V@>(Show@<V@>) { Show@<Map@<K@, V@>>; }
Show = <@ K@>(Show@<K@> show_key)<@ V@>(Show@<V@> show_value, Map@<K@, V@> map) {
  Unit@ _ := Puts(ForEach(map, String|'{ ',
    (Stream@<Char@> s, Entry@<K@, V@> e) {
      Unit@ _ := Puts(s);
      Format|'%s => %s; '[show_key(e.key), show_value(e.value)];
    }));
  String|'}';
};

@(Show);
