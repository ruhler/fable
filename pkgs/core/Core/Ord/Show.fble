
@ Ordering@ = /Core/Ord%.Ordering@;

<@>@ Show@ = /Core/Show%.Show@;

% Str = /Core/String%.Str;

Show@<Ordering@> Show = (Ordering@ a) {
  a.?(less: Str|Less, equal: Str|Equal, greater: Str|Greater);
};

@(Show);
