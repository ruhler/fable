
Char@ .= /Std/Char%;
FormatE .= /Std/Char/Ascii/Stream%;
Stream@ .= /Std/Stream%;

ShowBit(.Show) .= /Bit/Show%;
Bit4@ .= /Bit4%;

(Bit4@) { Stream@<Char@>; } Show = (Bit4@ a) {
  FormatE|'%s%s%s%s'[ShowBit(a.3), ShowBit(a.2), ShowBit(a.1), ShowBit(a.0)];
};

@(Show);
