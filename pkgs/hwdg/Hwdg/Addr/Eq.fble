
% True = /Std/Bool%.True;
% False = /Std/Bool%.False;

<@>@ Eq@ = /Std/Eq%.Eq@;

@ Addr@ = /Hwdg/Addr%.Addr@;

Eq@<Addr@> Eq = (Addr@ a, Addr@ b) {
    /Std/Int/Eq%.Eq(a.x, b.x).?(false: False);
    /Std/Int/Eq%.Eq(a.y, b.y).?(false: False);
    True;
};

@(Eq);
