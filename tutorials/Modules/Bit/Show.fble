
Char@ .= /Std/Char%;
Stream@ .= /Std/Stream%;
String = /Std/Char/Ascii/Stream%.String;

Bit@ = /Bit%.Bit@;

(Bit@) { Stream@<Char@>; } Show = (Bit@ a) {
  a.?(0: String|0, 1: String|1);
};

@(Show);
