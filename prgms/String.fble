
@ Char@ = /Char%.Char@;

<@>@ S@ = /List%.S@;
<@,@>@ List@ = /List%.List@;
<@>% S = /List%.S;

@ Str@ = S@<Char@>;
<@>@ CharLit@ = List@<Char@>;

(CharLit@){S@<Char@>;} Str = (CharLit@ mkList) {
  S<Char@>(mkList);
};

@(Str@, CharLit@, Str);
