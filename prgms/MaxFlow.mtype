
mtype MaxFlow<Node> {
  using List { S; };
  using UBNat { Nat; };

  struct Edge(Node src, Node dst);

  # Return the max flow from source node src to destination node dst in the
  # graph defined by the given list of edges.
  func MaxFlow(S<Edge> edges, Node src, Node dst ; Nat);
}
