
struct Unit();
union Bool(Unit true, Unit false);
union Fruit(Unit apple, Unit banana, Unit pear);

/// Expect: ERROR
proc main( ; ; Fruit) {
  ?(Fruit:pear(Unit()) ;
    $(Fruit:pear(Unit())),
    $(?(Bool:true(Unit()); Fruit:apple(Unit()), Unit())),   // This argument is maltyped.
    $(Fruit:banana(Unit())));
};
