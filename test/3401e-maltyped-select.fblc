
struct Unit();
union Bool(Unit true, Unit false);
union Fruit(Unit apple, Unit banana, Unit pear);

/// Expect: ERROR
proc main( ; ; Fruit) {
  // The select expression is maltyped.
  ?( ?(Bool:true(Unit()) ; Fruit:pear(Unit()), Unit()) ;
    $(Fruit:pear(Unit())), $(Fruit:apple(Unit())), $(Fruit:banana(Unit())));
};
