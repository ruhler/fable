# @@fble-test@@ none
# This module is referenced by the AllModules test.
@ Unit@ = *();
Unit@ Unit = Unit@();

@ Bool@ = +(Unit@ true, Unit@ false);
Bool@ True = Bool@(true: Unit);

# Package type for all modules special case.
@ Tok@ = %();
@ AbsBool@ = Tok@<Bool@>;
AbsBool@ AbsTrue = Tok@.<AbsBool@>(True);
AbsTrue;
