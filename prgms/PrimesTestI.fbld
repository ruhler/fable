mtype PrimesTestI {
  using UnitM { Unit; };
  using UBNatM { Nat; };
  using ListM { S; };

  union TestName(
      Unit NthPrime_1_10
  );

  struct TestFailure(TestName name, Nat a, Nat b);
  func Test( ; S<TestFailure>);
};

