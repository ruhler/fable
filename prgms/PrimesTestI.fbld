interf PrimesTestI {
  import @ { UnitM; UBNatM; ListM; };
  import UnitM { Unit; };
  import UBNatM { Nat; };
  import ListM { S; };

  union TestName(
      Unit NthPrime_1_10
  );

  struct TestFailure(TestName name, Nat a, Nat b);
  func Test( ; S<TestFailure>);
};

