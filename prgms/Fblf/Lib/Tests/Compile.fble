
% True = /Bool%.True;

% Chars = /Char%.Chars;

@ Output@ = /Fblf/Compile/Output%.Output@;
% Output = /Fblf/Compile/Output%.Output;

<@>@ Maybe@ = /Maybe%.Maybe@;

<@>@ Get@ = /Process%.Get@;
<@>@ Put@ = /Process%.Put@;

@ Stdio@ = /Stdio%.Stdio@;

@ String@ = /String%.String@;
% Str = /String%.Str;

@ Unit@ = /Unit%.Unit@;


Stdio@ Main = (Get@<Maybe@<String@>> _, Put@<String@> put) {
  #Output@ output = /Fblf/Test/Compile%.Compile(/Fblf/Lib/Add/Tests%.Tests);
  Output@ output = /Fblf/Test/Compile%.Compile(/Fblf/Lib/Md5/Tests%.Tests);
  Unit@ _ := Output(put, output);
  Unit@ _ := put(Str[Chars.nl]);
  Unit@ _ := put(Str[Chars.nl]);
  !(True);
};

Main;
