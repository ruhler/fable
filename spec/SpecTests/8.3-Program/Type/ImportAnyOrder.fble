# @@fble-test@@ no-error
# It's perfectly valid to import the Bool module before the Unit module,
# even though the Bool module depends on Unit.
% True = /SpecTests/'8.3-Program'/Type/ImportAnyOrder/Bool%.True;
@ Unit@ = /SpecTests/'8.3-Program'/Type/ImportAnyOrder/Unit%.Unit@;

Unit@ u = True.true;
u;
