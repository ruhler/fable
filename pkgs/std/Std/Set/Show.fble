# @module[/Std/Set/Show%]
#  The Show@ instance for Set@.

Char@ .= /Std/Char%;
Format, String .= /Std/Char/Ascii/Stream%;
Set@, ForEach .= /Std/Set%;
Show@ .= /Std/Show%;
Stream@, Puts .= /Std/Stream%;
Unit@ .= /Std/Unit%;

# @func[Show] Show a Set@ value.
#  @arg[@][T@] The element type.
#  @arg[Show@<T@>][show] How to show an element.
#  @arg[Set@<T@>][set] The set to show.
#  @returns[Stream@] The shown contents of the set.
#
#  For example, to show a set of Int@:
#
#  @code[fble] @
#   Show@<Set@> Show = /Std/Set/Show%.Show(/Std/Int/Show%.Show);
#
#   Set@ set = Set[Int|3, Int|1, Int|2];
#   Show(set);
#
#  Which in this case would show the following sequence of characters:
#
#  @code[txt] @
#   { 1; 2; 3; }
<@ T@>(Show@<T@>) { Show@<Set@<T@>>; }
Show = <@ T@>(Show@<T@> show, Set@<T@> set) {
  Unit@ _ := Puts(ForEach(set, String|'{ ',
    (Stream@<Char@> s, T@ x) {
      Unit@ _ := Puts(s);
      Format|'%s; '[show(x)];
    }));
  String|'}';
};

@(Show);
