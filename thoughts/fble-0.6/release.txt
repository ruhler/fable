Fble 0.6 Release
================

It's fairly late in the release development cycle now. How to finish out this
release?

The main thing for this release is standard library cleanup. I'm already
punting documentation for the standard library to later, because it's clearly
too much work. The standard library cleanup is a slog.

Other notable things I've changed along the way for the standard library
cleanup:
* Foreign Function Interface
* Composite character literal support (?)
* Change <- to := for monadic bind
* Support for '$' function application.

There's still a long way to go with the rest of the standard library cleanup.
I fear I'll have to give up at some point. Anyway, status is something like:

* Random numbers, API worked out in some detail. Needs next round of review
  and cleanup.
  - pkgs/core/Core/Bool/Rand.fble   ==> /Std/Bool/Random% [DONE]
  - pkgs/core/Core/Int/Rand.fble    ==> /Std/Int/Random% [DONE]
  - pkgs/core/Core/List/Rand.fble   ==> /Std/List/Random% [DONE]
  - pkgs/core/Core/Random/Lcg.fble  ==> /Std/Random/Lcg% [DONE]
  - pkgs/core/Core/Random/Lfsr/Tests.fble
  - pkgs/core/Core/Random/Lfsr.fble ==> /Std/Random/Lfsr% [DONE]
  - pkgs/core/Core/Random/Prng/Pure.fble ==> /Std/Random/Prng/Pure% [DONE]
  - pkgs/core/Core/Random/Prng.fble ==> /Std/Random/Prng% [DONE]
  - pkgs/core/Core/Random/Rng.fble ==> /Std/Random/Rng% [DONE]
* Cleanup standard Cmp/Ord interfaces.
  - pkgs/core/Core/Bool/Ord.fble        ==> /Std/Bool/Cmp% [DONE]
  - pkgs/core/Core/Char/Ord.fble        ==> /Std/Char/Cmp% [DONE]
  - pkgs/core/Core/Int/IntP/Ord.fble    ==> /Std/Int/IntP/Cmp% [DONE]
  - pkgs/core/Core/Int/Ord.fble         ==> /Std/Int/Cmp% [DONE]
  - pkgs/core/Core/List/Ord.fble        ==> /Std/List/Cmp% [DONE]
  - pkgs/core/Core/Ord/Eq.fble          ==> /Std/Cmp/Eq% [DONE]
  - pkgs/core/Core/Ord/Show.fble        ==> /Std/Cmp/Show% [DONE]
  - pkgs/core/Core/Ord.fble             [REMOVED]
  - pkgs/core/Core/String/Ord.fble      ==> /Std/String/Cmp% [DONE]
* Start work on Map cleanup
  - pkgs/core/Core/Char/Map/Tests.fble
  - pkgs/core/Core/Char/Map.fble
  - pkgs/core/Core/Char/Tests.fble
  - pkgs/core/Core/Int/IntP/Map/Eq.fble
  - pkgs/core/Core/Int/IntP/Map.fble
  - pkgs/core/Core/Int/Map/Eq.fble
  - pkgs/core/Core/Int/Map/Tests.fble
  - pkgs/core/Core/Int/Map.fble
  - pkgs/core/Core/List/Map.fble
  - pkgs/core/Core/Map/Eq/Tests.fble
  - pkgs/core/Core/Map/Eq.fble
  - pkgs/core/Core/Map/Show/Tests.fble
  - pkgs/core/Core/Map/Show.fble
  - pkgs/core/Core/Map/Test.fble
  - pkgs/core/Core/Map/Tests.fble
  - pkgs/core/Core/Map/Tree/Tests.fble
  - pkgs/core/Core/Map/Tree.fble
  - pkgs/core/Core/Map.fble
  - pkgs/core/Core/String/Map/Tests.fble
  - pkgs/core/Core/String/Map.fble
  - pkgs/core/Core/Set/Eq.fble
  - pkgs/core/Core/Set/Show.fble
  - pkgs/core/Core/Set/Tests.fble
  - pkgs/core/Core/Set.fble
* Next round of Stdio@ API revisions
  - pkgs/core/Core/Char/Stream/IStream.fble ==> /Std/Stream/State% [DONE]
  - pkgs/core/Core/Char/Stream/OStream.fble ==> /Std/Stream/State% [DONE]
  - pkgs/core/Core/Char/Stream.fble ==> /Std/Stream/Text% [DONE]
  - pkgs/core/Core/Stdio/Cat.fble   ==> /Std/Io/Cli/Demo%
  - pkgs/core/Core/Stdio/FFI.fble   ==> /Std/Io/File/*  [DONE]
  - pkgs/core/Core/Stdio/HelloWorld.fble   ==> /Std/Io/Cli/Demo%
  - pkgs/core/Core/Stdio/StringO.fble ==> /Std/Io/Cli/Print% [DONE]
  - pkgs/core/Core/Stream/Char.fble ==> /Std/Stream/State% [DONE]
  - pkgs/core/Core/Stream/IStream/Pure.fble [REMOVED]
  - pkgs/core/Core/Stream.fble      [REMOVED]
* Revise state monad based on random numbers api design?
  - pkgs/core/Core/Monad/State.fble [Removed]
  - pkgs/core/Core/State/Pure.fble ==> /Std/State/Pure% [DONE]
  - pkgs/core/Core/State.fble      ==> /Std/State%      [DONE]
* Next round of formalization/cleanup of native APIs
  - pkgs/core/Core/Cli/Demo.fble   ==> /Std/Io/Cli/Demo%
  - pkgs/core/Core/Cli.fble        ==> /Std/Io/Cli% [DONE]
  - pkgs/core/Core/Env/Native.fble ==> /Std/Io/Env% [DONE]
  - pkgs/core/Core/Native.fble     ==> /Std/Io%     [DONE]
* Character type classes:
  - pkgs/core/Core/Char/Type.fble  [REMOVED]
* Finish porting test cases over to std-test
  - pkgs/core/Core/Int/Show/Tests.fble
  - pkgs/core/Core/Int/Tests.fble
  - pkgs/core/Core/String/Tests.fble
  - pkgs/core/Core/Test/Tests.fble
  - pkgs/core/Core/Tests.fble
  - pkgs/core/Core/Debug/Tests.fble
* Figure out what to do with all the other miscellaneous stuff.
  - pkgs/core/Core/CRC/Debug.fble   [Removed]
  - pkgs/core/Core/CRC/Tests.fble   [Removed]
  - pkgs/core/Core/CRC.fble         [Removed]
  - pkgs/core/Core/Debug/Builtin.fble ==> /Std/Stream/Debug%
  - pkgs/core/Core/Debug.fble         ==> /Std/Stream/Debug%
  - pkgs/core/Core/Digits/Eq.fble
  - pkgs/core/Core/Digits/Show.fble
  - pkgs/core/Core/Digits/String.fble
  - pkgs/core/Core/Digits/Tests.fble
  - pkgs/core/Core/Digits.fble
  - pkgs/core/Core/Fifo/Tests.fble  [Removed]
  - pkgs/core/Core/Fifo.fble        [Removed]
  - pkgs/core/Core/Int/Digits.fble  [Removed]
  - pkgs/core/Core/Int/Iter.fble
  - pkgs/core/Core/Int/Read.fble
  - pkgs/core/Core/Iter.fble
  - pkgs/core/Core/Maybe.fble
  - pkgs/core/Core/Monad/IO.fble    [Removed]
  - pkgs/core/Core/Monad/Pure.fble
  - pkgs/core/Core/Scratch.fble     [Removed]
  - pkgs/core/Core/Test/Cli.fble    ==> /Std/Test/Cli%
  - pkgs/core/Core/Test/Run.fble    ==> /Std/Test/Run%

So, as you can see, it's a pretty long list of things. And a bunch of them
will probably be difficult.

---

Taking stock now. What's left to deal with in pkgs/core:

Map related modules:
* pkgs/core/Core/Map/Show/Tests.fble
* pkgs/core/Core/Map/Show.fble
* pkgs/core/Core/Map/Tree/Tests.fble    [REMOVED]
* pkgs/core/Core/Map/Tree.fble          [REMOVED]
* pkgs/core/Core/Map/Eq/Tests.fble
* pkgs/core/Core/Map/Eq.fble
* pkgs/core/Core/Map/Tests.fble
* pkgs/core/Core/Map/Test.fble
* pkgs/core/Core/Map.fble
* pkgs/core/Core/Int/Map/Eq.fble
* pkgs/core/Core/Int/Map/Tests.fble
* pkgs/core/Core/Int/Map.fble
* pkgs/core/Core/Int/IntP/Map/Eq.fble
* pkgs/core/Core/Int/IntP/Map.fble
* pkgs/core/Core/Set.fble
* pkgs/core/Core/Char/Map/Tests.fble    [REMOVED]
* pkgs/core/Core/Char/Map.fble          [REMOVED]
* pkgs/core/Core/List/Map.fble          [REMOVED]
* pkgs/core/Core/Set/Show.fble
* pkgs/core/Core/Set/Eq.fble
* pkgs/core/Core/Set/Tests.fble
* pkgs/core/Core/String/Map/Tests.fble  [REMOVED]
* pkgs/core/Core/String/Map.fble        [REMOVED]

Test modules:
* pkgs/core/Core/Int/Show/Tests.fble
* pkgs/core/Core/Int/Tests.fble
* pkgs/core/Core/Char/Tests.fble        [REMOVED]
* pkgs/core/Core/Random/Lfsr/Tests.fble
* pkgs/core/Core/Random/Lcg/Tests.fble
* pkgs/core/Core/Test/Tests.fble
* pkgs/core/Core/String/Tests.fble
* pkgs/core/Core/Tests.fble
* pkgs/core/Core/Debug/Tests.fble

Digits: ==> /Md5/Digits* [DONE]
* pkgs/core/Core/Digits/Show.fble
* pkgs/core/Core/Digits/String.fble
* pkgs/core/Core/Digits/Eq.fble
* pkgs/core/Core/Digits/Tests.fble
* pkgs/core/Core/Digits.fble

Other modules:
* pkgs/core/Core/Monad/Pure.fble  ==> [REMOVED]
* pkgs/core/Core/Int/Read.fble    ==> /Std/Int/Parse%
* pkgs/core/Core/Int/Iter.fble    ==> /Std/Int/Stream%.Range
* pkgs/core/Core/Iter.fble        ==> /Std/Stream%.ForEach
* pkgs/core/Core/Maybe.fble       ==> [REMOVED]

Getting closer...

---

I'm done populating /Std/* with all the packages I want. I think we're ready
to start the release burndown.

High level tasks:
* Get rid of core package entirely
 - Migrate remaining Tests.fble files to std-test
 - Move native code to std
 - Remove build file and any dependencies on it by other packages.
* Rewrite all the tutorials.
* Todo item burndown.
 - Consider writing some rosetta code stuff
 - Consider rewriting all the existing fble to make it more idiomatic.

---

Core package has been removed. Before rewriting the tutorials, I want to
address any todos that might impact how I rewrite the tutorials. These come to
mind:
* Move In@, Out@ out of /Std/Stream%.
* Rename 'import' to struct import and implicit type to struct export?
* Figure out how to organize std packages

Where should In@, Out@ move to? Also any other reference to In@ and Out@ under
/Std/Stream/.

* /Std/Io% is already taken to mean something else.
* Maybe: /Std/Stream/Io%? /Std/Monad/Io%?

/Std/Stream/Io% sounds reasonable. It's a specific type of io that works with
streams of data. Let me try moving it. See how terrible it feels.

I made the change. It doesn't feel great or terrible. Lots of places we need
both Stream@ and In@/Out@. Some places we don't. Oh well.

---

Next is fble spec work. Should be transparent to the user, but will help
organize tutorials better. Proposal:

     | '*(', [type, name, {',', type, name}], ')'      (* struct_type *)
     | type, '(', [expr, {',', expr}], ')'             (* struct_value_explicit_type *)
     | '@(', [field_arg, {',', field_arg}] ')'         (* struct_value_implicit_type *)
     | expr, '.', name                                 (* struct_access *)
     | expr, '.@(', field_arg, {',', field_arg}, ')'   (* struct_copy *)

     | [type], name, [':' name] {', [type], name, [':' name]},
       '.=', expr, ';', stmt                           (* import *)

Turns into:

     | '*(', [type, name, {',', type, name}], ')'      (* struct_type *)
     | type, '(', [expr, {',', expr}], ')'             (* struct_value *)
     | expr, '.', name                                 (* struct_access *)
     | '@(', [field_arg, {',', field_arg}] ')'         (* struct_export *)
     | expr, '.@(', field_arg, {',', field_arg}, ')'   (* struct_copy *)

     | [type], name, [':' name] {', [type], name, [':' name]},
       '.=', expr, ';', stmt                           (* struct_import *)

All go into the same section of the spec. We stop treating struct import like
syntactic sugar. Much nicer way to distinguish between struct value and struct
export.

Another question while we are at it: how about moving function bind into the
function section? Call it function_bind?

And then, perhaps, rename syntactic sugar section to literals section?

And another thought: now that we have removed kinds from let, does it make
sense to move kind into the section on polymorphism? Yes. I think that makes
sense. We only need to distinguish between types and normal values and higher
kinded things.

And here's a question: do I need syntax for normal kinded things? Is there any
value in having that anymore? Where could you ever use it?

Okay. Sounds like a round of spec cleanup is warranted.

---

I need to take spec cleanup for structs one step at a time, otherwise I'll
miss things. The steps are:

1. Rename explicit type struct value to just struct value.
2. Rename implicit type struct value to struct export.
3. Switch order of struct export and struct access in spec.
4. Move import into struct section, call it struct import.
5. Reread through spec and make sure it is coherent.

---

While on the topic of the spec, let's clarify :=, (., and .= as atomic
punctuation. First off... is there any other punctuation we might want to
join to this group? Maybe:...
* '@<' used in typeof
* '*(' used for struct type.
* '@(' used for struct export.
* '.@(' used for struct copy.
* '.?(' used for union select.
* '.%(' used for private types

Why don't I just set them up to be precise tokens? Then I can use a blanket
statement in the concrete syntax that lists them as tokens in the input that
cannot be separated by whitespace?

And... how about we update our use of ebnf while we are at it for better
consistency?

Extended Backus–Naur Form, from wikipedia:

There is an ISO standard: ISO/IEC 14977. Can we stick with that?

What syntax am I using? What syntax do I like? What syntax do other people
use?

Currently I'm using:
* name = ... | ... | ... ; to define a nonterminal.
* a, b, c to define a sequence of terms.
* 'xx' to define a literal string value.
* {...} to define optionally repeating values, zero or more times.
* [...] to define optional value, zero or 1 times.
* (* ... *) as a comment.

It can be a bit verbose with all the single quotes and commas everywhere. Oh
well.

Let's see what other people do:
* Double quotes instead of single quotes. But looks like I'm still to spec.
  Single quote is allowed.

Cool. In that case, seems like I'm doing the right thing? Let's just double
check the syntax.

Trouble: Stream@<... conflicts with my '@<' requirement.

Hmm... What should I do here?

There are a few things, like '@(' that are similar. Let me just be explicit in
the spec that those are '@' '(' instead of '@('. Have the spec tests match.
Have the lexer implementation match. Group things together otherwise where we
can.

Well, it's a little funny. But at least I'm more precise in the spec now. We
can decide to relax the spec later on if I find motivation to do so.

---

Spec is cleaned up. I should now feel free to redo the tutorials.

---

* Clean up std native code

Should I group stuff together? char, debug, list, maybe, env, int?

Or maybe the data types: char, list, maybe, int, string?

Data types sound reasonable to me. Let me do that. Then I can add list and
maybe helpers there.

---

* Change IntP Data traversal order to be natural order.

I don't see how this is possible. If I put IntP@ in order, then the order for
positive integers is 1, 2, 3, ... But the order for negative integers is -1,
-2, -3, ... when I want -3, -2, -1, ...

Unless we provide two different versions of Data for IntP@? One that goes in
forward order, the other that goes in reverse order? I guess that's doable.

Um, I need to figure out how to get the IntP@ in the right order first though.

Raw sequence:
 1, 10, 11, 100, 101, 110, 111, ...

They all start with '1', so we can remove that first bit and make it implicit:
 '', 0, 1, 00, 01, 10, 11, ...

But we need to encode explicitly if there are more bits coming or not:
 0, 10, 11, 1010, 1011, 1110, 1111, ...

Are these in order? No. They aren't. Look:
  1011 < 11

Try again then. Raw sequence, but in a lexicographical order:

 1, 10, 11, 110, 111, 1110, 1111, 11110, 11111, ...

This doesn't feel like a very efficient encoding. It's a unary encoding of the
sub number followed by bit 0 or 1, right?

 11110 is 2*4 + 0
 11111 is 2*4 + 1

I'm not sure it's worth putting things in the order you want.

Could we do something crazy, like write a number, then tell you if that's the
number of bits of the final number, or if that's the number of bits telling us
the next number of bits? Climb up, getting the number of bits in binary, and
then read those bits to get the final number?

Say we initialize a state to 1. Then we have a little program. The program has
the following instructions:
* A: read S bits, that's your final number.
* B: read S bits, that's your new state. Continue processing.

Except, we don't have a way to increase bits if we start with 1. It needs to
start with at least 2 so we can go from there to 3? Or the update function for
B has to be different:

If we are at S bits, we can encode any number that takes 2^S - 1 bits. For
example, say my state is 4. Then I can read any number from 0 to 15. If that's
not enough, then I get a number of bits from 0 to 15. I know I want more than
4 bits, so instead make the range [5, 20].

So my parser program becomes:

Given state S, initialized to 1:
* A: Return Read(S);
* B: S += Read(S); Continue;

And because we are talking positive numbers here, we can define Read(S) as
reading the sequence of bits, interpreting it as a positive number, and adding
1.

So, I would encode numbers as follows:

1: A0
2: A1
3: B...

Why make this so complicated? How about just have 'B' double S?

Given state S, initialized to 1:
* A: Return Read(S);
* B: S *= 2; Continue;

1: A1
2: BA10
3: BA11
4: BBA100
5: BBA101
6: BBA110
7: BBA111
...

So, better than unary, because we are doubling S at each point.

But also, notice the first bit after A is always 1, so we can get rid of that
to save some space.

1: A
2: BA0
3: BA1
4: BBA00
5: BBA01
6: BBA10
7: BBA11
...

Now encode B as A as 0 and B as 1:

1: 0
2: 100
3: 101
4: 11000
5: 11001
6: 11010
7: 11011
...

Wait. If I'm doubling S instead of adding 1, then it should be:

1: A1          S = 1
2: BA10        S = 2
3: BA11
4: BBA0100
5: BBA0101
6: BBA0110
7: BBA0111
8: ...

The point is, I should be subtracting out however many values we can capture
with the previous S.

I can worry about that minor optimization later though.

So, we read a unary number N. We do 2^N. That tells us how many bits we need.
We read that many bits to get our value.

I can do a unary number as raw 0, 1. I can do binary that way too. Cool. This
feels pretty efficient. Perhaps more efficient than I currently have, and with
the benefit of being in order.

Let me get positive numbers first. I'll worry about how to go the opposite
order later.

Um... It's easier to do S++ instead of S*=2 for the initial implementation.
Let me try that.

It appears not to be working. Can I figure out what I did wrong? How do I
debug this?

Silly error. Fixed now. We're getting good order for positive integers. Now I
just need to handle the reverse order somehow. Should be straight forward I
think?

Yeah, easy. I can literally just flip bits and reuse the same implementation.
Awesome.

Let's see how this impacts our benchmark time. I'm thinking it might be a more
efficient encoding than we had before, so could have reduced the time
noticeably from the 1:40 ish time we had before.

Time: 1:43.90
Memory: 82024

Oh well. Small regression. But at least things are in order now. And I have
that idea still for how to make it more compact.

Actually, probably it doesn't help. If it makes the code more complex, not
worth it?

My encoding size today for an N bit integer:
* 2*N - 1 bits.

My encoding size if I switch it up as planned:
* We get log the number of bits counting number of bits.
* But we get double the number of bits with the actual bits.

Yeah, not worth it.

---

* Verify all api is listed in include/build.tcl.

Maybe I can generate the actual list using nm somehow and compare with
include/build.tcl? Maybe I should write a script to do that as part of build?

One step at a time.

This looks like the list I want:

  nm -U -g out/lib/libfble.a

Let's review.

parse.tab.o:
0000000000003968 T FbleIsPlainWord
  Internal sharing between parse.y and name.c
00000000000009c0 T yyparse
  Not under my control.

type-heap.o, tc.o:
  Everything is meant to be internal. Shared with type checker presumably.

alloc.o:
0000000000000000 B gInitialized
0000000000000008 B gMaxTotalBytesAllocated
0000000000000010 B gTotalBytesAllocated
  Fixed all these to be static.

value.o:
000000000000256c T FbleLookupForeignValue
0000000000002c8c T FbleNativeValueData
0000000000002648 T FbleNewForeignValue
0000000000002c20 T FbleNewNativeValue
0000000000002cd0 T FbleValueFullGc
  Added to api.

0000000000000138 R FbleValueFlagIsGcAllocBit
000000000000013c R FbleValueFlagTagBits
0000000000000134 R FbleValueFlagTraversingBit
 - Made static.

expr.o, typecheck.o: internally shared.
0000000000000000 T FbleFreeExpr

load.o:
0000000000000000 D FbleDefaultPackagePath
 - Made static.

program.o, kind.o, interpret.o, unused.o: internally shared.

profile.o:
0000000000000018 R RootBlockId
 - Made static.

code.o, type.o: internally shared.

buildstamp.o: internally shared.
0000000000000000 D FbleBuildStamp

Looks okay in general then. I added the missing ones, made things static where
reasonable. It's too bad we have to export the internally shared ones. I
wonder if there is some way to do away with that.

---

* Cover all escape chars in StrE tests.

What I'm missing now: \0, \a, \b \f \r \v

But that's because none of \0, \a, \b, \f, \r, or \v are present in the Ascii
type. Should I name them? Like, Ascii. nul, bel, bs, ff, cr, vt?

It's part of ascii. Why not? That doesn't mean I need them to be printable.
But, on the other hand... why not?

---

* Why is the invaders ship dying animation so messed up on linux console?

What I observe: it flickers, then holds, then flickers, then holds.

What behavior do I want? It should be even back and forth and back and forth.

What does the code have?
* It should flip status every tick.
* Is it possible we are killing it repeatedly though?
 - What does Bombs.HitShip return when the ship is hit? It should be removing
   the bomb that hit the ship.

If we hit the ship, why return any bombs? I guess we still want to show them
on the screen.

Anyway, in theory kill is only called once. I can verify with a debug
statement call.

My hypothesis: we aren't clearly aligned with vsync or some such, and we are
updating just at the edge, and one of the images takes longer to draw than the
other, so we end up biasing towards one of the images.

I don't know how to confirm that, or how to fix the underlying issue. I can
work around it by not flipping at such a high rate.

Can I fix it in the app level by using SDL_RENDERER_PRESENTVSYNC when I create
my renderer? But I'm not using a renderer... Maybe if I switch to SDL
renderer.

Which, while I'm at it, how about switch to sdl3?

Actually, looks like I can just call SDL_GL_SetSwapInterval(1) when I
initialize things? Let's try it and see.

Yeah, I think that fixes it. But now it looks like a solid blur between the
two states. So I really do want to slow it down in the invaders game itself.

I feel like this looks much better graphics wise with set swap interval 1.
Cool.

---

* Figure out why we don't propagate an error
 - If /Core/Map/Data% Delete function is left undefined.
 - The tests print @[, then skip over to the next test?

I can't reproduce it. Oh well. Worry about it if we run into it again.
Probably there's a bug somewhere hidden dealing with propagation of errors.

---
* Double check performance of ReverseComplement, specifically the
  ComplementCode function. Maybe use a map instead?

Benchmark results switching to map:

Time: 0:41.29 ==> 0:29.88
Memory: 11128 ==> 11052

---

* Add vim syntax highlighting for \ and % escape sequences?

Is there any precedent for library specific highlighting in the existing vim
syntax file? No.

Do I want to make this precedent?

I suppose I should at least try implementing it and seeing how it feels.

There is precedent for this for other languages like C.

Here's the example:

   :syntax keyword Todo    TODO    contained
   :syntax match   Comment "//.*"  contains=Todo

Sounds like the highlight group to use is 'Special', or 'SpecialChar'.

I can define these special characters via keyword, contained, and say a quoted
word can contain them.

Formatting is: \0, \a, \\, \b, \f, \n, \r, \t, \q, \v, \u[hex]+, %%, %s

Note that these don't apply universally. Like, they would work in StrE, but
not Str. That's more reason not to add them to the vim syntax. I don't feel
like I need them in general.

No. I don't think it's worth it. Not enough value add. Not part of the
language itself. Not applicable in most places. Annoying to define because I
can't use keywords and I have to escape things with regex.

---

* Double check benchmark deadlock conditions

Let me add a library for App for turning it into a benchmark. You give it an
app and a number of ticks to run for. As long as it gets a tick request (and
drawings?), it replies with a tick. When it stops getting tick requests, it
just loops the benchmark from the beginning.

No fears of deadlock. Worst case the app gets stuck in a tight loop of tick
requests that we don't detect. That's not quite satisfying though... how could
we detect this? Just don't worry about it? But it seems a common thing to
happen, when the app has either reached its end, or is waiting for some key
press to continue.

Think about what can happen:
* The app can end: restart it
* The app can get in a loop waiting for a certain key press
  - it's not drawing, it's not requesting ticks, so restart it.
* The app can get in a loop ticking and drawing
  - I don't know how to detect this. Deal with it.

Which means if we get a request for input without a request for a tick,
clearly the app is waiting on user input, we should just restart it, since we
don't know what user input to give. If the app ends, restart it. Otherwise,
keep ticking away. Easy.

Except, trouble: how do we get the app to end when we are done? We have to
know how to quit the app. A sequence of events, or a single event, or
something.

Cool. Just need to port over the last few: Graphics, Invaders, Pinball.

---

* Why is fbld outputting errors in reverse order?

Repro: add too many arguments to @l in multiple places in the file.

./tutorials/HelloWorld.fbld:14:24: error: expected 1 arguments to L, but got 2
./tutorials/HelloWorld.fbld:14:10: error: expected 1 arguments to l, but got 2
./tutorials/HelloWorld.fbld:9:8: error: expected 1 arguments to l, but got 2

Questions:
* What order are the errors raised in?
* Do we evaluate front to back or back to front?
* Should we reverse the order of printed errors knowing that?

Given a sequence, I do the first then the second.

Do tries the first thing, skipping the next otherwise.

That's fun. It's the C implementation which is outputting errors in reverse
order. I bet the fble implementation works fine.

Looks like it evaluates sequences in LIFO order. I can put args on in reverse
order to fix that I bet.

---

* Double check error message for over application of a function.
  It shouldn't say "can't apply arguments to something of type (Foo@) { ...}"
  Because that's not true, right? Better would be "too many arguments?"

Example:

  AssertEquals(/Std/Int/Eq%.Eq, /Std/Int/Show%.Show, wnt, got, wnt);

  error: cannot apply arguments to something of type <@ T@> { (Eq@<T@>,
  Show@<T@>, T@, T@) { TestResult@; }; }

Yeah, that's totally misleading. The error message I want in this case is: too
many arguments. Or something like that.

I'm not sure where the error message should be, or what the best location is,
but I can at least switch it to something more meaningful that shows the type,
how many arguments were provided, and how many (at most) were expected. Maybe
go with that as a step forward and worry about improving the message later if
it feels to awkward when I run into it next.

---

* See if there is more opportunity to typecheck more stuff before erroring
  out?

There is a progression of what I can do here:
1. Stop at first error.
2. Allow errors from multiple expressions that don't depend on each other.
3. Allow errors from multiple expressions that do depend on each other.

I'm at (2). Sure, there are probably opportunities to do a little more within
(2), but the real benefits are going to come from (3): allowing typecheck in
the body of a let expression, even if some of the bindings have failed.

But imagine if I succeed at that. Given that I re-type check every thing?
Well, actually, maybe it's not so bad. I was going to say we would start
getting duplicate errors all over the place, but probably we wouldn't.

Anyway, this would be a good thing to do, but it's also a risky thing to do,
particularly without exercising the error paths much. And I don't expect to
exercise the error paths much more in this release. So my vote is to put this
off to the early part of a future release.

And maybe wait until after I've fixed type checking dependencies, otherwise I
really will get too many duplicate errors. Because every module I compile will
give me errors for every other module it depends on directly or indirectly,
rather than just the first. So we have like O(N^2) instead of O(N) duplicate
error messages.

---

* Write a script to help find unused modules?
 - Because I suspect I have some unused modules in my packages...

What does this mean? What is an unused module?

For something like md5, or invaders, I could probably list the top level
modules. Things like: AppIO.fble, Bench.fble, Tests.fble. For things like Std,
every module is potentially just a library to be used by another module.

I could try listing all the modules not referenced by another module, in all
my packages, and manually reviewing them. Or we could maybe generalize
fble-deps tool to search for modules and give an entire dependency graph
instead of the flattened graph.

But honestly, this seems like a lot of work for very little value.

Let me drop this. If it is useful to add back later, it should reveal itself
as such and we can consider adding it then. Next time, I should record which
module I'm afraid is unused.

---

* Optimize size of generated FbleCode and resulting .c, .s artifacts?

The question is, what am I optimizing for? We could certainly shrink things by
using macros, smaller label names, etc. But that's not what I'm after. For .c
files, I think it's the memory required by cc to compile it that is the
bottleneck.

Here's my vote: look at some of the bigger generated .c and .s files. See if
anything obvious sticks out, or what the top things are that stand out. If
there are no obvious ways to improve things, then don't worry about this.

Presumably the most impactful improvements are the kinds of things that will
happen when I optimize fble for performance: direct branches in inline code
units, omitting error checks at runtime, etc.

Here are the top few:

 662766 graphics/Graphics/Triangle/Tests.fble.s
 771608 pprof/Pprof/Server/Main.fble.s
1076908 graphics/Graphics/Camera/SinCos.fble.s
1373630 invaders/Invaders/Graphics.fble.s

/Invaders/Graphics% is mostly a pile of list and literals. Same with SinCos.
There's lots of literals in /Pprof/Server/Main% too.

What I see looking through the .s code for /Invaders/Graphics%:

Common blocks for struct access like this:

  ubfx x1, x0, 13, #6
  add x1, x1, 42
  ubfx x2, x0, 19, #6
  add x2, x2, 42
  lsr x0, x0, x1
  sub x1, x2, x1
  bfi x0, x1, #1, #6
  orr x0, x0, #1
  mov x2, -1
  lsl x2, x2, x1
  bic x0, x0, x2, lsl #7

Common blocks for function calls, such as:

  sub SP, SP, 16
  ldr x0, [R_LOCALS, #184]
  str x0, [SP, #0]
  mov x0, R_RUNTIME
  mov x1, R_PROFILE
  ldr x2, [R_LOCALS, #24]
  mov x3, 1
  mov x4, SP
  bl FbleCall
  str x0, [R_LOCALS, #192]
  add SP, SP, #16
  cbz x0, .Lo.0000.101.abort

We have 65K lines in the .s file. There's a function with almost 3K
fble instructions. That's averaging like 22 machine instructions per fble
instruction. The best way to reduce the size is going to be to optimize the
fble code or optimize the generated code.

Let's not worry about this.

---

* Why don't interpreted cli tests output when running them during build?
 - I have to go to the generated file to see the outputs?
 - Because we generate the output first, then interpret as a test output.
   It's slightly awkward to get the dependencies passed directly to the 
   testsuite helper?

Here's what we have:

  proc ::run_cli { target cmdargs args } {
    build $target "$::b/pkgs/std/fble-cli $args" \
      "$::b/pkgs/std/fble-cli --deps-file $target.d --deps-target $target $cmdargs > $target" \
      "depfile = $target.d"
  }

  proc ::run_cli_tests { target cmdargs deps } {
    run_cli $target.out "$cmdargs -- --prefix Interpreted." $deps
    testsuite $target $target.out "cat $target.out"
  }

What makes this annoying is the depfile thing. Does testsuite support that? It
doesn't. So how about I use fble-deps directly to create the target.d file,
have it depend on itself or something, and then depend on that for the call to
testsuite? Or add support for varargs to testsuite to pass the depfile thing?

Easy.

---

* Clarify in spec whether poly apply to an undefined value results in an error
  or not.
  And add spec tests for that case.

Let's start with the spec test, see what happens. Then decide what to do.

Um... is this even possible to write? Undefined means using a value in the
binding of a recursive let. I suppose something like:

  <@ T@> { Unit@; } u = {
    ...

But I can't do poly apply at all of a value in a recursive let, because it has
kind @ in its own binding.

We don't have an undefined expression anymore, so no need to worry about that.

I think we're fine. I don't think this is possible to write, so no need to say
in the spec what the behavior should be.
    
I suppose it's fair to ask when we get an error here:

  <@ T@> { Unit@; } U = <@ T@> { /Std/Undef%.Undef.undef<Unit@>; };
  Unit@ u = U<Int@>;

Does the error happen when we define U or when we apply to U? It's not the
same question, but it is a question. Current spec says:

   The body of a poly is evaluated as part of evaluation of the poly value.
   The runtime is the runtime of the body.

That makes it pretty clear. I can add a test to this case to be extra
explicit.

---

* Remove now unused FBLE_PROFILE_ENTER_OP, EXIT_OP, and REPLACE_OPs?
 - Or wait to see if we want them for code coverage?

I need to go back through history to recover these anyway. How about I remove
them now, and add them back if/as needed if/when I want it for code coverage?

Sounds fair to me.

On the other hand, if I remove it, there's only one kind of profile op left,
and it seems silly to have support for general profile ops when all we are
doing is sampling. But the extra cleanup for that sounds more tedious.

What cleanup would I do?
* Remove FbleProfileOpTag
* Rename FbleProfileOp to FbleSampleOp? Change arg to count?
* There's no longer any need for a list, because multiple samples should be
  merged.
* So replace profile_ops with a single size_t sample count, which could be
  zero or not. If not zero, do the sample, otherwise don't.
  sample_count field.

Okay? Shall we? Let's see what happens.

I suppose it's worth testing by generating a profile before and after of
fble-benchmark.

The only tricky thing is in the compiler, figuring out when to add to the
sample count of a previous instruction versus starting a new count. Something
to do with basic blocks.

Maybe we should accumulate count of instructions in the current basic block,
and any time we end a basic block, move the count from the running count to
the last instruction in the block? Then all we need is a running count of
instructions in the basic block.

Okay. I'll try that. I'm worried I'll break things. Maybe I should compare
disassembly before and after...

No. It's too hard to tell when the end of a basic block is. Better to figure
out when the start of a basic block is.

No. It's still hard. Maybe I can leave the same logic as before somehow, just
change profile ops to pointers to instructions, or what have you?

---

* Test unicode for character based native input and output streams.
 - Writing π to a file should result in π in the file.
 - Reading π from a file should result in π being read.
 - Be careful not to read from a file and write out, which fails to test that
   it shows up as unicode within fble.
 - If we can test it shows up with the right code point inside fble, then we
   can test output by using fble-cli-demo cat on a file with unicode.

The natural vehicle for testing these things is fble-cli-demo. I don't trust
passing a multibyte character in and having that show up as a unicode
character before going back out. I really want a way to specify a unicode code
point directly as a thing to read or print.

I can use files or stdin/stdout. It's the same code path.

We have the problem with unicode command line args and code pages on windows
today that complicates this. Let's split it up. To start, don't worry about
the windows issue. Implement something I can run and test and works on linux.
Treat the windows thing separately.

Things we could do:
* Add an fble-cli-demo command for printing a unicode character with given
  code point. I'm confident we can pass the code point using ascii characters,
  read the correct value in fble, then whatever we print as output we can
  check in a grep command like I already have. That's a good test for unicode
  in output streams. But, if we already have confidence we can read unicode
  characters correctly, then we can just reuse fble-cli-demo cat command and
  the existing unicode test that way.

So, focus on testing that we read unicode characters properly. There are lots
of input paths though:
* command line argument
* environment variable
* input file.

Ideally we could test all of them. For the purposes of this specific todo, I
care about input file. I don't have demo code existing today that knows how to
read stdin. What would I do?

What kind of standard commands read and consume stdin, and what do they do
with it? Anything usefully relevant here? How about grep?

wc? wc has a -c option to count characters instead of bytes. But it's not the
default.

I could have a 'chars' command that takes an input file and converts it to a
list of unicode code points? Not standard, but demonstrates input and could be
used to test unicode?

Let's say I wrote a 'chars' command. What exactly should it do? For each input
character... print the code point in hex? Max 20 in a row?

Maybe use U+ notation? U+ followed by uppercase hex with at least 4 digits.

So, if I have text:

helloπ!

I would convert that to:

U+0068 U+0065 U+006C U+006C U+006F U+03C0 U+0021 U+000A

I'm fine with spaces between entries. Where should we put newlines?
* nowhere? do it all one one long line?
* After every character?
* After every 8 characters, which worst case gives us 8 * (1+2+6) = 72?

Let's do after every 8 characters a newline. Okay? Good. Spec is clear. I
think 'chars' is a reasonable enough name: gives you the input as a sequence
of characters.

Hopefully the implementation isn't too complex.

Should we read from stdin or from a file? How about stdin? We already have
examples of how to read from a file with cat.

I'll want a way to show an integer using uppercase hex... Or a way to show a
character using U+ notation? /Std/Char/Show%.U maybe? Or
/Std/Char/Show%.Unicode. Sure. But that wants a way to show Int as (uppercase)
hex? Or just implement that internally, because uppercase versus lowercase is
hard to specify?

Steps:
1. Implement and test /Std/Char/Show%.Unicode.
2. Implement fble-cli-demo chars command.
 - Actually, let's have it take an input file, optionally '-'. Just like cat.
   We can reuse the code perhaps.
3. Write my test case: call chars on my sample file with unicode in it.

Done. It's a little bit hacky. Particularly with switching between strings and
streams everywhere and being a non-standard command and not working on
windows. But it gets the job done, no?
