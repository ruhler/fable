Char_% { @(Char@); };
List_% { @(S@, List@, S); };

@ Str@ = S@<Char@>;
<@>@ CharLit@ = List@<Char@>;

(CharLit@){S@<Char@>;} Str = (CharLit@ mkList) {
  S<Char@>(mkList);
};

@(Str@, CharLit@, Str);
