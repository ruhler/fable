
@ Int@ = /Std/Int%.Int@;

@ Char@ = *(Int@ code_point).%(@/Core/Char%);

# Char from unicode code point.
(Int@) { Char@; } Chr = (Int@ code_point) {
  @(code_point);
};

# Unicode code point of the character.
(Char@) { Int@; } Ord = (Char@ c) {
  c.code_point;
};

@(Char@, Chr, Ord);
