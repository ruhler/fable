
@ Bool@ = /Std/Bool%.Bool@;
% True = /Std/Bool%.True;
% False = /Std/Bool%.False;

<<@>@>@ RngM@ = /Core/Random/Rng%.RngM@;

# Generates a random boolean.
<<@>@ M@>(RngM@<M@>) { M@<Bool@>; }
Rand = <<@>@ M@>(RngM@<M@> rng) {
  rng.r.choose(True, False);
};

@(Rand);
