# @@fble-test@@ compile-error 6:13
@ Unit@ = *();
<@>@ Maybe@ = <@ T@> { +(T@ just, Unit@ nothing); };

# Kind mismatch: expected poly type, but got poly value.
<@>@ Foo@ = <@ T@> { Maybe@<T@>(nothing: Unit@()); };
Unit@();
