Stdlib Stdio
============

Goal: design the part of the standard library necessary for interacting with
other programs via c99 stdio like standard.

Relevant features of C99 stdio to consider supporting:
* signals - for handling signals sent from other programs
  register signal handler
  send signal
  abort
* File IO:
  - stderr, stdin, stdout
  - reading, writing, appending, seeking
  - binary, text
  - files, streams
* File operations: remove, rename, temp files
* atexit, exit, 
* getenv, setenv
* system command
* main, args, exit

In summary: signals, files/streams, env, main, exit

Should we support all those things? Can we do without some?

What I know today:
* I need files to save things like high scores and models for games.
* I need streams to do input and output.
* I need env to find the user's home directory.
* I probably want exit to be able to return error status.
* I definitely need command line arguments.

The limitations I currently have:
* No support for signals.
* Exit code is 'True' or 'False', rather than a number.
* I haven't implemented setenv yet.

Can signals do anything useful for tests? Like, fail could raise a signal, and
we could have a way to catch the signal, and that gives us our 'try'
implementation?

---

Consider two different API layers:
1. Raw access to underlying C calls.
2. Natural way to express API in fble.

Even if we provide raw access, it still makes sense to provide and promote an
fble specific API. On the flip side, it's potentially bad from a performance
point of view to layer (2) on raw access in fble, which then goes to C. We may
get better performance by batching calls from (2) into higher level calls on
the C side.

Partly it's a question of how much of the C99 standard API we want to support
in the fble standard library.

---

Thought on signals: it doesn't make sense to interrupt a pure function based
on signal. Not in a way that can modify the results.

It could make sense to interrupt an IO operation, or to invoke a side effect
during a pure computation that is only visible at IO level. Like, imagine we
take note of a signal and check for it the next time you are in the Do
function for the IO monad.

An example practical use of a signal would be to allow ctrl-c to interrupt a
potentially long running (background?) computation. The computation would have
to be interruptible though, which means it can't all be pure.

---

What if we passed 'Env' as another argument, then you could interact with it
purely? And also return 'Env' as a modified return?

---

One thing that is making this challenging is having to come up with the entire
set of IO interfaces all in one go. It's not very modular. Better would be if
we could come up with individual interfaces for signal, env, file, etc. and
compose them together. That way we could work on providing and defining and
refining the individual interfaces separately.

I sort of, but not completely, did this with networking. I layer sockets API
on top of the main stdio API. But that isn't quite compatible. If I layer App
API on top of main stdio API the same way, that's well and good, but how to we
write an app that uses sockets? Linear stacking of APIs like this doesn't
extend well. We want arbitrary combinations.

Admittedly, there is something fundamentally different about String args, exit
code, and App height and width. While file IO, env, networking can all be done
as side effecting operations, the way I've set up input arguments, exit
code, app height and width isn't. But perhaps could be?

Consider the env API for example. We can have getenv/setenv side effecting
operations. Or we could pass a Map from String to String as input and return a
Map from String to String as output from your main function. Passing as input
and return as output fits much better with functional behavior, but it gets us
back to a world where we need to encode every possible interface in the type
of the main function, which does not compose well.

You could imagine instead of passing command line arguments and returning an
exit code that we do those all through side effects. Have a GetArgs function
that knows how to pull the arguments out of thin air. Have an Exit function
that specifies a non-default exit code. Then our top level runner could just
be 'IO'.

There is precedent for that kind of thing. It's like saying a main function
runs in a state monad that has access to command line arguments as part of the
state, and you can pull it out of that state if you want it. Or ignore it
entirely if you don't. From that point of view, we can make the API separate
from the implementation.

But there is a fundamental difference between an API where we pass Env@ as an
argument up front versus accessing it via GetEnv and SetEnv. If we pass it up
front, we have to populate it completely every time we run a program, even if
we never use it. There's a potentially significant performance cost we could
avoid if we instead access a small part of it via GetEnv/SetEnv functions. The
filesystem is a more extreme case. Theoretically, but not practically, we
could load the entire filesystem into a tree, pass it as an argument to the
program, return a modified filesystem.

Anyway, the point I wanted to get to was, we still want to use generic
interfaces for the APIs: for all M with Monad@<M@>, FooI@<M@> is the
interface. For actual IO, we provide a FooI@<IO@> interface, and our top level
runner works in IO@. Now we can compose side effecting interfaces arbitrarily
like:

  AI@<IO@> a = ...;
  BI@<IO@> b = ...;
  CI@<IO@> c = ...;
  Run(main<IO>(a, b, c));

It's important to me that we can run tests in a pure environment. We should be
able to provide pure modeling of these IO computations. This is where we run
into trouble today. Most models will be state monadic computations. But it's
really tedious to compose multiple different state monads together.

It would be something like the following, but I don't know how to implement
it:

  AI@<ASM@> a = ...;
  BI@<BSM@> b = ...;
  CI@<CSM@> c = ...;
  Run(main<Compose@<ASM@, BSM@, CSM@>>(Lift(a), Lift(b), Lift(c)));

If we had an ST like monad, however, then it would be easy to compose these.
We just need a way to create a pointer to some object and store the pointer
internally. Then we can as easily do:

  AI@<ST@> a := ...;
  BI@<ST@> b := ...;
  CI@<ST@> c := ...;
  Run(main<ST@>(a, b, c));

What is this ST@ thing? It's just another interface. The key thing we can do
is allocate a pointer to a value that we can read and write:

  Read@<A@> = M@<A@>;
  Write@<A@> = (A@) { M@<Unit@>; };
  New@ = (A@) { M@<*(Read@ read, Write @write); };

Read, Write, and New all depend on generic monadic type M@. I can provide a
builtin pure ST runner function that prevents us from returning pointers using
a type like:

  <@ A@>(<<@>@ M@>(ST@<M@>, Monad@<M@>) { A@; }) { A@; } Run;

Note that A@ cannot depend in any way on M@, so you can't leak any allocated
Write@ or New@ instances associated with whatever specific M@ we use in the
internal implementation of Run.

ST@ is kind of like saying we do everything in terms of memory. Similar to how
processes said we do everything in terms of get/put. Side effects as memory
mapped IO. But generic monads work great to describe arbitrary side effects,
and they let you describe more complicated side effects as atomic things,
which is nicer than expressing everything in terms of get/put read/write.

Why not just add pointers to fble, and skip the need for ST@? Because if we
can do pointer operations in pure function evaluation, then we would need to
define clear order of operations for evaluating arguments to a function or
recursive let bindings. We have to over constrain everything. If we use monads
to specify the sequencing, you can express just the constraints you need in
the code directly. But as soon as you start to use monad for these
constraints, you have exactly ST@.

Where does this leave us?
* Continue to discuss explicit args, exit versus args, exit via side effects.
* Continue to use generic monad for describing individual IO interfaces.
* Provide IO and ST instances for each of the IO interfaces.
* Provide a builtin runner for ST that can run it as a pure computation.
  - This can't be implemented as pure fble code, but should be easy to
    implement as a builtin function. To do as pure fble code, we need some way
    to erase and recover types. I don't know how to do that in general without
    tracking types at runtime, but we can do it easily enough with static type
    checking with a builtin ST interface.
* Provide a builtin runner for IO that can run as applications.
* Continue to discuss the details of what/which individual IO interfaces I
  want to provide as part of the standard library.

Remember also that the exposed runner interface can be different from the
advertised generic app interface. I expect a single or very few fixed low
level IO interfaces implemented as things like stdio.c, app.c. I expect a few
standardized collections of generic interfaces for people to use, but also the
ability to implement a customized generic interface by composition of IO/ST
interface instances for each of the IO interfaces as needed.

Maybe a good next question is: what should be the fixed low level interface?
* stdio.c, app.c? Can we come up with a single one that works everywhere?

In other words, how reasonable would it be to have:
* List@<String@> args accessible via a GetArgs interface?
* Return Int@ exit code via an Exit interface?
* Set up app width, height, event/effect interfaces via side effects?

String args and exit code seem doable, but also maybe not necessary if we want
to say our low level interface aligns with C99 main programs.

The app interface could be done as a layer over SDL. Something like:
* App function that creates an app and returns width, height, event/effect
  ports and, I suppose, closes whatever existing app is running.

Maybe it's more a question of how much of the code we want to have in C and
how much in fble? As much as possible in fble is more flexible for writing
fble code, but also adds a layer of complexity we could avoid, and provides a
nice layer of abstraction between different platforms.

---

It worries me slightly that things like 'Signal' and 'Exit' are not pure state
interfaces. They interrupt the flow of control. That suggests ST@ may only
work for interfaces that can be modeled as impacting state.

Anyway, let's look through C99 again, and see what it suggests:
* abort - signals API
* atexit - not sure if/why we need this? Maybe signal handling related? 
* exit - not quite signals API. Hmm...
* getenv - easy? Depends if we have setenv too. If just getenv, it can be a
  pure function?

Sigh. It's not any easier the second time around.

---

In light of the observation that signals couldn't be purely modeled as ST@,
let's take a closer look at monad composition. Maybe it's not so bad as I made
out.

The idea is, you start with some monad, and you modify it to support
additional side effecting operations. Let's say I've stacked and composed a
bunch of stuff, for stdio, for files. Again, this is for pure modeling
purposes. Now consider I want to add two more: getenv, and signals.

I want an interface like:

  <<@>@>@ GetEnv@ = <<@>@ M@> { *(
    ... N@,                 # Output monad type.
    Monad@<N@> m,           # Output monad instance.
    GetEnvI@<N@> getenv     # Instance of GetEnv for that output monad.
  ); };

  <<@>@ M@>(Monad@<M@>) { GetEnv@<M@>; };

And similarly:

  <<@>@ M@>(Monad@<M@>) { Signals@<M@>; };

Now I should be able to compose, given M@, Monad@<M@>:
  
   GetEnv@<M@> getenv = ...(m);
   Signals@<getenv.M@> signals = ...(getenv.m);
   main<signals.M@>(...)

Here's the problem. The getenv interface is on getenv.M@, not signals.M@.

I kind of doubt we could come up with a general way to take any Foo@<M@> and
lift it to a Foo@<getenv.M@>. How might that work? I think details are needed
to better understand.

  G.M@ = <<@>@ M@, @ A@> {
    (Env@) { M@<A@>; }
  };

  G.Do mx f = \env ->
    Do(mx(env), f)(env);

  G.Return x = \env -> Return(x)
    
  G.Lift mx = \env -> mx
    
Okay, so I can easily lift something of type M@<A@>. How about something of
type M@<A@> -> M@<B@>? I want to lift it to G.M@<A@> -> G.M@<B>:

  lift f = \gmx env -> G.Return(f (gmx(env)))

Perhaps we could define a Lift@<A@> interface that let's you lift something
for any type A@. Then provide the instance of Lift@<A@> for all the relevant
interfaces. But this really feels needlessly complicated. Surely there is some
pattern we could auto-derive things for?

Like, all structs, all unions, all functions get lifted the same way. So can
we write code that's generic to the structure of the type and ignores the
field names, etc? That would be useful for equals implementation too. And ord,
and stuff like that.

---

Coming back to what to do in practice:
* List@<String@> args for command line args.
  Easy. I see no reason not to do this.
* Return Int@ for exit status?

Today I return Bool@ for exit status: pass or fail.

C99 suggests you can return 0 or EXIT_SUCCESS for successful termination,
EXIT_FAILURE for unsuccessful termination, otherwise it is implementation
defined.

Assuming C99 is attempting to return success vs. failure, it makes sense to
return a bool, rather than a more general Int@ that is interpreted in an
implementation defined way.

But I feel like, in practice, we can and do write C programs that return
different non-zero exit status. I certainly do. Who defines that? libc? bash?

Bash says exit status is exit status, or 128 + n for signal n. That suggests
no more than 128 exit status values are interpreted. The man page I have for
exit says the least significant byte is returned to the parent.

sysexits.h uses a collection from 64 to 78 to mean different things.

Maybe a broader interpretation of exit is provided in POSIX.1-2001? Looks like
POSIX defines wait, which has something to say about exit status.

I'm implementing stdio main in C, and I'm trying to stick with C99. That means
the only thing I can legitimately do is return success or failure. And in
theory I'm doing it wrong today by picking arbitrary numbers for exit status.
I should use 0 or EXIT_SUCCESS or EXIT_FAILURE and nothing else.

It sounds like, if I'm after portability, I should return success or failure,
and nothing more. I could return Int@ and let the platform do whatever it
wants with it, and leave that up to the programmer. But then in theory I would
need to provide EXIT_FAILURE, otherwise they wouldn't have a way to portably
indicate failure.

Seems like python sys.exit chose the approach of: zero for success, non-zero for
failure, but you better know what platform you are on to make sure you pick
something reasonable. There doesn't seem to be a well established convention
beyond 0 vs. non-zero. How useful is it in practice to distinguish between
them? When would someone depend on the exit status?

I depend on the exit status for fble spec tests:

  switch $type {
    compile { set es 2 }
    runtime { set es 3 }
    default { error "unsupported error type: $type" }
  }

Right? Except I don't see it documented anywhere? Oh, yeah, FbleMainStatus.

It seems like what I should do is come up with a philosophy for how to think
of IO and integration with unix/posix/other existing platforms. Use that
philosophy to guide my decision making.

---

There are a bunch of different standards that allow you to write programs to
take full advantage of whatever platform you are on. The more we support, the
more you can write programs to do different things. But not all features are
supported on all platforms. If we support some feature on one platform but not
another, your program will work only on that one platform.

Potential philosophies:
* Pure fble-ness. Pretend nothing else exists today.
 - Not practical. You couldn't write anything useful.
* Lowest common denominator.
 - Portable, but maybe not practical if it can't do stuff like sockets that we
   want in practice.
* Pick existing standards.
 - We could support one or more existing standards.
 - The developer picks what they want.
 - It's up to the developer to be as portable or as capable for a specific
   platform as they want.
* fbleisms versus standardisms.
 - file IO is a good example. An fbleism would be M@<Output@>. A standardism
   would be more like writing to an Int@ representing the int file descriptor.

My feeling is we support specific standards on an as-need basis. Try to
support everything that standard supports (?). But also provide a suggested
'fble' standard, which is a more portable subset of things I think most people
want to do with fble.

Undefined behavior is an interesting issue. Like exit code for C99. You have
0, EXIT_SUCCESS, EXIT_FAILURE as defined behavior, and any other non-zero as
undefined behavior. If you know what platform you are running on, as a
programmer you could make some assumptions about that undefined behavior. So,
should my C99 interface be only the defined behavior, or allow and preserve
the undefined behavior too? Or, should I say, implementation-defined behavior?

Seems like a lot of the IO is related to posix shell:
* You can invoke a command.
* You pass command line arguments.
* You have an environment you can modify when you invoke the command.
* You have redirection of files, with stdout, stderr, stdin

dash man page claims it's based on POSIX 1003.2 and 1003.2a.

Another consideration: think about the C API for sockets compared to the tcl
API. TCL is so much simpler and nicer. Who would want to use the C API? From
that point of view, maybe it's better for me to pick simpler APIs and
encourage fble users to adopt those, rather than try to provide complete
capabilities via existing API standards?

That almost suggests I shouldn't think in terms of C99, posix, or that like.
Think in terms of what's more natural to me as a programmer and the common
things I want to be able to do, expose those, and implement them via C99 or
other. Better not to inherit all the complexities of all these standards. And
I cringe to think about supporting pthreads, for example, in fble.

I suppose what really matters for IO is just the input/output? You could say
pthreads is an internal implementation detail? Though there may be some
non-determinism / concurrency that you can express with pthreads you couldn't
express otherwise? Or things with signals and such?

Fble is about functions. The most natural way to write my program is as a
direct function that takes the types I care about and returns the types I care
about.

I need a compatibility layer to interface with, e.g. a posix shell. Generalize
inputs to strings + env. Generalize outputs to... strings/files? Introduce a
way to interact with streams/file IO.

I wonder if it's worth coming up with some crazy way of generalizing things
instead of thinking in terms of C programs?

Either way, we want something that can be generalized: any specifically typed
program can be represented as this single common type, which is the type that
interfaces with the rest of the world. Try to get into the fble-isms as soon
as possible, and only go back out at the last moment.

Something like:
* Input: string for small things, stream/file of char or byte for large things.
* Output: string for small things, stream/file of char or byte for large things.
* Status code for success/failure?
* Interaction: streams for interaction (via sockets, files, or otherwise?)

On the other hand, we could think of the user interacting with a posix
program, and the stdio interface is really about providing a way to convert
between that posix interface and a raw fble function.

If we had an Android app, we could think about a different conversion layer.

I wonder if IO shouldn't be part of Std. At least, not things like files, env,
stdout, stderr, etc. That's all pretty specific to ... command line linux
programs. Very different than for Android apps, Windows apps, etc. Why not
have a separate compatibility layer / package for that kind of thing?

Which brings us back to the question of what standard to use, and that C99
seems to be a pretty reasonable standard to work with to start. But just look
at the input/output related features of the standard, not internal things.

---
Things I definitely want in some form or other:
* command line arguments, exit status, stdin, stdout, stderr, file IO, env
  access

Things that are less obvious to me:
* signals, abort, exit, atexit
* full filesystem manipulation
* system command

Let's think about the less obvious ones. Maybe I can rule those out, at least
for the first iteration.

Thoughts on 'exit':
* As in, explicit call to exit rather than by returning from the top level
  program.
* It changes control flow. If we wanted, we could have a monad to describe
  this control flow.
* Seems rather un-modular to me. To allow any function to terminate the
  program. Regardless, we would want a monad to describe the functionality. If
  we had such a monad, we could just use a monad to propagate it.
* Which suggests it's fine to just return exit status code from the top level
  function. No need to also support an IO side effect for exiting. Good.

Thoughts on 'atexit':
* That would be part of an Exit@ monad if we had one. As with 'exit', we can
  implement it as part of running the Exit@ monad. No need for something
  special built in.

Thoughts on signals:
* Reminder of what it contains:
 - A set of standard defined signals: SIGSEGV, SIGINT, etc.
 - The ability to raise a signal explicitly.
 - The option to handle a signal - on top of the stack, then return to
   whatever code was interrupted.
* From the point of view of interacting with the outside world, I see two
  things:
 - Being able to respond to a signal sent to the program from outside.
 - Being able to return a 'signal n' exit status.
* It doesn't make sense to me to interrupt pure code for handling signals.
* The alternative would be to have a monad that checks for signals as part of
  'Do'. I could imagine having such a thing. It could interrupt control flow,
  so couldn't be done as ST monad.
* Seems like it's mostly a question of whether we care enough to support
  signals. Maybe this is a good thing to slate for 'future work'. So long as I
  have a decent way to add additional IO capabilities in the future.

Maybe let's think a little more about a decent way to add additional IO
capabilities in the future. That way I could focus for now on an MVP style
capabilities, and leave the door open for future support for more esoteric
things for people who want or need those.

---

The nicest way to add additional IO from a user perspective is to add new
modules that we can reference, preferably without changing the top level type
of the program. Any new app would refer to the interface from the new module
in its main function. Any existing old app would not have to change at all.
That's the goal.

---

ST monad feels sketch to me. The fact that you can't implement it in pure
fble. That it does some magic to be able to cast values to any time. That it
allows a function to secretly record information you pass to it. That it
allows two different functions to secretly communicate and hence have a
potentially extremely complex state machine. That it basically says: You can
do anything you want to the ST state. Or, rather: you can have an arbitrarily
complicated ST state without me knowing about it?

I'm on the fence. The alternative would be a state monad or state transformer
monad. How is that any better? Yeah, the state has to be explicit. But in
practice you are just going to copy and share out whatever abstract type the
function wants you to. Better not to have to mention that type if you can't do
anything about it. More modular to be able to change the internals of the
state without the caller having to know about it.

It comes down a bit to the question of what should be part of the interface of
a function and what should be part of the internal details. Is internal ST
state part of the interface?

In terms of input/output, read only variables like we have today are like a
kind of input? ST would allow you to interact with other entities, using true
input/output. As a caller, you can't tell when two functions are otherwise
communicating behind your back. That sounds scary. Except if you constructed
the two functions, you could tell. You could at least prevent them by not
letting them have access to the same pointer.

For the record, to make state transformer monad work better for composition,
rather than try to do (M@, Monad, IfcA) ==> (M@*, Monad*, IfcA*, IfcB), we should say
you are given the final monad instance and the way to interact with it and you
provide your interface there: (M@, Monad, StateT@<S@>) ==> (IfcB). It's
the callers job to give you an implementation of StateT@<S@> for whatever
state you need internally to model things.

Composition would then look something like:

  % M = State<*(SA@ a, SB@ b, ...)>;
  M@ = M.M@;
  Monad = M.Monad;
  IfcA = MkIfcA(M, Monad, (S@ s) { s.a; }, (S@ s, A@ a) { s.@(a); });
  IfcB = MkIfcB(M, Monad, (S@ s) { s.b; }, (S@ s, B@ b) { s.@(b); });
  ...
  M.Run(main<M@>(Monad, IfcA, IfcB, ...));

I could standardize how I define things so it's pretty cookie cutter boiler
plate to compose together. No need for ST monad.

---

Here's what I propose for a general philosophy:

POSIX, C99, etc. provide ways of composing functionality together. Fble too. I
am happy with using fble for internal composition. Thus, an fble program
should be built on fble based composition.

But I want fble programs to be useful in POSIX / C99 environments too. I want
to be able to make an fble program a component that composes with other POSIX
/ C99 things.

That's different from saying I want an fble program to support construction
via POSIX/C99 composition. The fble program is a leaf node in the composition.

So what I'm interested in is a way to export an fble program in a POSIX/C99
usable wrapper.

For example, consider the environment. I claim there's no reason to support
setenv inside of fble. Because fble doesn't support fork internally (use fble
composition, not posix composition internally), and setenv would only effect
the fble process, not the outside world. Nobody would see the results of
setenv except for fble itself. But if you want to pass around that kind of
thing internally in fble, use fble mechanisms to do so, not the external ones.

Let this be our guide.

Hopefully this means I don't need to support every little different thing in
fble. Use tighter, more well defined interfaces than C99 and POSIX define.
And I definitely don't need to use the same details as those standards define.
For example, no need to give access to Int@ file descriptors and require that
open allocates the smallest available file descriptor next.

---

It sounds like POSIX is trying to be compatible with C99. In some cases it is
more specific about behavior. For example, for exit status, C99 says 0,
EXIT_SUCCESS, EXIT_FAILURE, otherwise implementation defined. POSIX says
EXIT_SUCCESS is 0, EXIT_FAILURE is between 1 and 125, the least significant
bits of status are available via wait, and the full bits of status are
available via waitid.

Looks like POSIX standard is available online:
* pubs.opengroup.org/onlinepubs/9799919799.2024edition/mindex.html

Which downloads and extracts as susv5-html.

So what do I want for fble standard IO? C99? Posix? Some fble-ized version of
it?

I want something standard for fble programmers to be using. For example, if
you are writing an app, you should use the same I/O as if you are writing a
command line program. Don't have 2 or 3 different standards. Pick one. Make it
work well with fble. Don't tie it explicitly to C99 or POSIX, but make it so
we can implement it in C99 or POSIX so that we can use fble programs in those
environments.

Okay? Philosophy is clear? C99 and POSIX standards are clear? Let's step back
then, go back to the beginning, re-examine things in this light. Figure out
what makes sense to support and what not, and what APIs I want fble
programmers to use.

---

1. Command line arguments.

Easy. Pass a list of strings, like we do. Done. Can easily be modeled in pure
fble.

2. Exit status.

C99: 0, EXIT_SUCCESS, or EXIT_FAILURE.
Posix: EXIT_SUCCESS = 0, EXIT_FAILURE in [1,125], others accessible various ways.

Proposal: Switch to Int@, use the posix interpretation. No need to define
EXIT_SUCCESS or EXIT_FAILURE, but I can if I want.

That feels more consistent with what I would expect to be able to write, and
avoids frustration for people who just want to distinguish between a couple
different error codes, like I do for fble-test.

Implementation is straight forward. We already have a dependency in Int@ via
String@, so no extra cost there in terms of complexity.

Can easily be modeled in pure fble.

3. Environment.

I'd like to think of it as read only. If we do that, than we should be able to
get away with a pure function: (String@) { Maybe@<String@>; }.

We can distinguish between empty value and unset, right? Yes. Thus the need
for the maybe. We can call this function 'env'.

Can easily be modeled in pure fble.

The concern with making this a pure function is what happens if some non-pure
code runs and decides to call setenv, and then we get the wrong value? We
could read the entire environment at process startup, but that may be unduly
expensive, particularly if most programs read little or none of the
environment.

python reads the environment up front and saves a copy. Maybe we should do the
same to be on the safe side? Or... be ambiguous, don't worry about it to
start, fix any 'bugs' in the implementation later on if someone starts mucking
with the environment.

Note: we could in theory provide GetEnv as a pure function that's builtin.
That feels wrong to me. Because its value depends on the environment you run
in. Better to pass it as an argument. Also, that way we can test it by
providing alternate values.

Okay? Good. Let's settle on that.

4. stdin, stdout, stderr.

---

Trouble with updating exit code from Bool@ to Int@:
* FbleStdioMain function can generate its own errors. How to distinguish
  between these and user returned errors? We're talking things like compile
  error, runtime error, usage error, other error.

Now this is a real conundrum. If on the one hand I want to fble program to
be able to determine its exit code, but on the other hand, I have legitimate
fble-stdio specific error codes. What do I do?

The easiest way out is what I have now: Success or Failure for fble programs
only. Use the rest of the posix space of exit status for fble-stdio things.

What do other interpreters, like python do?

Python
* I don't see anything in the docs, but if I give bad args, I see exit status
  of, e.g., 2.
* Python does define a bunch of standard os exit statuses. I wonder if that's
  sysexits.h? OK, USAGE, DATAERR, NOINPUT... Yeah. That's exactly sysexits.h.

tcl: I don't see anything specific.

Java at least says explicitly it will return 0 or non-zero, but the
application could also return whatever it wants to.

Seems to me like nobody has a good story about this. Anything I can do?
* Pick high value exit status for things returned by fble-stdio to reduce
  chance of potential overlap with what the app returns? That seems to be what
  posix does with shell: 126, 127, and possibly 128+ are all used for other
  things.

Let's face it, seems like exit status was a bad idea from the start, besides 0
and 1.

If I want to be posix reasonable and distinguishable from other standard
things, I should pick something between 1 and 125 for all my error codes. If I
want to minimize chance of interfering with the application, I should pick
higher numbers. But doesn't it seem likely others would think the same? What
if you want to implement your own shell like application, and that itself can
have errors?

I think it's safe to say the goal of exit codes are:
1. success vs failure, for use in pipelines.
2. application specific meaning of failure status.

The problem is, in the case of (2), we have many layers of application: shell
invokes interpreter invokes program. They can only have 1 exit status at the
top level. How can you distinguish between things?

The only way to make use of different non-zero exit statuses is to know
exactly what you're running and exactly what you expect it to return. It's
pretty common, it seems, for programs to return different exit status for
different failures. I know I make use of that for fble tests, for example.

So there are two approaches I can take:
1. The pass/fail approach
 - Well defined. Can be well defined on platforms outside of posix.
   Practically useful. Leaves space for well defined fble-stdio exit codes in
   a posix environment.
2. The Int approach.
 - There's always going to be some platform or environment where you can't
   distinguish your exit codes. But we/you can arrange to make that less
   likely.

I guess it's a question of how important it is to allow a program to specify
different failure codes? I feel like there's a clear urge to be able to do so,
but practical uses are much fewer. You can use stderr for more details.

I suppose there's another option:
3. The Int approach, but interpreted as pass/fail?

For a specific application, I feel like I can rely on the exit status. The
problem in this case is more one of composition. It's possible we won't be
able to compose things into an application that can clearly disambiguate
between different status results. As long as everyone clearly specifies how
they handle exit status, then things are well defined, if possibly ambiguous.
Then our goal should be reduce the chance of ambiguity.

The trouble is, if an application can return any exit status, there is no way
to eliminate the chance of ambiguity. There is no way to have any meaningful
exit status from fble-stdio that isn't app specific, because it could be the
app returned that exit status. The only option to allow for meaningful exit
status from fble-stdio beyond what the app returned is to limit the range of
exit status the app can return.

Okay, so we use a similar approach as others have:
* You can return any Int@ you like.
* 0 is guaranteed to be EXIT_SUCCESS.
* non-zero is guaranteed to be EXIT_FAILURE.
* We can distinguish between 0 or more, but not an unbounded number of
  non-zero exit status codes. We'll use modulo of some kind to give what info
  we can.
* Now I can pick fble-stdio exit codes with large enough number to define the
  range of exit codes an app can return when running with fble-stdio.

Here's a question for you: 
* Does bash treat exit status of 256 as success or failure?

It treats it as success. That makes things harder unfortunately. I don't like
it. What I want is:

 0 ==> 0
 x ==> (x-1)%N + 1, where N is number of legal exit codes.

For example, if we have N = 4, exit results are:

 0: 0
 1: 1
 2: 2
 3: 3
 4: 4
 5: 1
 6: 2
 ...

But that's not a pure bitmask. So you need to be careful if interpreting
things that way.

An alternative option could be to clamp: any exit status greater than X is set
to N. You have full visibility in the supported range. Put your most important
cases there. You don't have access to things outside that range. That sounds
better to me, to be honest. It doesn't work as well for something like fsck,
which can literally return 256 different exit status codes in theory.

Okay, so here's a proposal:
* Exit status is Int@. 0 for success. Nonzero for error. Application specific
  interpretation of error status codes.
* fble-stdio or other interpreters will have a maximum number of supported
  application error codes, starting at 1. For maximum portability, keep your
  error codes as small as possible.

That can be the approach I use in fble. Why this is good:
* If all you care about is pass/fail, it works great. In theory it can work
  totally portably, because every platform supports at least success/failure.
* If you care about more, you can say what you care about most, and make use
  of it on platforms that support it.

Okay? Sound good? Sounds reasonable? Sounds like a plan? Good. Let's do it.

Steps:
* Change stdio return type from Bool@ to Int@
* Document interpretation of Int@ exit status.
* Change FbleMainStatus to use higher values for non-app exit status. Let's
  say... 96, 97, 98, 99? Or, 'c', 'r', 'u', 'o' converted to ascii? That's
  kind of fun, and around the same range: 99 to 117? Or, maybe better, allow
  96 user defined exit status codes, then go 97, 98, 99, 100, or 'c', 'r',
  'u', 'o' if you prefer.

Cool. Wish me luck. 

---

This is a bit messy. Let's sort things out:
* Use 112 instead of 96 for the base.
* FbleMainStatus - unrelated to stdio. Just running programs in general.
  Status codes 0 through 1, say? As defined. It's only use is to understand
  how FbleMain failed, if it did. Nothing else.
 - Today I'm abusing it for a lot of other things I shouldn't be.
* FbleStdioMain returns [0, 111], 112, 112+FbleMainStatus.
* FbleAppMain returns [0, 111], 112, 112+FbleMainStatus.

Perhaps I should add helper functions for converting to standard exit status?
And have a common point of sharing? Probably yeah.

---

While changing exit status over from Bool@ to Int@, I think it would be useful
to define 'Success' somewhere. Maybe Failure too as 1. Just to reduce
instances of magic numbers.

In that case, what do you think about defining an Exit@ type?

I do like use of 'Success' and 'Failure' rather than 'True' and 'False'. It's
a little weird using Int equality for Exit@ comparison though. At what point
should the synonym be part of the interface as opposed to treating Exit@ as an
abstract type?

Anyway, the switch over is done for exit status. I like the use of Success and
Failure in code. I like the proposed standardization of exit status and
interpretation. Cool.

---

Concern with my approach for getenv: can we really pretend the environment
doesn't change during runtime? For example, what if you change the working
directory. Does PWD change?

Some stats on my computer:
* 45 environment variables
* 421 bytes worth of environment variable names.
* 3080 bytes worth of environment variable values.

On python: os.environ['PWD'] does not change after os.chdir
On tcl: array get ::env 'PWD' does not change after cd
On C: PWD doesn't change after chdir in the environment either.

PWD must be a shell special thing.

So maybe I don't need to be so afraid?

It looks like C99 doesn't even have a setenv/putenv function. Interesting.
environ is part of posix, not C99. I'm sort of using posix for fble-stdio,
based on the interpretation of exit status.

Implementation options:
* Pure call to C99 getenv on demand.
* IO call to C99 getenv on demand.
* Cache results of C99 getenv to avoid changes during runtime.
* POSIX environ based copy of everything at startup.

Here's a crazy idea. The fble-stdio run function could take two arguments: a
list of environment variables to read and the Main@ function. It could read
the environment from that list at startup, then pass a pure function for the
environment. The benefits being:
* We only read what you ask for at startup (so... no conditional env var
  queries?)
* It's well defined and intuitive when we query the environment variables.
* No need to iterate over everything or do a bunch of work if no environment
  variables are read.

The implementation could use an IO based getenv internally.

The downsides are:
* No way to iterate through list of all env vars - fine. You can't do that in
  C99 either, and it sounds like an abuse to me.
* You have to know all the environment variables you want to look for up
  front. Tricky, in case the variable to read is based on a command line
  argument or file IO or another environment variable's value.

I feel like it's a common case to read a config file that refers to an
environment variable that you don't know about at program startup.

Proposal:
* Assume, don't enforce, that environment does not change at runtime.
* Provide a pure getenv function, implemented via direct call to C99 getenv.
* If the assumption doesn't hold well in the future and that causes problems,
  we can change the implementation to make a copy of environ at startup and
  use the copy.

---

Do you ever imagine adding a setenv interface to fble? If so, then getenv
needs to be able to observe the results of setenv. Then we need to do it via
the monad. If not, then getenv should conceptually read the env at startup and
not change again; it's only a performance concern to implement it that way.

Why I don't want setenv: the general philosophy. I'm not going to be forking
from within fble, so nobody would see the modified value except callers of
getenv. But in that case, they ought to just use fble code to pass that
information, not abuse getenv/setenv as a back door IO channel (they could
use files for that if they wanted I suppose...).

Why I might want setenv: ??? Even if we want to fork something, we could ask
people to pass an explicit env to use at fork time.

Review of posix getenv/setenv/putenv/environ with an eye towards what changes
and what stays the same if you stick to those interfaces:
* extern char** environ
  You can completely replace environ by updating the char** it points to.
* Behavior is undefined if the application modifies the pointers to which
  environ points. Not sure how to interpret that precisely:
  a. environ[3] = "mynewvalue"  ?  pointer is modified?
  b. environ[3][4] = 'a';       ?  contents pointed to is modified?
  The language sounds most like (a) is not allowed?
* The app isn't allowed to modify the string pointed to by the result of
  getenv unless it has replaced environ or used putenv.
* getenv is supposed to return one of the pointers in environ.
* Only setenv, unsetenv, putenv are supposed to be used to modify what environ
  points to. Or point to a different environ. That suggests again option (a)
  from above.
* Sounds like putenv is the only legal way to be able to modify the contents
  of an environment value without changing out the pointer.
* Worst case I could always iterate over env, make a copy of everything it
  points to. The real question is, can I just copy the pointers, or I have to
  copy the byte contents of the entries as well?
* Sounds like setenv changes what environ[x] points to, rather than the
  contents of the existing environ[x].
* Not clear what unsetenv can do. It can remove a pointer. Can it free the
  underlying memory too?
* After calling putenv, you are allowed to modify the contents of the string
  and that has the side effect of changing the environment. But at least
  putenv is supposed to keep the string value around as long as anyone might
  refer to it.
* The environment list is limited by ARG_MAX?

In short... sounds like it's risky to just copy pointers for the following
reasons:
* If they were initialized via putenv, the contents could change at any time.
  Thus avoiding any potential benefits of the copy.
* If unsetenv is called, the pointer might be freed.

Seems like the spec is geared towards making it reasonable to implement
setenv/getenv/unsetenv rather than making it easy to copy.

I don't know. Review of considerations:
* C99 only has getenv.
* Performance overheads of upfront full copy.
* Would we ever want setenv?
* What makes sense from an application point of view as opposed to
  implementation point of view?

Options:
A. (String@) { M@<Maybe@<String@>>; }
 - Can use getenv directly. C99 works fine.
 - No performance overheads. No upfront copy.
 - If we want setenv some day, it works fine.
 - Whether this makes sense or not from app view depends on how we deal with
   streams and files.
 - Still trivial to model as pure: (String@ x) { m.return(Lookup(env, x)); }
B. (String@) { Maybe@<String@>; }
 - Can't make a safe upfront copy in C99.
 - Performance overheads of upfront full copy.
 - Doesn't work if we want to add setenv later.

When you put it like that, (A) seems better in every way. Let's stick with
that. That's what I'm using now anyway.

Cool. That sounds like the right approach.

Next up, the hard stuff:
* streams, files, etc.

Hm... Taking a step back. Using the reader approach for env means potentially
any M@<...> operation could change the environment? Not for subsequent
operations though. Just for child operations, right? And I'm not providing any
way to do it. I guess you still have to trust somebody else isn't given the
ability to do so.

Anyway, let's think about streams and files.

stdin, stdout, stderr. Output stream is (Output@) { M@<Unit@>; } for character
streams. Which is what we're using in these cases? Input stream is
M@<Maybe@<Char@>> for character streams, which is what we are using for stdin,
stdout, and stderr?

---

Taking a step back. Command line args, exit status, environment, stdio, file
system... that's all very C99/posix specific. Think about Android for example.
There's no concept of command line args or exit status. Standard streams
aren't really used.

Why does it make sense to bias my standard io so much towards C99/posix?

How about we take a step back and consider the fundamentals of what's going on
here. Something to do with side effects, multi-tasking, shared external state,
task creation and removal.

fble has functions. We can describe side effects with the use of monadic
programming. It's a good way of describing side effects. It feels natural and
sort of composable. In a single threaded environment like fble, every 'side
effect' and composition of side effects is atomic. Nothing else is going to
change the world externally while you are running, because you're the only
thing that is running.

As soon as we interact with the outside world or some other thread of
execution, we lose that atomicity. The only way to do useful things is to
change state outside of yourself that something else can observe. It takes you
time to make those changes. You don't want the outside world to see the
intermediate effects. You want it to appear as if you had an instantaneous
effect.

My gut tells me this might be a reasonable multithreading approach: you have
full control of atomicity of side effects. Ignore the practicalities for now.

Okay, fine idea. But where does that lead us?
* Cooperative multithreading?
* Server that can handle multiple requests concurrently?
* Blocking vs. non-blocking behavior?
* Interacting with external state, e.g. files?
* Persisting state across instances. Meaning of private/public in that
  context?

---

Here's how I imagine multi-threaded processes might look in fble:
* We have shared state, which a process can read/write via atomic
  transactions.
* A process can allocate new shared state.
* A process can create another process.
* A process can block on a function of shared state.
* A process can interrupt/signal another process.
* Processes don't return values, though that can be modeled easily enough via
  shared state.
* A process continues to run until it completes, or until there is no longer
  any way the process can impact the top level shared state. So, something
  like: if a process has a reference to the top level shared state, the
  process stays alive. If the process has a reference to shared state that is
  also referred to by an alive process, the process is alive. Otherwise the
  process gets garbage collected.
* Conceptually a process is something you launch that will interact with a
  given shared state over time until it is done. I picture hanging a sloth on
  a tree. The tree is the shared state. The sloth does whatever it's going to
  do.

There doesn't seem to be a notion of persistent state here. That has to be
outside somehow, because we can't have things running when the computer is
off, even though we can persist state.

I'm not providing all the details of the APIs for processes. I'm not claiming
the high level idea is practically implementable. But I think, best of best
world, this is the kind of thing we could expect.

Then the question is: is this useful? Is it something useful to add to fble?
Is it something we could already model in fble? Does it shed any light on how
we should treat the standard IO interface in fble?

It seems possible that posix does not provide the mechanisms and guarantees we
need to support this proposed fble model. For example, no standard posix way
of doing transactions across multiple different files on the system.

I don't think this tells me much about how to access a filesystem. That's an
API for ways to manipulate a specific type of shared state. There is nothing
inherent or fundamental about it. stdin, stdout, stderr are specific ways of
interacting with other processes, but again, nothing inherent or fundamental
about it.

It almost seems like what we need for interacting with the outside world is
something analogous to how we generalize all types into strings. Generalize
all state into ??? A hierarchy of files?

---

Claims:

1. It's not really fair or fundamental to bias the standard IO interface
towards C99/posix. That would be an arbitrary choice, not a derived choice.

2. For fble-stdio implementation, which adapts fble to a C99/posix
environment, it doesn't make sense to explicitly distinguish between a c99
version and a posix version and something more general. It should support
whatever you can write using C code builtins in the platform, and the user can
decide at the fble level which of those are safe to use given their
portability requirements.

3. It's important to write your code in a generic Monad@<M@> monad with side
effecting interfaces passed in, rather than write code directly in an IO@
monad:
 - For testing purposes, so you can reuse the code in a pure test environment.
 - So that the caller can modify the interfaces accessible to the child, for
   more fine grained control. For example, add a wrapper around the file open
   interface to make sure you can only see a subset of the files visible to
   IO@ in general.

In terms of IO vs ST vs side effects vs processes, the interesting question, I
think, is about what state is considered internal hidden state versus what
state is explicitly part of the interface. When working with a generic
Monad@<M@>, the interface is clear: it's whatever you are given to do with M.
But when going to implement a specific M@, we need some notion of state.

Consider a few alternatives:
1. IO monad, you can do anything to the world you want.
2. ST monad, you can allocate and reuse otherwise internal state.
3. state transformer monad: the internal state becomes explicit with Monad@<M@>.
4. Process: the internal state is stored as internal state of a concurrently
running thread/process.

There's some question or issue or connecting thing, or underlying issue or
something about those that can be unified and clarified and settled on. I feel
it.

New idea for how to bridge the gap between an 'open' IO monad and a closed
Monad@<M@> with explicit interfaces: instead of making IO the monadic type M@,
make IO an interface that allows you to create interfaces for builtin things.

Now the top level runner can become something like (ignoring args and exit
status for the moment):
  
 <<@>@ M@>(IO@<M> io, Monad@<M@> m) { M@<Unit@>; }

Where we provide IO implementations of interfaces such as:

  <<@>@> M@>(IO@<M@>, Monad@<M@>) { GetEnv@<M@>; }
  MkGetEnv = ...

The important part here is that everything is still using M@, not IO@, and it
takes extra effort to pass that IO@<M@> around everywhere. So rather than it
being easier to use IO@ than <<@>@ M@>, it now becomes harder. That should
encourage use of more precise generic interfaces. Uh, maybe there's a better
name for it than IO@ if we go down this route.

The top level runner would then look something like:

  Run $ (IO@<M@> io, Monad@<M@> m) {
    GetEnv@<M@> getenv <- m.do(MkGetEnv(io));
    Sockets@<M@> sockets <- m.do(MkSockets(io));
    main(getenv, sockets);
  };

There's nothing to say whether the user implements this adaptor logic
separately from their main code or not, except, if they want to test their
main code in a pure context, they ought to do it separately.

I think this gives us all the building blocks we need to come up with an
approach that I will be happy with. I just need to understand the IO vs ST vs
state monad vs process thing better, particularly with a mind for how to
export MkGetEnv instances that we can use in the pure testing environment.

---

I wish we had something part of the fble language that let us type erase and
unerase. The interface is simple. You go from A@ to *@, then from *@ to
Maybe@<A@> or equivalent if the *@ matches your target A@.

That would allow us to implement ST monad in pure fble. It gives us even more
flexibility, because we can pick our own data structures. We could implement a
process monad.

Conceptually what I want is, instead of using a union type, which is an
explicit, closed set of possible fields, I want an open union type which you
can stuff anything into and out of. Conceptually easy syntax wise too. Could
be something like:

   +() is the type
   +()(type: value) to construct. e.g.  +()(Bool@: True);
   x.<Bool@> to access - runtime error if wrong tag/type.
   x.?(type: ...) to switch.

This kind of thing is not an uncommon language feature. Think of
java.lang.Object, instanceof and cast in Java for example.

The trouble is, I can't think of a reasonable way to implement it given how we
currently deal with polymorphism.

In theory poly apply could pass the actual type value, and we could track and
construct at runtime the actual type value and we store the type with the
value in our open union, and to access it you do a type equality check at
runtime. Painful, very. Especially to add overhead to everything for what is
sure to be a very rarely used feature.

I've tried to think of all sorts of workarounds, easy or hard. Polymorphism
always gets in the way in the end. Also, as soon as we have this ability, it's
possible to hide away types, so we can't do a forall s. ST s ... kind of trick
anymore to ensure some data doesn't get returned by a function.

To get a better idea of the issue with polymorphism:

  <@>@ Nothing@ = <@ T@> { Maybe@<T@>(nothing: Unit); };

At runtime, Nothing@<Bool@>, Nothing@<List@<Int@>>, Nothing@<String@>,
Nothing@, are all the same FbleValue* instance. That value is compatible
between all these types, but the types themselves are not compatible. So even
if we had some way to create a unique key object at runtime for locking and
unlocking values, we can't guarantee the safety of those type conversion.

The only way to safely hide the type of an object is to track the type
dynamically. Otherwise you couldn't combine and swap different instances of
untyped values. Tracking types dynamically at runtime feels expensive. I can't
think of any hacks based on unique ids or other things that are guaranteed to
be associated with a unique type to simplify the implementation because of
polymorphism.

---

Consider 4 alternative approaches to implementing "pure" versions of the IO
interfaces.

1. Using state transform like monads.
You pass a State@<FooInternal@, M@> interface MkPureFoo, it uses that to model
via state internally.

This can be done today. Essentially it means we are explicitly listing all the
internal states of things in the types statically. No language changes
required, I suspect it's tedious, and it feels unfortunate that we need to
expose internal state to the caller when the caller doesn't care anything
about that state.

2. Dynamic types.
Add a language feature for dynamic types. Essentially a new kind of open union
value keyed on types. Requires runtime support for tracking and comparing
types.

You pass a State@<Erased@, M@> interface to MkPureFoo, or some such. It uses
that to allocate and store its own erased state.

Once you have dynamic types, you can do all sorts of cool stuff: a better ST
monad, modeling of processes deterministically internally, maybe dynamic
loading of modules? Fun stuff like that.

3. ST monad
Add a builtin for ST monad, where an object can allocate references of a
particular type, share those references, and then read/write those references.

This is not as general as dynamic types: nobody gets to see the unerased
value, no control over data structures for mapping reference to value, you
can't copy/fork the state of references. I'm not convinced it can be done
entirely in a type safe way if we want to allow people to run ST as pure
functions, for fear references leak from one run to another. All the downsides
(and upsides) of having pointers in the language.

4. Process monad.
Add a builtin monad or feature for supporting concurrency. As part of that,
include the ability to allocate shared state. Feels like a more general, more
sane version of ST monad. Except, the right way to do processes will not allow
you to run them as pure functions, because of the possibility of different
interleavings changing the results. If only we had dynamic types, we could
model and 'run' process monads as pure deterministic functions, though in
practice we would need cooperative multithreading and have some other
limitations like not being able to interrupt a pure function.

Of these 4, (1) is already supported, (2) makes sense to add as a language
feature, I claim (3) and (4) make more sense to add as a library using
builtins. Because we can already describe perfectly well an function that
makes use of the kinds of side effects we need for (3) and (4). We just don't
have a way of implementing those side effects inside fble.

I'm tempted to try all of the above, though it will be a lot of work and a bit
of a digression from cleaning up the standard library. Regardless of which
approach I chose, it doesn't matter for picking what APIs I want to add to the
standard library. It only matters for the pure implementation. The only place
I'm using the pure implementation will be for tests, where I can afford to be
a little weird.

Okay? Let's shelve this discussion for the time being. Go with approach (1)
for now, because that's already supported. Get a better feel for how painful
it is. Consider (2) or others for a subsequent release. Get back to the still
hard question of what standard interfaces we should provide for making things
like shell programs.

Let's summarize where I am there in terms of strategy.

1. High level types of programs we want to compose upwards:
* A command line program: you give string args, it runs and returns an exit
  code. It has access to files and input and output and such.
* A graphical application: you have a window, you can draw to it and get input
  events.
* An ncurses like terminal application: you have a terminal window you can
  draw to and get input events.

2. Platform builtins utilities.
These are implementations of world side effecting things that you can (may be
able to) do on the underlying platform you are running on.
* Read/write to stdin, stdout, stderr
* Read/write to files.
* Access the environment.
* Make sockets.
* Modify directories.
* and on and on.

3. Strategy for high level program
* Provide an fble-foo.c implementation of a top level command/application that
  runs in the system you want to use it on. fble-stdio, fble-app, fble-curses,
  or some such.
* Each of these expects a specific type for the main program to run, providing
  any arguments required by the driver, and an IO@<M@> interface to enable
  access to any builtins. You give it something that works for all M@ with
  that interface, it will run it with a specific instance of M@ appropriate
  for the application.

4. Strategy for platform builtins.
* Provide a Foo@<M@> interface describing the API.
* Provide an IO@<M@> -> Foo@<M@> function to provide an implementation of
  the interface using builtins.
* Provide a State@<S@, M@> -> Foo@<M@> function to provide an implementation
  of the interface using pure state, or whatever other features of a pure M@
  you would need to model the behavior.

5. Composing everything together:
* User provides a main function that works for all M@, with required IO
  interfaces supplied. For example:
    
    <<@>@ M@>(Foo@<M@> foo, Bar@<M@> bar, Monad@<M@> m, List@<String@> args) {
      M@<Int@>
    } Main;
* User wraps that in a custom driver function for IO or pure as desired:
    IOWrapper = <<@>@ M@>(IO@<M@> io, Monad@<M@> m, List@<String@> args) {
      Foo@<M@> foo := m.do(MkFoo(io));
      Bar@<M@> bar := m.do(MkBar(io));
      Main(foo, bar, m, args);
    };
    PureWrapper = ... tedious, details to work out as needed.
* We pass that as an argument to a standard generic runner associated with
  the high level type of program you are trying to run:
    /Std/Stdio/IO/Run%.Run(IOWrapper);

I only support two kinds of top level programs today:
1. stdio - intended to be a command line program
2. app - intended to be a graphical application

I make use of the following builtin interfaces:
* stdio - stdin, stdout, stderr
* files - read, write
* environment - getenv
* sockets - client, server
* debug - trace (but this is slightly different, because it violates pureness)

The builtin interfaces can and should be designed to compose well together.
For example, 'read' can open a stream with the same type that stdin has, so
you can reuse all the standard utilities for working with streams.

For this first pass, maybe I'm okay just supporting those immediate
interfaces. Though to be consistent, I should really try to make them more
comprehensive.

Let's think more on names of things that I have today. First, stdio versus
app.

stdio - takes a list of command line arguments, returns an exit status.
Supports standard input output streams. This is a command line program. It
works with the shell.

app - takes a window that you can draw to, and get inputs. Also supports stdio
interface.

What does 'stdio' mean from a C99 or posix point of view?

C99:
* stdin, stdout, stderr
* file access: rename, remove, tmpfile, fopen, fclose
* Says nothing about command line args or exit status.
* Says nothing about directory access. Nothing about directory hierarchy.

That suggests to me that 'stdio' is a good name to use for an interface which
corresponds to C99 stdio: working with individual streams and files, but where
filename is an opaque, unstructured string. That's a nice interface to start
with.

C99 refers to list of args and int return code as a 'Hosted environment', in
contrast to 'Freestanding environment' when talking about execution
environments. 'hosted' isn't a terribly intuitive name for me. Not as much as
stdio is for stdin, stdout, stderr (and apparently file IO but not
directories).

In C99, getenv belongs to stdlib, considered part of 'general utilities'.

Terminology from posix:
* 'Command' seems like a thing: a directive to the shell to perform a
  particular task. So probably includes the program and arguments.
* 'Application' is a somewhat general name for a program.
* 'Program' - an application to accomplish a defined task.
* 'Screen' refers to a rectangular region of columns and lines on a terminal
  display.
* 'Shell' is like sh, bash, csh: an interpreter for shell commands.
* 'Utility' is some times used. Sounds like this corresponds most closely?
 - Does not include shell builtins. Otherwise I'm not sure how it differs from
   a program or application.

Yeah, sounds like posix uses 'utility' for programs that can be used in the
command name part of a command. Specifically: 'non-built-in utility'.

Since 'non-built-in utility' is not intuitive to me... I'm thinking maybe
'cmd' is good? Short for command? That's what it really means to take a list
of string arguments and return an exit status. It is being used in a command.
It's not a command itself though...

/Std/Cmd%.Main@?

fble-cmd?

On the flip side, what would we call a graphical application?

Some random ideas:
* CLI versus GUI?
 - CLI stands for command line interface. That's good.
 - GUI for graphical user interface.
 - TUI for terminal user interface?

That's cool. I like that distinction: CLI, TUI, GUI. It's exactly that three
that I was thinking of. It very well captures the distinctions between them.
It seems to be standard terminology. fble-cli, fble-tui, fble-gui. Sounds
reasonable to me.

What does it look like, more practically speaking? Focus on CLI, for example.
* fble-cli is the new name for fble-stdio.
* Define generic Main@ for CLI in some CLI module:
  - /Std/CLI%, /Std/Cli%, /Std/UI/Cli? None feel quite right to me.
* Define Run function in some combination of CLI + whatever I want to replace
  'IO'.

Sounds reasonable. Don't need to commit yet.

What terminology do I want to replace 'IO'? The concept I'm going for is
something builtin that is provided by the platform.
* builtin?
* platform?
* ffi?
* native?

Posix 'builtin' means no need to execute a separate utility, because the logic
is built into the shell itself. That doesn't quite match what I'm thinking of.

'Platform' sounds right to me, actually.

  Specific computer hardware, as in the phrase "platform-independent".  It may
  also refer to a specific combination of hardware and {operating system} and/or
  {compiler}

It's just a bit longer of a word than I'm hoping for.

FFI is not quite right: it suggests a way to call into some other interface
generally, rather than to call into the platform specifically. Java uses 'JNI'
for java native interface. NativeInterface might be nice. 'NI'? Or 'OS'?
Wikipedia claims JNI is an FFI. Why did JNI switch to a foreign function
interface in later versions? Is that something to learn from here?

To me, 'native interface' suggests more what I have today: you can implement
functionality using whatever native thing you've implemented things in. Like,
I implemented fble-stdio in C assuming posix whatever. So you can call into
that and do whatever the native platform allows.

'foreign function interface' is more like assuming you have some symbols
available for functions you can call. Those functions can do anything that you
could do in the platform, so we need to wrap them in some FFI monad to
indicate as such (or NI?). But FFI also has a way to convert types. If we
assume a symbol takes some arguments (via registers, or calling conventions
associated with your architecture), then FFI would provide a way to convert
between those and fble.

It's almost like a native interface targets C code, but FFI targets machine
code level interoperability. Since you can call all of posix or whatever from
machine code, that's more general.

The convenience of FFI is you can write everything in fble syntax. The
compiler is responsible for any boilerplate you need.

I don't really want to open that can of worms, but it's something worth
exploring more at some point.

How about for now we use the cryptic name NI for native interface.

So, we have CLI, GUI, TUI. We have NI. What other terminology remains to sort
out?
* Stdio
* Env
* Sockets

That's enough for me for what I have now. Let's just review potential other
things to add, and sort out where they might go.

C99: Signals, Locale maybe.

Posix:
* Configuration interrogation: uname, sysconf, getconf, etc.
* Process management: fork, spawn, exit, wait
* Data access:
 - file access: fopen, fclose, etc.
 - file metadata: access, chmod, realpath, readlink, etc
 - file read/write: fgetc, fputc, fread, etc.
 - directory operations: closedir, link, mkdir, etc
* etc.

Anyway, the point is, I think it's fair to separate file metadata and
directory operations outside of what we call 'stdio', which would be basic
file access and read/write.

Thus I'm proposing:
* CLI, GUI, TUI for top level program interfaces.
* NI for anything magically builtin.
* Env for getenv and maybe setenv.
* Sockets for sockets.
* Stdio for C99 equivalent operations on files. Nothing on directories there.

And that's all I need to get started. That's enough for the first big revision
of the standard library. Flush out Env, Sockets, Stdio as individual APIs.
Don't worry about adding directory operations, etc. yet. That can be future
work.

Good. What's the development plan then? Steps involved include:
* Rename 'fble-stdio' to 'fble-cli'.
* Rename 'IO' to 'NI' (or 'Native'?).
* Change Main@ to pass an NI instead of Stdio@.
* Move Stdio Main to CLI main. Don't mention 'stdio' there.
* Separate Stdio from Env.
* Rename 'fble-app' to 'fble-gui'.

This will be a lot of work. Let's put a stake in the ground, plan to go this
route, implement it, see how painful it is.

---

Maybe we start with the low level interface change.

/Core/Stdio/IO%.Main@, change from

  @ Main@ = (List@<String@>) { IO@<Int@>; };

To:

  @ Main@ = <<@>@ M@>(Native@<M@>, List@<String@>) { M@<Int@>; };

This will be called CLI eventually. For now:
* /Core/Native% defines Native@.
* /Core/CLI%.Main@

/Core/CLI% or /Core/Cli%? Or /Core/CommandLineInterface?

Let's play around:
* /Core/Gui%
* /Core/GUI%
* /Core/Tui, /Core/TUI, /Core/IO /Core/Io.
* /Hwdg%
* /Cli% fble-cli /Core/Cli% /Std/Cli% Cli@ CLI@ Gui@ Tui@ TUI@ GUI@
* /Std/Main/Cli%

I'm leaning towards /Core/Cli% for now, /Std/Cli% eventually. I bet I'll get
used to it. Gui@, Cli@, Tui@ feel better to me than GUI@, CLI@, TUI@. We're
getting rid of IO@, so we won't have that inconsistency anymore.

---

How to make this first step? Honestly, it might be easier to add a new
fble-cli program, and have that coexist with fble-stdio for a little while.
Add support for fble-cli, slowly migrate apps over, then kill fble-stdio.

Here's a question: how do we want to organize the various packages?

I don't want to put everything into a single std package. For example, I don't
want std to depend on libSDL. Should it depend on sockets? Other native
builtin interfaces?

I do want to keep the hierarchy: /Std/App, /Std/Sockets. So maybe split those
off into separate packages: std-app, std-sockets. How about: std-cli?

How about, let's not worry about it for now. I think it will be easy enough to
reorganize later. Just so long as we don't bring app or sockets into std
package directly without thinking about it first.

So, for now, put fble-cli in core.

How will I implement it? It's slightly more complicated, because I need to
provide a monad instance. Can it be some trivial monad instance that does only
sequencing? I need to pick a value for M@.

<@>@ M@ = <@ A@> { A@; }?
Return = (A@ x) { x; }
Do = (A@ mx, (A@) { B@; } f) { f(mx); }

Sounds reasonable enough. The sequencing is there. Hopefully not too hard to
implement Return and Do in C code. That's the most annoying thing here.

An alternative would be to implement M@, Return, Do in fble directly.
Essentially go back to (List@<String@>) { Exit@; }. Not even with the IO@ bit
of it. How to make that safe though? I don't want anyone to be able to write
and run code with that type signature using fble-cli.

Let's try the hard way first. Because I only have to implement it once and not
worry about it anymore.

One detail: how do we decide in FbleCliMain which builtins to support? Like,
if I want to reuse FbleCliMain and fble-cli for a program which uses sockets
builtins. How would I do that?

We want FbleCliMain to be called from the top level, no extra arguments, so
that you can compile to use it directly. But then there's no chance to
register ...

I'm confused. How do we do things today? It already works today. Ah. Because
you have the dependency on it and we'll link it in? Because you compile
against it, not interpret against it. If you ran fble-cli as an interpreter,
we wouldn't get the builtin for network because we have no chance to load it?
I could believe that. Don't worry about that to start.

---

Okay. I've drafted the code. I like separating Stdio from the runner. The code
compiles. How do I test it? Should I implement fble-cli first?

---

IStream has type M@<Maybe@<Int@>>. If M@ is pure, this turns into
Maybe@<Int@>. There's no way to tell when precisely to invoke it. That needs
to be triggered by do, right? I can't just pass around a Maybe@<Int@> that
reads one byte when you instantiate it. We need to pass around a function that
we can call repeatedly for each byte you want to get. That makes things a
little trickier.

Maybe instead we just need: 
  M@ = <@ A@>(Unit@) { A@; }

It's a function, you pass Unit@, it returns the value. The do function is the
important part, right? It looks like:

  Do(ma, f) = (Unit@ u) {
    f(ma(u))(u);
  }

And that's how you do the sequencing properly. Sounds reasonable? Let's try
that.
    
---

The migration from fble-stdio to fble-cli is done now. What's the next step?

Some random thoughts:
* Could we implement fble-app as a thing you instigate entirely via native
  code so that we can reuse fble-cli? In that case... should everything be
  fble-cli, no need for fble-tui, fble-app?

Brainstorm of kinds of things to do next:
* Split off Env@ from Stdio@
* Flesh out desired Env@ API.
* Flesh out desired Stdio@ API.
* Change apps to define themselves with minimal necessary interface?
 - e.g. remove App Main@ and Stdio Main@ types?
* Start pulling things into Std?

How do I feel about the new approach? Not bad actually. I like that it
separates command line arguments and the fble-cli runner entirely from the
Stdio API. I like that the Main@ function we export to users is for generic
M@, but is also the type we use internally. No need to have an unsafe wrapper
laying around in .fble code.

I think the next step should be to flesh out proposals for Env@, Stdio@,
and maybe also think about App@ and Sockets@ interfaces to double check they
compose well with Env@ and Stdio@.

Env@ seems easy enough:
  get :: String@ -> M@<Maybe@<String@>>
  set :: String@ -> String@ -> M@<Unit@>

I don't generally like the idea of 'set' as a global thing, but somebody's
going to want to have it eventually I assume? It's easy enough to implement.

Uh... how about an overwrite argument to set? How about an unset function?

Alternatively, I can start with Env@ with a single get field. No need to worry
about set, delete, or whether those things are visible across different calls
to set/get or fork. Later on, if I add the ability to fork or do anything that
can set or read environment variables, we'll have run to add a field to Env@
with minimal disruption? Or we could start with a single builtin Native@<M@>
to get function?

Not sure.

---

Let's move discussion for Env@ to stdlib.env.txt. It's separate enough from
the others I'm not too worried about composition.

---

There's some idea from stdlib.env.txt that we could have an abstract notion of
a registry as a map from string key to value and reuse that for Env@ and for
Stdio@ filesystem. Let's look more closely at C99 stdio interface to see if
that makes sense.

C99:
* FILE: stream + position + buffer + error + eof
* stdin, stdout, stderr
* text vs binary streams.

C99 functions:
* remove, rename, tmpfile, tmpnam
* fclose, fflush, fopen, freopen, 
* setbuf, setvbuf
* fprintf, fputc, fputs, fwrite, etc.
* fscanf, fgetc, fgets, fread, etc.
* fgetpos, fseek, fsetpos, ftell, rewind
* ungetc,
* clearerr, feof, ferror, perror

In terms of referring to files, it's by unstructured name:
* open, close, remove, rename, and tmpfile

What's weird and different about files compared to environment is the 'data'
changes incrementally. Like somebody could be writing while someone else is
reading? Or you could modify the contents at a specific point in the stream?

The ability to incrementally output to streams is fundamentally important for
stdin and stdout and interactive programming.

What if we had stdin, stdout, stderr be 'streams', but have files be more
atomic in nature? What can you do with files?

1. read - is this a snapshot of the data at the time of open, or depends on
timing of when you read the next bytes? In particular, what if you are trying
to communicate via file and you want to open a file that hasn't been
completely written yet? Like a fifo kind of a stream?

Like, I'm thinking, if we have the concept of a stream, which is interactive,
can we do atomic manipulation of the file system with respect to streams?

Two things to keep in mind:
1. The stream interface - that lets you write and read from the stream,
traverse different parts of the stream.
2. The underlying contents of the stream.

Note you can have two people with two different interfaces to the same stream.

Opening a file for read gets you access to a new interface to the contents of
the file. The interface is associated with whatever contents exist at the time
of open.

So, maybe think about it like this: you have an interface, and you have a pipe
of data. The interface can be used to modify the pipe of data. At any given
time, there is a 1 to 1 mapping from file name to pipe of data. And a given
interface is associated with exactly 1 pipe of data.

I like to separate read/write from a stream. That is, you either have a write
interface or a read interface, or two separate write/read interfaces. Though
they can point to the same underlying pipe of data.

We have two different kinds of streams: text (Char@) and binary (Int@?).
Though I guess the type is associated more with the interface than the
underlying pipe of data.

Seems like I'm proposing the following entities:
* File
* text write interface - tied to a particular File
* text read interface - tied to a particular File
* binary write interface - tied to a particular File
* binary read interface - tied to a particular File
* mapping from file name to file.

The only way to interact with a file is through a text/binary read/write
interface.

Operations on directory of files:
* Get an interface to an existing named file.
  open for read, open for append, stuff like that.
* Create a new file, set its name (or get a random available name?), get an
  interface to it. open for write, creat
* Rename - needed because we can't directly refer to the file itself.

Wouldn't it be nice if we could directly refer to files, rather than creating
and renaming and all that stuff indirectly via interfaces?

I need to better understand the modes of opening a file. Specifically in terms
of when a new file is created versus when an existing file is reused. Imagine
somebody has opened a file for reading in some mode, and goes part way through
reading it. Now I open a file with some other mode. Does the previous reader
have a handle to the same file as me or not?

* read is clear: you are opening the existing file. No new file is created.
* write is unclear:
 - If no file exists, you are creating a new file.
 - If a file exists, you truncate it to 0. So... no new file?
* append:
 - If no file exists, create it.
 - If it exists, start adding to the end. No new file.

In summary:
* If there was a file there before, you are getting that same file.
* If there wasn't a file there, you are failing (read) or creating a new file.

The only way to create a new file with the same name as an old file is to
remove or rename the old file first it would seem.

The stream interface works for a lot of stuff in linux, but not everything.
Thus we have 'ioctl'. Ioctl is more of an arbitrary side effect, but you have
to know the names of the things you want to interact with. Would it make more
sense to provide that kind of interface as the generic way to interact with
the outside world in an untyped way?

So, instead of names of files, you have names of interfaces. What you can do
with the interface depends on the interface itself: if it's a special name,
you know it. But maybe it's not a special name, and you wouldn't have a way to
discover the interface without that special name, so you need a standard
interface regardless.

Writing strings to files is a pretty generic way to do things. The difference
with ioctl is that it works on memory regions directly: arbitrary read/write
of a region of memory. You could do that inefficiently with files: write a
string address, read a string address, etc. The difference is with memory you
get more random access. I don't have that kind of random access in fble... But
also, it doesn't make sense for some streams, like stdin. But it could make
sense for the interface to a stream.

If fble supported dynamic types, we could have a function like:

  (String@, Untyped@) { M@<Maybe@<Untyped@>;> }

String@ to identify the interface. Or maybe you have a separate way to open it
and an abstract handle to the interface. The Untyped@ argument that is
interpreted by the specific interface in question, and the Untyped@ result
interpreted by the caller based on the specific interface in question.

Anyway, that all feels a little bit low level. There must be some nice higher
level abstractions that people want to use that I can provide in a standard
way:
* Output stream - for stdout, stderr, writing to a file.
  (Output@) { M@<Unit@>; } write, M@<Unit@> end.
  But with Output@ support for Char@ or binary via Int@.
* Input stream - for stdin, reading from a file.
  M@<Maybe@<T@>> read, where T@ is Char@ or Int@.
* File naming - for opening files by name for reading or writing.
  Or renaming, or deleting.
  Lookup, Insert, Remove, Rename

Questions/notes:
* How do I actually translate file bytes to Char@? Don't we need to know what
  encoding or locale to use?
* I see no need for buffering options. Write(Output@) let's you output
  everything you have all at once well enough. There's no notion of buffering
  for input: you get what's available. The system can read in chunks to make
  things more efficient internally if it wants. No reason to request it to
  block when it has data otherwise.
* I see no need for ungetc. You can implement that on top of Stream@,
  hopefully? Depends. We'll need to be careful.
* Do I want to support seeking into files? Seems like a function from stream
  to stream. Could be useful for efficiency.
* We probably want some way to provide more detailed error information for
  things that fail.

The idea of having a handle, like a FILE*, that can be implicitly impacted by
side effects to M@ is a little tricky. Consider:

  ungetc

If it's a IStream@ to IStream@ function, it's super easy. I can provide a
replacement function. If, on the other hand, it's an implicit side effect via
M@, that needs some magic to have the side effect take effect. It's almost
like we want to separate the notion of pointer to a stream from the stream
itself. We pass the pointer around everywhere, I update the value, providing a
new IStream@ instance by referencing the previous instance, and everyone with
the pointer switches to using my new value.

Kind of like the question of MIter@ versus get:

  M@<Maybe@<A@>> get;
  @ Iter@ = M@<Maybe@<*(A@ next, Iter@ rest)>>;

Seems like 'get' is a combination of Iter@ + state monad? Maybe it should be
implemented as such?

