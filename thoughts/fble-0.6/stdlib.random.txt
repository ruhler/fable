Random
======

I want a library for pseudo random number generation. Primary initial use is
in games: random food spawning points, random puzzle generations, etc.

Right now I have LFSR. In general pseudo random number generators are a
sequences of states of some arbitrary type S@ depending on the generator,
where each state S@ has some number of pseudo random bits associated with it,
again depending on the generator.

For example, my 8 bit LFSR has 8 bits of state S@, and each state has 1 bit of
randomness associated with it.

Once we have the interface for a pseudo random number generator, we can
compose that together all sorts of ways for higher level use cases. Like, get
a random Int@ in some range. Or draw from some distribution.

The particular choice of random number generator matters because:
* Some are clearly better than others, depending on the use case.
* To be able to repeat a sequence across different runs, you need to be able
  to identify a particular generator to reuse given your seed.
* Over time, better generators are discovered and likely to be generally
  adopted.

High level questions are:
* What should the low level interface for random number generators be?
* Which particular random number generators should I provide as part of the
  library?
* Should we have a default recommended random number generator for people to
  use?
* What high level interface should I provide for getting random things?

The user of a low level random number generator wants a way to capture the
seed, store it somewhere, and reuse it later across different invocations.
Let's be honest, a seed is a representation of the state S@. We could store it
as a String@ or an Int@ generically I suppose. Or allow custom seeds depending
on the random number generator chosen.

If we are all within fble, we could pass around the State S@ explicitly as the
seed.

A relevant question for the low level interface is if we want to reuse that
same interface for true random numbers, which presumably come from outside of
fble.

If we didn't care about true random numbers, I might have a simple interface
like:

  <@>@ Generator = <@ S@> { *(
    (S@) { List@<Bit@>; } RandomBits,
    (S@) { S@; } Next
  ); };

Here I decided to return a list of bits because it makes clear from the list
how many bits of randomness you get from the state. Other options could be to
return an Int@ and have a separate parameter describing the range of Int@ that
is returned.
  
I think it's important to allow the generator to return its natural number
random bits at a time, rather than say force it to return 1 bit or a requested
number of bits, because that makes sure we can clearly identify a particular
state. Alternatively, we could require S@ to track substates, conceptually
some RealS@ plus a List@<Bit@> of bits not yet consumed from the previous
state.

Instead of having two separate functions, we could have a single function that
advances the state and returns the random bits all at once.

  (S@) { *(S@ next, List@<Bit@> rand); }

That's how LFSR works. LFSR also has the option to inject entropy in the
stream, for example to change the sequence depending on whether you move some
direction up, down, left, right in the snake game. I suppose we could always
inject entropy by choosing to advance the state N number of times? That might
not be the most effective approach, depending on the generator being used.

Perhaps the most natural way to interact with a random number generator is
monadically:

  <<@>@>@ Random@ = <<@>@ M@> { M@<List@<Bit@>>; }

A single operation to get the next available bits and advance the state. Or,
optionally, we give a way to get/set the seed:
    
  <<@>@>@ Random@ = <<@>@ M@> { *(
    State@<M@, Int@> seed,
    M@<List@<Bit@>> random
  ); };

What's nice about this interface:
* It works for true random numbers, because we don't have to mention the type
  of state S@ anywhere, that's hidden inside of M@ and generalized away by
  generic seed type Int@.
* We could generalize the interface to return a particular type randomly:
  Random@<M@, Foo@>. The underlying generator can be of type Random@<M@,
  List@<Bit@>>, or whatever it prefers. We build ways to get other types of
  objects randomly on top of that.

We could implement Random@ by providing a state monad on M@ for the type of
random number generator:

  <<@>@ M@>(State@<M@, LFSR@>, Monad@<M@>) { Random@<Bit@>; }
  LFSR = ...;

And hey, if you are passing the State@, you may as well keep it to control
seeding rather than have that be part of the Random@ interface.

And we could always provide State@ and Monad@ instances for a custom StateM@
type for anyone who would rather have the interface above:

  (S@) { *(S@ next, List@<Bit@> rand); }

So, instead of things like:

(LFSR@, List@<Pos@>) { Pos@; } GetNextFood = (LFSR@ rand, List@<Pos@> snake) {
  Shifted@<Int@> row = ShiftInt(rand, MaxRow);
  Shifted@<Int@> col = ShiftInt(row.lfsr, MaxCol);

We would have something like... well, first off, I would rather change
GetNextFood to be M@ so we can ShiftInt twice using Do and := syntax. But
otherwise you still can do:

(LFSR@, List@<Pos@>) { Pos@; } GetNextFood = (LFSR@ rand, List@<Pos@> snake) {
  StateM@<Int@> row = RandInt(MaxRow, LFSR, StateM<LFSR@>, rand);
  StateM@<Int@> col = RandInt(MaxCol, LFSR, StateM<LFSR@>, row.lfsr);

Which is honestly just as easy. Well, details are probably wrong, but I think
it should be pretty easy.

And hey, we don't even really need to define 'Random' as a thing, because it's
literally just: M@<Foo@>.

So, for example, LFSR could be:

  # Get random bits from a linear feedback shift register.
  <<@>@ M@>(State@<M@, LFSR@>, Monad@<M@>) { M@<Bit@>; } LFSR;

Perhaps we should define a generator to standardize the low level feature
support. Basically what our atomic operation is going to be:

  <<@>@>@ RandomGenerator@ = <<@>@ M@> { M@<List@<Bit@>>; };

On top of which we can build RandInt, RandList, RandMaybe, RandFoo,
RandNormal, etc. Where each individual operation uses as many bits as it can
and as few state transitions as it can, but the smallest operation is an
integer number of underlying state transitions so we can always capture a
meaningful seed in between operations.

Sounds like a plan to me. In summary:
* The low level interface is M@<List@<Bit@>>;
* Many of the generators will be constructed with State@ as an argument, which
  is what you can use to get/set the seed associated with that state.
* Maybe we provide convenient ways to convert a seed S@ to an Int@. Or maybe
  not.

I wonder if it's better to ask for a specific number of bits. If the low level
operation knows how many bits it needs, and we know that we aren't going to
ask only once for all the bits we need, and we know that has to correspond to
an integer number of state transitions, then we may as well have the interface
be:

  (Int@) { M@<Int@>; }

Where the argument is the number of bits of randomness desired, and we return
an Int@ in the range 0 to 2^n-1.

That moves the onus for repeat calling into the generator though. Wouldn't it
be better to move that common logic elsewhere? So the generator only has to
care about how many bits of randomness it can naturally return anyway? I kind
of think so.

---

So far I've assumed we want to be able to read out the current state and
reseed back to that. But maybe we don't need to be so powerful. Maybe we just
need to say: if you start with this Int@ seed, we can go into a deterministic
state S@ of the random generator.

That gives a simpler interface for seeding, perhaps. For example, LFSR@ could
provide a Seed function of type (Int@) { LFSR@; }, along with whatever other
parameters you have.

If we wanted to and it's clear how, we could provide a way to convert S@ to
the Int@ that produces S@ when using the Int@ as a seed. If it's feasible. But
also not required.

---

For me the most natural low level interface for using a random number
generator is a single function:

  <<@>@ M@>(Monad@<M@> m)<@ A@>(A@, A@) { M@<A@>; } 

It chooses between two options randomly. We can build everything else up off
of that.

The most natural interface for generating random numbers is...? I feel like
most existing random number generators give you a list of bits of some length.
So return a list of bits: either as an explicit list, or as an Int@ with some
known range to encode the list of bits.

Within fble, as long as we can go from a seed to an initial state, it should
be fine. We need to not change the implementation of things like RandInt
though, otherwise we'll give back a different random sequence.

If you want to make things portable across different languages, it sounds
hard. You have to know the implementations of things like RandInt or whatever.
Or you provide the state transition for the underlying generator and let the
user figure out how to use the well defined states themselves.

The getrandom function takes a count of bytes and fills in that many. Maybe
that should be the low level interface: specify a number of bits and get a
list of that many random bits back. Or specify a number of bits and get an
Int@ back in the range [0, 2^n-1]?

Anyway it probably doesn't matter, because it's not hard to adapt different
interfaces to each other. The more important consideration for the interface
we force on people is probably performance. Which would suggest it's better to
do bulk random calls instead of bit by bit, and maybe better to encode things
as Int@ than List@<Bit@>.

Brainstorm of higher level API to offer:
* RandInt - pick a random integer in a range. Or between 0 and some number.
* Choice: Pick a random element from a sequence.
  Same as RandInt + List get?
* Choices: Pick k random elements from a sequence with replacement.
* Shuffle: randomly sort a list.
* Sample: Pick k random elements from a sequence without replacement.

Maybe I want an interface like Show@, Eq@, etc. to produce a random data
value? Like, random bool@? Or that only makes sense for finite sized data?

What I currently use today:
* random int between 0 and max.
* random bit.

Let's not worry about distributions like normal, Poisson, etc. yet, because I
don't have a floating point representation for numbers yet.

It would be nice if I didn't force a dependency on Int@ for the interface
though. But is List and Bit better? Can I please just ask for a single bit at
a time? Is it really going to be that slow? How about start simple, revise
later if it becomes a performance issue.

Straw proposal:
* /Std/Random%
    Random@: <<@>@ M@> { <@ A@>(A@, A@) { M@<A@>; }; };
* /Std/Int/Random%
    (Random@<M@> r, Monad@<M@> m, Int@ lo, Int@ hi) { M@<Int@>; } Range;
    (Random@<M@> r, Monad@<M@> m, Int@ hi) { M@<Int@>; } Random;
* /Std/Bool/Random%
    (Random@<M@> r, Monad@<M@> m) { M@<Bool@>; } Random;
* /Std/List/Random%
    Choice
    Choices (with weights?)
    Sample
    Shuffle
    
  
Questions:
* 'Rand' or 'Random'?
* Similar to Eq, Show:
    /Std/Bool/Random%.Random 
 or /Std/Bool/Rand%.Rand
 or /Std/Bool/Rand%.Bool
 ?

Let's go short with rand. I think that's fair. And we can be a little bit
ad-hoc on naming. But I would expect a common name, like 'Rand' to return a
uniformly random value across the range of the type. Whereas for integer, we
get a limited range.

Brainstorm of naming from other languages:
* python:
  - randrange - start, stop, step, with some of those optional.
  - randint - a <= x <= b
* C:
  - rand - random int across whole space.
* Java:
  - next - x in range [0, 2^n-1] for given n.
  - nextInt - random across whole range of int.
  - nextInt(bound) - 0 <= x < bound
  - They clearly specify the algorithm in the java docs.
* Go:
  - Int - non-negative
  - Int31 - non-negative 31 bit integer
  - Int31n - [0, n)
* Rust:
  - random - across whole space of given type
  - random_range - selects randomly from a range.

How about this for my naming:
* Rand means across the whole space.
 - Which we can't do for things like Int@.
* Range makes sense to me for lo to hi. Inclusive?
* How about 'Of' for [0, N)? I want a random integer of N choices. Makes sense
  to rename from [0, N).
* Do I want N bits of randomness as an option? Or use Of/Range for that?
 - I think I rather do Of/Range for that. It's too arbitrary otherwise.
 - Otherwise we could call it 'Bits' I suppose.

Next proposal:
* /Std/Rand%
    Rand@: <<@>@ M@> { <@ A@>(A@, A@) { M@<A@>; }; };
* /Std/Rand/Lcg%
    Implementation of Rand@ using linear congruential engine.
    Lcg - generic to parameters.
    Rand48 - parameters of lrand48 which can be seeded.
* /Std/Int/Rand%
    (Rand@<M@> r, Monad@<M@> m, Int@ lo, Int@ hi) { M@<Int@>; } Range;
    (Rand@<M@> r, Monad@<M@> m, Int@ n) { M@<Int@>; } Of;
* /Std/Bool/Rand%
    (Rand@<M@> r, Monad@<M@> m) { M@<Bool@>; } Rand;
* /Std/List/Rand%
    (Rand@<M@> r, Monad@<M@> m, List@<A@> l) { A@; } Choice;
    (Rand@<M@> r, Monad@<M@> m, List@<A@> l) { List@<A@>; } Shuffle;

It's not complete, but I think it's a useful start. Things that are missing
potentially:
- python sample, choices equivalent.
- Rand@ instances for other types like Unit@, Maybe@, etc.

I don't know how to pick my default LCG. Maybe Java's or standard C++? Or come
up with my own choice based on literature review? Sounds dubious...

---

Notes from research on LCG:

  x = (a * x + c) mod m

  m: modulus
  a: multiplier
  c: increment
  x0: seed

Power of 2 modulus have smaller periods for lower bits.

Question: why not use a cryptographically secure prng by default? Then we
don't need disclaimers all over the place about them not being secure. If it's
only 2 to 10x slower, seems worth while? Of course, who knows if I implement
it correctly.

e.g.

  deterministic random bit generator NIST SP 800-90A Rev. 1

Sounds too hard to start though. Maybe use lrand48?
  m = 2^48, a = 0x5DEECE66D, c = 0xB, pull from the higher order 32 bits.

This is the same as Java, so seems reasonable for a first choice.

But if we know the higher order bits are better quality, how many bits do I
take each iteration? Just 1? 32? Just 1 sounds nice and easy to start, to be
honest. No need to keep track of which bits I've used so far. No need to worry
about bits getting less random as we go down the list. Maybe performance isn't
great, but when is it ever in fble?

Let's start here. Nice and easy. Swap out LFSR entirely. See how it feels.

---

How to structure implementation?
* Lcg is fine as a function. We can set parameters for Rand48. I would love to
  have direct Int@ to be able to test it.
* But for Rand@ implementation, I want the higher order bit.
* So, is the type for Rand48 the one that gives the sequence of Int@, or the
  one that gives the high order bit? How to distinguish between the two?

In practice I have honestly two use cases:
1. Be able to test Lcg. Be able to test it with Rand48 against known values
   from another implementation.
2. Pick Rand48 as my Rand@ implementation.

Um... should we also be honest in assuming Rand48 is really the only parameter
set I plan to implement, aside from testing?

Maybe have:
  /Std/Rand/Lcg%.Generators.Rand48
  /Std/Rand/Lcg%.Rand.Rand48

Use that prefix of Gen versus Rand to distinguish among them? Or:
  /Std/Rand/Lcg%.Generators.Rand48
  /Std/Rand/Lcg%.Rand48

Or don't bother with testing and exposing the raw Lcg interface. Just export
Rand48 as an instance of Rand@ given the state monad. And you use the state
monad to set the seed if you want to.

Yeah. Let's keep things simple.

Or, perhaps:

  /Std/Rand/Rng%.Rand           - default random number generator
  /Std/Rand/Rng/Rand48%.Rand48  - specifically rand 48

How to seed properly? How to test? Who knows.

Or, if I have a default. Why not just /Std/Rand/Rng%.Rand?

Oh, if I want to test, I can just observe the intermediate state values. Easy.

Anyway, I have a draft implementation of Rand48, the interface, and a way to
generate random integers in a range. Let's see if we can use this in the snake
game.

---

How to use in snake game? First, let's see the structure of snake:
* StepRand: Dir@, LFSR@ to LFSR@
  - Adds entropy into the shift register.
  - Could be Dir@ to M@<Unit@>.
  - Called from Move function, which is State@ to either State@ or Dead.
* GetNextFood: LFSR@, List@<Pos@> to Pos@
  - Called from Move function.

Simplest way to substitute things out would be:
* Store Int@ rand as part of State
* Implement StepRand and GetNextFood as generic in M@ with Rand@.
* Add a helper function...

It's odd that GetNextFood doesn't return its updated state.

I wish the Move function was a monad with state?

Anyway, if I want to be surgical about it, I would do:
* GetNextFood - operates on Rand@.
* StepRand - operates on Rand@.
* Helper function: Random(M@<...>, Int@) to pair of Int@ and A@?

Would it make sense to put everything in a monad? Doesn't have to be pure
state. It can be a mix of state and maybe to capture the case of dead.

---

It's tedious. Having to switch from being able to do a raw S@ to S@ function
on the seed to using calling a generic monadic state function. Maybe it's the
right thing to do, but it feels awkward to make this change. Like I'm being
sucked into generic M@ against my will.

---

In the end, the conversion to Snake was tedious but okay. The code doesn't
look too bad. The two things that stand out are it's not so convenient to
inject bits into the random number generator, and we need to add this adapter
layer to go from functions with explicit clear types to monadic style.

Shall we try getting rid of LFSR everywhere else now too? Current uses are:
* Invaders
* SatGame
* Layout game

Seems like it shouldn't be too bad?

Except I have to go through the same tedious exercise.

I understand that in general we shouldn't assume we can explicitly represent
the state of the random number generator. But for my pseudo random number
generators, you definitely can. And for Snake and invaders, etc., I'm assuming
you can.

If we can assume you can explicitly represent the state of the random number
generator, it is tempting to have an API that talks about that explicitly.

Rand@ = <@ A@> { *(S@ state, A@ result); }

Aka: Rand@ = StateM%.Result@;

And now Rand can be:

  <@ A@>(A@, A@) { StateM@<S@, A@>; }

And now running it is as easy as:

  % r = rand(a, b);
  r.state, r.result

Or, if I'm already in the monad:

  Int@ result := do(rand(a, b))
  
Maybe all I need is the helper function to give me the StateM wrapper around a
pseudo random number generator. But I can only do that if I assume there is
some state... but we have that in the constructor.

<<@>@ M@>(State@<RandS@, M@>, Monad@<M@>) { Rand@<M@>; } Rand;

If I have access to that, I should be able to transform that into a StateM
based API for working with random numbers.

But should I? If we do this, it encourages us to hard code pseudo randomness
into our implementations. But isn't it better to force us to support real
randomness too? But that would suggest we need to surface the Rand@ interface
all the way to the top of Snake. It doesn't feel great. It's nice when things
are contained and pure, not having to pass around implicit magic everywhere.

---

For Invaders I ended up implementing my own API wrapper around the interface
which provides a monad for random number generation that I can run. The game
loop is also set up a little weirdly in that we don't have the ability to
route out the updated prng value after using it, so instead of have a separate
place to advance the prng and draw rand values off of it.

There is clearly jank when using Rand48. I want to switch back to LFSR for
this. How about providing an LFSR implementation of Rand@ to start with and
worry about optimizing for performance later?

---

Maybe what I need is a few helper function to make it easier to run some
random number generation on explicit state.

You write your random number generator for abstract monad M@ and Rand@<M@>.
Then, at the top level, you want to run it. I provide a helper that takes the
generic function to run and evals it or execs it as a pure function given an
int seed.

/Core/Rand/Pure% perhaps?

But how do I choose which random number generator I want? We need to pass that
to the Run and Eval functions too, right? And seed it? I kind of wish
everything had a standard Int@ based seeding API.

If we don't mind baking Int@ into the random API, we could change the
primitive to Of and use that to reduce the number of calls to the random
number generator needed. That should improve performance? Or ask for a
specific number of random bits, returning an Int@ in [0, 2^n).

In other words, we want to pass around batches of bits. Int@ is the type we
chose to do that. We can use it for asking for bits, and we can use it for
initializing the state of the random number generator.

Maybe I can have a Prng@ interface that deals directly with S@ and Int@,
without any Monad@. Then have a common adapter for that to the Rand@
interface?

I definitely want to keep the monad interface for describing random generators
though. Which is used in all my cases. What I want is:
* Easy access to a monad for describing random generators.
  for all M@ probably works fine for this.
* Easy implementation of a pseudo random number generator.
  S@, Seed, Of, Influence(?)
* Easy way to run a random generator of my choice.

The last one is the tricky one. I have a custom random generator that works
for all M@. I want to either Run or Exec or Eval. And I want to pick which
random number generator to use, and what the seed should be? Or what the
initial state should be?

Do I want just a function 'Pure' which goes from generator to State@<S@>
associated with whatever rng I pick to use? Then I say things like:

   Eval $ Pure(Foo(x, y), Seed(Int|42));

That seems reasonable? Is it any better than what I've already tried?

---

How about 'feed' for the name of the function that lets you inject some bits
into the prng?

---

Next straw proposal:
* /Core/Rand%
  <<@>@ M@> { *(
    (Int@) { M@<Int@>; } bits,        # Returns n bits of randomness
    (Int@, Int@) { M@<Unit@>; } feed  # Feeds in n bits of randomness
  };
* /Core/Rand/Prng%
  Pure - function that takes a custom generator, a pseudo random number
         generator type and returns a StateM@<S@>.
* /Core/Rand/Foo%
  Provides implementations of Prng@ and a Seed function.

How to get here?
* Update implementations of Rand48 and LFSR to 

Umm.... I don't currently use my Rand48 implementation. Everyone is using LFSR
at the moment. Any reason to keep Rand48 around then? Maybe I should
temporarily remove it just to simplify the transition? Or is it good to have
two different rngs, to make sure the API supports both reasonably well?

I think first step should be implement Prng helper and make use of it. Then
change Rand@ definition under the covers to return n bits of randomness. Then
add the feed option. See where that leaves us.

For both Lcg and Lfsr, we have a general algorithm that works for a general
seed type, but with different embedded parameters. So maybe I want things
like:

  /Core/Rand/LFSR%.LFSR(<params>)
  /Core/Rand/LFSR%.LFSRs.8, 12, 24
  /Core/Rand/LCG%.LCG(<params>)
  /Core/Rand/LCG%.LCGs.Rand48

That kind of thing?

I need to decide on capitalization here.
  Prng, PRNG?
  Lfsr, LFSR?
  Lcg, LCG?

It's nicer to add s to all caps. PRNGs versus Prngs. LFSRs versus Lfsrs. But
we have things like Cli in core library instead of CLI. I feel like Prng,
Lfsr, Lcg is the right way to go long term.

I don't know. It feels weird. Maybe it's better to provide a concrete monadic
type M@ that the generators can be written to use? Like, abstract types
instead of parameterized types?

---

It's certainly annoying having to pass M@, m, r explicitly every time we call
a random generator function. Could we do the trick like we do for Output@?
Compose everything using generic M@, and then instantiate it all at once? Or I
want to be able to mix calls to rand and IO?

Anyway, a bunch of things I'm wrestling with:
* Need to separate LFSR@ parameters from state, so that we can implement a
  seed function for LFSR independent of parameters.
* Migrate satgame over from it's custom Random@ type to Rand@.
* Implement /Core/Bool/Rand%.Rand.
* Implement a nice way to go from generic M@, Rand@ to a state monad.

---

Review of different ways of doing things:

1. Explicit pure state monad.
  @ S@ = ...;
  <@>@ Rand@ = StateM@<S@>;
  Monad<Rand@> Monad = ...

Pros:
* Simple types involved.
* Don't have to pass M@, m, r around everywhere.

Cons:
* Hard codes monad type - harder to compose.
* Hard codes state type S@, and transitively the kind of prng.
* Won't work with true random IO that takes to the outside world
  - Unless we have a hack for S@ to let it be "the world"

2. Abstract hard coded stuff
  Same as (1), except we let different modules provide different hard coded
  sets of things.

Example:
* This is the approach I'm using with Map@ right now.
* This is the approach I use with invaders Prng@.

Pros:
* Simple types involved.
* Don't have to pass M@, m, r around everywhere.
* Can work with different kinds of prng, but have to pick one at a time to
  work with or take as input at a higher level.

Cons:
* Can't mix two different prng impls in the same program for the same custom
  code.

3. StateT approach - generalizes the monad
  @ S@ = ...;
  <<@>@> Rand@ = StateTM@<S@>;
  Monad@<Rand@<M@>> = <<@>@ M@>( ... ) { Monad@<Rand@<M@>>; }

Pros:
* Doesn't hard code monad type: can interleave with other side effects.

Cons:
* Hard codes state type S@, and transitively the kind of prng.
* Won't work with true random IO.
* Complicated use of lift. More complicated types.

4. Hidden forall approach - generalizes state and monad.
  Rand@ = <<@>@ M@>(Monad@<M@>, RandImpl@<M@>) { M@<A@>; }
  Monad@<Rand@> Monad = ...;

Example:
* This is the approach I use for Output@.
* This is what I sort of tried using for Snake random number generation.

Pros:
* Simple types involved.
* Don't have to pass M@, m, r around everywhere.
* Supports any kind of state for the prng.
* Supports any kind of monadic type for the state of the prng.
  - Including true random numbers.
* Don't have to pass around M@, m, or r everywhere.

Cons:
* Doesn't allow interleaving of side effects from different monads.

5. Explicit forall approach
  <<@>@,@>@ Rand@ = <<@>@ M@> { <@ A@>(A@, A@) { M@<A@>; }; };

Example:
* This is the approach I've been trying to implement.

Pros:
* Works with any type of state or monad.
* Allows full interleaving of side effects from different monads.

Cons:
* Complex types. Have to pass around M@, m, r everywhere.
* Tedious to provide top level M@, m, r implementations to run.

So... where does that leave me?

Monad composition still feels unsolved. Even if I use the totally generic M@
approach, for example, how can I add state into cli native M@? I have to use
state monad anyway.

And while it seems great to be totally generic, I'm not convinced it's good to
surface all the side effects to the very top level. For example, Snake and
Invaders use pure rng, they can do that entirely internally. No reason for the
callers of Snake and Invaders to need to know about that internal
implementation detail. Not unless they need to switch to true random numbers,
at which point they need the assistance of the caller.

Which brings me back around to discussion I've had before. If all side effects
can be represented as state transformations and we had a single monad that let
you allocate state, would it not be better to just put everything in that same
monad? aka... IO monad? Or ST or Process or whatever realization of it that
you want.

But we can pass state via function calls as well as via state monad. I suppose
state monad is just function calls at the end of the day. But it can be done
as a reader monad to some extent.

---

Think about this a little more deeply. What are the fundamental underlying
challenges?

1. We build up a library of utilities on top of a few small axioms.
For example: Output@ is all built up on top of putting to a stream. Random@ is
all built up on top of getting some random bits. IO is built up on top of some
monadic interface.

I don't want to hard code the underlying axioms. But it's annoying to have to
pass them around everywhere.

Honestly, I don't think this is the biggest problem I'm facing. It's not that
bad to pass m, n, r, whatever around everywhere.

This is the only problem with writing generic code that operates given a Rand@
interface. That's not the part that's hard for me to write.

What's the next fundamental challenge? It shows up as it being tedious to
provide a pure implementation for whatever random number generator I want to
use. in Snake, RunRand has about a 10 line adaptor function needed, and it
only works if I push m and r to the last arguments (which feels weird).
Invaders is similar, a dozen lines of code just to declare everything to use
LFSR. Layout also has it. Why so much? We need to define:
* M@, S@, m, s, rand, and our Run function.

This is where we are specifying the specific LFSR we want. It's where I'm
saying: I want this LFSR and to use it with this pure state monad.

Why can't I easily factor that out into common code shared by all my games?
It's specific to pure state monad, but generic in rng type so long as the rng
is prng. Note that it's separate from the definition of my custom generator
functions (of which there may be multiple) and separate from the use of my
custom generator functions (of which there may be multiple). Once you define
it, you compose it with the use of the custom generator functions.

Conceptually what I want is:
1. Declaration of the thing: specifies which LFSR I want to use.
 - Provides M@, m, rand, Run functions.
2. Use of the thing: combines with custom generator functions.

If the generator functions are forall M@, then we only need to export the Run
function. Except maybe I want Eval and Exec too. Alternatively we could write
the custom generator functions to use M@, m, rand directly from here. Doesn't
that make a lot of sense? We can import and supply the higher level functions
we want too, to avoid passing around m and rand directly everywhere.

It's like, take whatever you were going to import for doing the random number
stuff, and import it under the context of your chosen random number generator.
Then refer to those modified imports instead.

The other end of approaches is to write the custom generators using totally
abstract functions. But I don't need that. I know I want them to use a
particular consistent value.

In other words? My Invaders approach seems the best. Import and set things up
using our random prng monad. Import whatever you need under a Rand namespace,
for example. Then do Rand.M@, or RandM@? Except, that's more verbose than M@
and m. Which is why I prefer not to do things this way. The namespace aspect.
If you take M@ as a parameter, it makes total sense what you are referring to
locally when you say M@. If you want to use a hard coded value, suddenly you
need to be more clear that you want the Rand hard coded value. Which might be
fine if you define all the random functions in the same block as the random
stuff? Then we can use M@ and m?

Anyway, the common code I want to factor out is:
* I give you the Rand function, which is forall M@, State@<S@, M@>, Monad@<@>
  returning a Rand@<M@>.
* You give me: A concrete M@, m, r, Pure functions to turn any custom
  generator function into a StateM monad which I can then Run, Eval, Exec as I
  like.

There are three ways we have discussed to combine this with your custom
generator functions:
1. Require the generator functions to take forall M@ as last args / return
type.
2. Have the generator functions take M@, m, r directly. No need for 'Pure'
function to convert.
3. Have the generator functions take M@, m, r as first arguments. To combine:
apply the hard coded M@, m, r that you already have.

(1) let's me not have to worry about the hard coded M@, m, or r at composition
time. (1) and (3) let me not worry about them at custom function declaration
time. (2) let's me use the generator functions directly without extra logic
needed at use time (aside from Run, Eval, Exec...).

Which is best approach to use? (1), (2), or (3)?

---

I know (1) can work. It just feels weird. It doesn't allow us to write the
custom generator functions to capture the monad and rand arguments
statically.

For example, something like:

  % Prng = /Core/Rand/Prng%.Prng(/Core/Rand/Rng%.Rand);
  (Dir@)<<@>@ M@>(Monad@<M@>, Rand@<M@>) { M@<Unit@>; } StepRand = ...

  Prng.S@ rand = Exec $ Prng.Pure(StepRand(s.dir), s.rand);

I also know (2) can work. I was going to say it's not flexible to having
Rand@<M@> passed in, but if we have that, we don't need to worry about this
anyway. So maybe no downside? It's something like:

  % Prng = /Core/Rand/Prng%.Prng(/Core/Rand/Rng%.Rand);

  (Dir@) { Prng.M@<Unit@>; }
  StepRand = {
    <@>@ M@ = Prng.M@;
    Monad@<M@> m = Prng.m;
    Rand@<M@> r = Prng.r;

    (Dir@ d) { 
      ...
    };
  };

  Prng.S@ rand = Exec $ StepRand(s.dir);

It's nice we don't need an extra function call wrapper around StepRand where
it is used. That feels very good. Also, that we can capture m, r as statics in
the function returned by StepRand. The only downside is having to refer to
Prng.M@ in the type for StepRand, and having to 'import' M@, m, r in the body.

How would (3) work? I don't think I've tried this. I guess something like:

  % Prng = /Core/Rand/Prng%.Prng(/Core/Rand/Rng%.Rand);

  <<@>@ M@>(Monad@<M@>, Rand@<M@>, Dir@) { M@<Unit@>; } StepRand = ...

  Prng.S@ rand = Exec $ StepRand<Prng.M@>(Prng.m, Prng.rand, s.dir);

I don't like it as much. So, honestly, I'm a little surprised to say, (2)
looks the best to me.

Can I try it? Let's try it. See how it feels to migrate things over to it.

For Snake, (2) isn't bad. Minor annoyances:
* Having to import M@, m, r in every custom generator.
* Having to pass M@, m, r to functions like Range.
 - But we have to do that regardless. That's how Range works.

To go the next level though, I should factor out the common code for the Prng
module declaration. Which means defining some Prng@ type that let's us turn a
State@ into a Rand@, a state type S@, and a Seed function to go from Int@ to
that type. Let's make this the next step. And then I'm thinking I definitely
want to revamp the Rand@ type to work on Int@, or some other list of bits
representation. Like... List@<Bool@>? The nice thing about List@<Bool@> (which
we can rebrand Bit@ = Bool@; Bits@ = List@<Bit@>) is it captures exactly how
many bits of state you are getting. We don't have that for Int@.

