
<@>@ Show@ = /Core/Show%.Show@;

% Format = /Core/Char/Ascii/Show%.Format;

@ Loc@ = /Fbld/Loc%.Loc@;

Show@<Loc@> Show = (Loc@ x) {
  Format|'%s:%s:%s'[
    /Core/Show%.String(x.file),
    /Core/Int/Show%.Show(x.line),
    /Core/Int/Show%.Show(x.col)];
};

@(Show);
