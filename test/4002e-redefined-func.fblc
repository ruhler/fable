

struct Unit();
union Fruit(Unit apple, Unit banana);

func f(Fruit x ; Fruit) {
  x;
};

func f(Fruit x ; Fruit) {
  ?(x ; Fruit:banana(Unit()), Fruit:apple(Unit()));
};

/// Expect: ERROR
func main( ; Fruit) {
  f(Fruit:apple(Unit()));
};
