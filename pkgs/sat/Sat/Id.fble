
@ Int@ = /Core/Int%.Int@;

@ Id@ = Int@;
% EqId = /Core/Int/Eq%.Eq;
% OrdId = /Core/Int/Ord%.Ord;

% Map = /Core/Map/Tree%.Map(OrdId);
% Set = /Core/Set%.Set<Map.Map@, Id@>(Map);

@(Id@, EqId, Map, Set);
