
@ Char@ = /Char%.Char@;

<@>@ S@ = /List%.S@;
<@>% S = /List%.S;

<@,@>@ Lit@ = /Literal%.Lit@;

@ Str@ = S@<Char@>;
<@>@ CharLit@ = Lit@<Char@>;

(CharLit@) { S@<Char@>; } Str = (CharLit@ lit) {
  S<Char@>(lit);
};

@(Str@, CharLit@, Str);
