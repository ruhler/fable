

Char@ .= /Std/Char%;
Stream@ .= /Std/Stream%;
Concat .= /Std/Stream/List%;

ShowBit = /Bit/Show%.Show;

Bit4@ = /Bit4%.Bit4@;

(Bit4@) { Stream@<Char@>; } Show = (Bit4@ a) {
  Concat[ShowBit(a.3), ShowBit(a.2), ShowBit(a.1), ShowBit(a.0)];
};

@(Show);
