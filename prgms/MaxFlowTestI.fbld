mtype MaxFlowTestI {
  using UnitM { Unit; };
  using UBNatM { Nat; };
  using ListM { S; };
 
  union TestName(Unit Basic);
  struct TestFailure(TestName name, Nat a, Nat b);

  func Test( ; S<TestFailure>);
};
