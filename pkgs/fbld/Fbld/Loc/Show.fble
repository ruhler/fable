
<@>@ Show@ = /Core/Show%.Show@;

% Format = /Core/Char/Ascii/Output%.Format;

@ Loc@ = /Fbld/Loc%.Loc@;

Show@<Loc@> Show = (Loc@ x) {
  Format|'%s:%s:%s'[
    /Core/Char/Output%.String(x.file),
    /Core/Int/Show%.Show(x.line),
    /Core/Int/Show%.Show(x.col)];
};

@(Show);
