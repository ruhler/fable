
@ Char@ = /Core/Char%.Char@;
<@>@ Show@ = /Show%.Show@;
% Str = /Core/String%.Str;

# Show --
#   Implementation of Show@ for Char@. See documentation of /Show%.Show@.
Show@<Char@> Show = (Char@ x) {
  Str[x];
};

@(Show);
