Show@
=====

Show@ class is wanted for unit testing: be able to print out the value of a
data type in human readable format, so you can tell in a test error message
what went wrong.

Typical use is for testing and debugging. You could see it used for
interacting with humans, for example in a tictactoe game.

The simplest approach is just (A@) { String@; }. The concern with that
approach is performance. If you have a bunch of Show instances implemented as
concatenation of show instances, this leads to lots of intermediate strings
and copies. If, on the other hand, you can write it in such a way that it
prepends to an existing string, you can avoid all those intermediate strings
and copies. It is significant from a complexity point of view.

My goals:
* Implementation of Show should feel natural.
* Avoid quadratic blowup for showing large composite objects.

How do we use the result of Show? Maybe we don't need to produce a string at
all. What if we are going directly to an output stream?

Sounds tricky to me. We might want to build from the middle (current simple
approach), build from the back (to produce efficient string), or build from
the front (to produce efficient output stream).

What does Haskell do?
* show :: a -> String
* showsPrec :: Int -> a -> ShowS
* showList :: [a] -> ShowS

Where ShowS is String -> String, where the meaning is prepend your show output
to the given string.

Haskell Show has a convention of it producing valid Haskell code. I don't care
about that.

Note that 'Format' strings are a convenient way to implement Show. If I need
some special type for Show, I should have Format support that special type
too.

Imagine we are showing a pair, for example:

  Format|'a: %s, b: %s'[show(a), show(b)]

It's all sequencing:

  'a: ', show(a), ', b: ', show(b)

I would like to describe it as sequencing and using Format command. Sequencing
shouldn't care too much about order?

We could have Show produce an intermediate value that explicitly represents
the sequence:

  Show = <@ A@> { +(String@ str, List@<Show@> seq); };

That way I can decide if I want to traverse forward or reverse. For example:

  Print = (Show@ x) {
    x.?(str: Print(str));
    x.?(seq: ForEach(x.seq, Print));
  }

  ToString = (Show@ x, String@ tail) {
    x.?(str: Append(str, tail));
    x.?(seq: ...
  }

String to String function means allocating intermediate function values. Or,
using Show definition above, intermediate Show values. That's unfortunate.
Either way, it would be nice if it was real easy to produce a String for show?
But why? The only place we ever use the final strings could be in output
streams, for example.

---

Abstractly the API I want for implementing show is:
* output a string
* show one thing followed by another thing

Best represented as an unbalanced binary tree.

If we used String to String functions, it would look like:

* outputStr = (String@ str) { (String@ tail) {
   Append(str, tail);
  }
* AppendShow = (Show@ a, Show@ b) {
    (String@ tail) {
       a(b(tail));
    }

If we have tail to start, we don't need intermediate functions to be
allocated. If we don't, this turns back into a binary tree, except less
flexible because we are forced to construct a string with it. We can't output
to a stream, for example.

In practical use, I would expect code like:

  PutLine(Show(x));

Here we are calling show without the tail to start, then passing the function
result to PutLine. I may as well just build the binary tree.

---

On the other hand, binary tree is an arbitrary structure mostly related to
ease of construction of the shown output. Assuming PutStr() does atomic string
output, that could lead to weird breaks in output conceptually. Unless we say
PutShow(...) is atomic?

If we've already allocated the intermediate substrings, it seems fine to save
and reuse those rather than pre-compose into one big string.

I guess where I'm going is that we have two types: a binary tree and an
interface to produce a binary tree for your type. We can have top level
functions to convert a binary tree to a string (not sure when you would use
that) and output to a stream (seems the most likely use case).

We should have a variation on Format that takes and produces the binary trees.
Although in that case it's not quite as efficient because we have to break up
the format string into separate substrings.

Or, we could go directly to output stream, and implement a string output
stream for that case?

---

Let's imagine we were defining an output stream from scratch. What do we want?

<@>@ OStream@ = <@ S@> { *(
  @ S@,
  (String@, S@) { S@; } str,
  (S@, S@) { S@; } seq
); };
  
The string implementation is:
  S@ = String@
  str = Append,
  seq = Append

Is that the efficient version? No, because seq(seq(show(x), show(y)), show(z))
builds the intermediate string for xy that it then throws away when forming
xyz.

The string builder implementation is:

  S@ = (String@) { String@; }
  str = (String@ s, S@ f, String@ t) { Append(s, f(t)); }
  seq = (S@ a, S@ b, String@ t) { a(b(t)); }

That way we can't build xy until we've already passed it z.

But I would like to know, honestly, when do I use Show for anything other than
outputting to an output stream today, not including the recursive calls? Debug
trace, which could be redone as an output stream.

I bet we don't do it ever. We never do anything with the shown string except
show it to the user, via output stream or debug trace.

That doesn't necessarily make things any easier, because output stream is
abstract in its type to allow for, e.g. playing a game versus testing a game.

---

If we are going to have a special write-only output stream that we use instead
of strings, should we also have a special read-only input stream? All the
existing string functions for searching in strings could be applied to an
input stream, right?

I feel like I could get stuck in a deep well going around in circles for a
long time. What can we reasonably do with Show?

A. String@
B. (String@) { String@; }
C. <@ S@>(OStream@<S@>) { S@; }

A doesn't compose well naturally. B composes well for producing a string, but
not sending output. C has the overhead of being polymorphic, but it composes
well for any use case you could imagine.

Shall I try out (C) and see how it goes? It's certainly different from what
I'm used to. How would I approach making this change in the first place?

Now we'll have three types we need to come up with names for:
1. S@ - actually, we can just use S@ as a type parameter.
2. Show@<T@> - interface for types T@ implementing Show@.
3. ShowStream@<S@> - interface for types S@ implementing stream?
   Stream@<S@>
   OStream@<S@>
   StrString@<S@>
   StrBuilder@<S@>
   StrOut@<S@>
   OutStr@<S@>
   Stream@<S@>
   Out@<S@>

It's also worth thinking about io. Because whatever we pick for output stream
type here will be similar to io output stream.

Let's pencil in approach (C) for now, but spend more time thinking about io
and unit testing framework before going ahead, in case there's anything
obvious we can share.

---

Thinking more about io, it seems like a key question is whether we want Show
to be an atomic thing or a causal time oriented thing. You can think 'Format'
versus 'PrintF'. As String@, it is definitely the atomic thing. When I
interleave Show calls at the app level in practice, it is the interleaved
thing.

Maybe it's useful to distinguish between the two? But why? Why not always
allow the interleaved view? Isn't that more general?

I think it's just the performance cost: we can't go directly to String@ if we
use the interleaved view, we would need to use a state monad. But we may as
well do that unless we rather pay the quadratic blowup cost of direct
concatenation of strings? Not clear. We could using (String@) { String@; } to
avoid the quadratic blowup, but we can't if we go the interleaved approach.
Interleaved would have to be... (T@, String@) { (T@, String@); } (aka state
monad).

When I think of Show, I don't think of it getting interleaved between a bunch
of other things. So, maybe we want to be more explicit: Show produces the
shown thing, you have some separate atomic operation in a higher level monad
called Print which can print the thing. In which case, let's just use a binary
tree of strings, right? A string builder, that you have to explicitly
collapse via a Print call.

For 'Format' function, maybe it makes sense to do the tree via lists. So, for
example:

@ Shown@ = +(String@ str, List@<Shown@> seq);

And at any point, we could have a function: Flatten which takes Shown@ and
flattens it into a single str value. If we wanted.

Print can be (Shown@) { M@<Unit@>; } for whatever monad you want. Or it could
be (Shown@) { String@; } to go straight to a string.

I think I should seriously consider this type as the type to use for Show@.

But! This has another performance issue. Let's review.

1. String@ - quadratic blowup from composition.
2. Shown@ from above - you have to print everything completely (to Shown@
value) before you output anything. Not good for, e.g. pprof, where we want to
output a really long value for a list of profile stack frames.
3. Monadic or function based with interleaving - you don't even have to build
up the end of what you want to show until you've finished outputting the
beginning.

So honestly, if I care about performance, shouldn't I use a monadic interface?
Isn't that better than making the user pick between abcd vs. (ab, cd) vs. (a,
b, c, d)? And, that would naturally fit into the io stream type. As in, we say
Show@ just means you can write the value to an output stream. Easy. One
concept. No such thing as atomicity, but who cares? Atomicity doesn't mean
anything in practice anyway. Use a String@ state monad, print to that, then
print out that String@ if you want 'atomicity' (though still no guarantee
about anything anywhere about when the data will be observed). Not unless we
are talking about streams of strings instead of streams of characters and
someone is counting. But when I think of Show, I don't think of it being as
breaking up something into sub strings. I think of it as a raw sequence of
characters that should be taken as a whole, not split up into arbitrary bits
(like the ones we tend to make in practice when producing them).

Sounds reasonable to me. My vote at the moment: implement Streams first. Then
say Show@<T@> is a function to output an element of type T@ to a Char@ stream.

---

I have a Put@<T@, M@> type now. How do we practically use it with Show@? It
seems pretty inconvenient to pass around the output stream and monad
everywhere. Does that mean we have to defer those to the last, build up a tree
of functions?

<@>@ Show@ = <@ T@, <@>@ M@>(Monad@<M@>, Put@<Char@, M@>) { M@<Unit@>; }

PutLine = (String@ str)<<@>@ M@>(Monad@<M@> m, Put@<Char@, M@> out) {
  ...
};

Maybe we want a type:

 @ Print@ = <<@>@ M@>(Monad@<M@>, Put@<Char@, M@>) { M@<Unit@>; }

So that makes:

 <@>@ Show@ = <@ T@>(T@) { Print@; }

Now all our composition happens at the function level, without the monad or
put instance being mentioned anywhere? But how do we sequence that with other
things? You can't. We are purposefully restricting the Print@ interface so all
you can do is print. If you want to sequence it, you have to do that at a
higher level. Show@ means it works for all monads. So it can't special case
anything for a specific monad.

And I can implement Format function internally, taking Print arguments. Maybe
%p instead of %s?

The implementation of Format will pass that monad and output args through.

And then, at the top level, we have a print function... well, the thing itself
is the print function. So you can interleave that way.

Sounds reasonable ish. But is there not a performance concern here? That we
have to build up a tree of functions before we can start outputting things? Or
will it only be a shallow tree?

Imagine I have a complex data type, like a map. If we call Show on that, are
we strictly evaluating the whole thing into a big tree of functions, or are we
holding on to the original object until we've output the result? I should walk
through this by hand to better understand. Imagine a list of objects, walk
through that. See how it scales to do a print of the list then output to IO.
See if it builds up a huge function, or just starts unwrapping one element at
a time.

---

Here are some different ways we can do things, as I see it:
1. Create a string right away. Current Show@ interface.
2. Create a binary tree, traversing full object at the start. Render that tree
   to IO or String@ on demand.
3. Create a binary tree of thunks, traversing full object at the start. Same
as (2), just using function values instead of explicit binary tree.
4. Lazily construct output. Don't traverse full object at the start.

It sounds like I want (4). If that's the case, though, then my type for Show@
(or Print@, or whatever I want to call it) should be a function that I call at
time of rendering.

Question: is taking Put and Monad arguments sufficient, or I need to take an
extra Unit@ argument?

  Print@ = (PutChar@<M@>, Monad@<M@>) { M@<Unit@>; }

Imagine a long list.

  PrintList = (List@ xs, PutChar@<M@>, Monad@<M@>) {
    xs.?(empty: ...);
    Unit@ _ <- Print(xs.items.head);
    Print(xs.items.tail);
  }

This is fine, because it uses monadic bind. But that's different from:

  PrintPair = (Pair@ p) {
    Format|'(%s, %s)'[Print(p.a), Print(p.b)]
  };

Because I call the function right away, which calls Print(p.a) and Print(p.b)
right away? I'm confused. How is Format implemented? Assume it's just Seq for
simplicity.

  Seq = (Print@ a, Print@ b, PutChar@<M@> p, Monad@<M@> m) {
    Unit@ _ <- a(p, m);
    b(p, m);
  };

That means the 'Seq' function isn't evaluated unti lyou provide the PutChar
and Monad values. So if we had
    
  PrintList = (List@ xs) {
    xs.?(empty: ...);
    Seq(Print(xs.items.head), PrintList(xs.items.tail));
  }

It builds up a list, proportional to the input list, of:

  Seq(Print(x0), Seq(Print(x1, Seq(Print(x2, ...)))));

What I want is to not be able to call PrintList recursively until after the
first Print function has been called. Aka, the implementation we had before:

  PrintList = (List@ xs, PutChar@<M@>, Monad@<M@>) {
    xs.?(empty: ...);
    Unit@ _ <- Print(xs.items.head);
    Print(xs.items.tail);
  }

Which means I don't think we can have a function that we can use like:

    Format|'...'[Show(a), Show(b)]

Because it doesn't express the sequencing requirement between Show(a) and
Show(b) in the data flow graph.

Slight tangent: is there any clever way we can make a function that returns a
function in a recursive way so we can do varargs with different type args?

Well... we could return a function of its own type, but I don't know how we
would finish the recursion if you keep getting a function back. Unless what
you get back depends on what you put in?

If you are going to be lazy in evaluating a function, you have to see that
behind a function call:
  
    Format|'...'[(Unit@ _) { Show(a); }, (Unit@ _) { Show(b); }]

Unless we have two different functions: you implement Show, you call ShowL?
Eager and deferred. You implement Eager. You use deferred everywhere. Then it
can work. Format takes a list of deferred, and results in an eager.

---

Use of (Unit@ _) { ... } feels slightly heavyweight for a syntax. We can't
abstract this behind a function call like L(...), because the arguments to
functions are evaluated before the function is called. Does it make sense to
provide a special syntax to facilitate this case? For example.

   Format|'...'[<{Show(a)}, <{Show(b)}]

The other use case we have today for this is unit tests

  Test(Str|'RunTest.pass_passes', (Unit@ _) {
    MainResult@ result = RunMain(Run(pass), Empty<String@>, Empty<Int@>);

    Bool@ wnt = True;
    Bool@ got = result.exit;
    AssertEquals(/Std/Bool/Eq%.Eq, /Core/Bool/Show%.Show, wnt, got);
  }),

Becomes:

  Test(Str|'RunTest.pass_passes', <{
    MainResult@ result = RunMain(Run(pass), Empty<String@>, Empty<Int@>);

    Bool@ wnt = True;
    Bool@ got = result.exit;
    AssertEquals(/Std/Bool/Eq%.Eq, /Core/Bool/Show%.Show, wnt, got);
  }),

Honestly, I don't think it helps in this case, because the function is so big
to begin with. It's already using statement form. A special syntax would
mostly be useful for simple expressions.

   Format|'...'[*{Show(a)}, *{Show(b)}]
   Format|'...'[(Unit@ _) {Show(a)}, (Unit@ _) {Show(b)}]

It's pretty verbose this way. Either way, to be honest. Could we put it into
the monad somehow?

  Unit@ _ <- m.do(Format|'...');
  Unit@ _ <- m.do(Show(a));
  Unit@ _ <- m.do(Show(b));
  m.return(Unit);

That's pretty tedious too.

The other option would be:

Show = (...) { 
  Format|'...'[ShowL(a), ShowL(b)];
};

ShowL = (T@ x, Unit@ _) { Show(x); };

It's a pain to have to define both Show and ShowL everywhere. Do we have to
though? Maybe not? How about always defining the lazy one?

Show = (Pair@ p, Unit@ _) {
  Format|'(%s, %s)'[Show(p.a), Show(p.b)];
};

Careful. If this was a list instead, we have to write it differently.

This is wrong:

Show = (List@ l, Unit@ _) {
  Format|'%s:%s'[Show(l.items.head), Show(l.items.tail)];
};

We need to instead write, awkwardly, or maybe:

Show = (List@ l, Unit@ _) {
  Shown@ a <- Show(l.items.head);
  Shown@ b <- Show(l.items.tail);
  Format|'%s:%s'[a, b];
};

That might work? But we are missing sequencing.

Show = (List@ l, Unit@ _) {
  Shown@ a <- m.do(Show(l.items.head));
  Shown@ b <- m.do(Show(l.items.tail));
  m.return(Format|'%s:%s'[a, b]);
};

Now monad and sequencing is explicit. What is this type Shown@? Where does m
come from and get passed to?

Show = (List@ l, Monad@<M@> m, Put@<Char@, M@> out) {
  Shown@ a <- m.do(Show(l.items.head, m, out));
  Shown@ b <- m.do(Show(l.items.tail, m, out));
  m.return(Format(m, out)|'%s:%s'[a, b]);
};

That doesn't really make sense.

Here's an interesting idea for varargs:

  Format|'(%s, %s)'<A@, B@>(a, b);

No. I doubt that would work.

The most basic approach with sequencing is quite tedious:

Show = (List@ l, Monad@<M@> m, Put@<Char@, M@> out) {
  Unit@ _ <- m.do(Show(l.items.head, m, out));
  Unit@ _ <- m.do(PutStr(m, out)|':');
  Unit@ _ <- m.do(Show(l.items.tail, m, out));
  m.return(Unit);
};

Compared to:

  Format|'%s:%s'[Show(l.items.head), Show(l.items.tail)];

---

New idea: iterators.

I claim we can return exactly what we want for show as a data structure which
looks very much like an iterator:

  I@ = M@< +(*(T@ next, I@ tail) items, Unit@ empty) >;

You invoke monadic sequencing to access the next item (or end).

For example, say M@<A@> = (Unit@) { A@; }, and T@ is String@:

  I@ = (Unit@) { +(*(String@ next, I@ tail) items, Unit@ empty); };

Show = (List@ l, Unit@ _) {
  l.empty.?(R@(empty: Unit));
  R@(items: @(next: ...

Anyway, you can imagine having a library function to take two iterators and
produce one that goes through sequentially:

  (I@, I@) { I@; } Seq = (I@ a, I@ b) {
    (Unit@ _) {
      R@ r = a(Unit);
      r.?(empty: b(Unit));
      r.@(tail: Seq(tail, b));
  };

Show = (List@ l, Unit@ _) {
  l.empty.?(R@(empty: Unit));
  Seqs[Show(l.items.head), ShowStr|':', Show(l.items.tail)];
};

Notice that the recursive applications to Show are partial, so they will
capture the input argument without executing the body of the show. That's how
you avoid looking at the end of the list until you've already done something
useful with the front.

We get different things depending on choice of M@ here:
* Pure@ - A straight up list of T@.
* (Unit@) { A@; } - A lazy list of T@, without intervening side effects.
* M@<A@> - A generic list of T@, which can be interleaved with side effects.

The general monadic sequencing would look like:

  (I@, I@) { I@; } Seq = (I@ a, I@ b) {
    R@ r <- m.do(a);
    r.?(empty: b);
    m.return(r.@(tail: Seq(r.tail, b)));
  };

For pure, that would turn into?

   a.?(empty: b);
   a.@(tail: Seq(a.tail, b));

Which is exactly what append looks like. It's your quadratic blowup for
string@. Especially of T@ was Char@ instead of String@. Then we have literally
the String@ type.

Hey! This looks exactly like /Md5/Stream%.

If I chose the names right, we can define an iterator type such that
I@<Pure@, T@> is equal to List@<T@>. That would be pretty cool, honestly.

And then we say what? That Show@ takes an element of type T@, and returns an
iterator of Char@, but in what monad? For Show@, I want it to be... String@,
or (Unit@ ) { A@; }. So maybe we say it's for any monad you might provide to
it. That forces it to be pure.

Next question: how does Format look with streams? I bet it works just fine,
exactly the way I want. You give it a list of streams, it returns a stream.
And, we could reuse it for String@ if M@ is Pure@.

We just need to be careful about how we implement Show.

This is not the right implementation of Show for a list, because it traverses
the entire list up front:

Show = (List@ l) {
  l.empty.?(Format|''[]);
  Format|'%s:%s'[Show(l.items.head), Show(l.items.tail)];
};

Instead it should be implemented as... ???

And we're back around the loop to where we started.

But maybe it's like this, which doesn't look too bad:

Show = (List@ l, Monad@ m) {
  l.empty.?(Format|''[](m));
  Format|'%s:%s'[Show(l.items.head), Show(l.items.tail)](m);
};

The key point being that we pass m through Format instead of through the
recursive Show call. So that the recursive call is provided too few arguments,
and thus becomes a partial application which isn't evaluated until the format
function internally supplies the second arg, which will only be after it has
already output the front of the list.

I can deal with this I think.

---

Stepping way back. I original had show return a String@. We said that's bad
because it has quadratic blowup and means you can't output anything until
you've prepared everything for output. An output stream interface would allow
you to address both of those, but it makes the interface for Show that much
more complicated, which is unpleasant. Do I want to take the leap? Is there
any way to get the best of both worlds?

If it weren't for performance, the answer would be easy: output to String@.
That's what we want to write. That's what we want to work with.

---

Maybe I can abstract things in a different way. If we want to think about
things functionally, then an output stream should be represented as a sequence
of elements. I just want to keep the data structure abstract so we can pick
and choose what data structure is most appropriate given our performance
requirements.

Let's imagine some abstract type S@ for the stream. I want the following
operations:

  S@ Empty;                 # indicates end of output stream.
  (T@, S@) { S@; } Items    # adds something to the front of the stream.

Then we can have:

  Show@ = <@ T@>(T@) { S@; }

In other words, exactly like String@, except I'm making an abstract type S@
and providing in ways I haven't specified yet, different functions for Empty
and Items.

Next step is to show we could pick a type S@ and functions 'Empty' and 'Items'
that allow us to output the front of the list before showing the tail of the
list. Except, I know I can't do that without some intervention in the code
doing the construction, right? Maybe?

  ShowList = (List@ xs) {
    xs.?(empty: Empty);
    ...

And now I don't have a way to compose multiple sequences, which is what I
really want. Let's be honest, the interface I really want for show is string +
concat based, not char + prepend based.

  (String@) { S@; } PutStr
  (S@, (Unit@) { S@; }) { S@; } Seq

  ShowList = (List@ xs) {
    xs.?(empty: PutStr(""));
    Unit@ _ <- Seq(ShowHead(xs.items.head));
    ShowList(xs.items.tail);
  };

  ShowList = (List@ xs) {
    xs.?(empty: PutStr(""));
    Seq(ShowHead(xs.items.head), ShowList(xs.items.tail));
  };

We need to have the Unit@ argument somewhere to prevent unrolling the whole
thing. Either it belongs with the ShowList function, or with the Seq function.

  ShowList = (List@ xs, Unit@ u) {
    xs.?(empty: PutStr(""));
    Seq(ShowHead(xs.items.head), ShowList(xs.items.tail), u);
  };

Putting it with the Seq function feels much more natural to me. I may as well
call it a 'do' function?

Generic output stream:

  S@ End
  (T@) { S@; } Put
  (S@, (Unit@) { S@; }) { S@; } Do

Now the annoying thing is how to pass the functions Put and Do, and the
generic type S@ everywhere. But I already know how to do that?

  Putter@ = <@ T@, @ S@> { *(
    S@ end,
    (T@) { S@; } put,
    (S@, (Unit@) { S@; }) { S@ } do
  ); };

  Shown@ = <@ S@>(Putter@<Char@, S@>) { S@; };
  
  Show@ = <@ T@, @ S@>(Shown@<S@>);
  
  Shown@ End = ...;
  (Char@) { Shown@; } Put = ...;
  (Shown@, (Unit@) { Shown@; }) { Shown@; } Do = ...;

  (String@) { Shown@; } PutStr = (String@ s) {
    s.?(empty: End);
    Unit@ _ <- Do(Put(s.items.head));
    PutStr(s.items.tail);
  };

I claim that's it. The simplest interface we can have, that lets us do
whatever we want. You want string? Easy:
  End = Str|'';
  Put(c) = List[c]
  Do(a, fb) = Append(a, fb(Unit))
  
You want function from string to string?
  End = \s -> s
  Put(c) = \s -> c:s
  Do(a, fb) = \s -> a(fb(Unit, s))

You want output stream?
  End = m.return(Unit)
  Put(c) = put(c)
  Do(a, fb) = bind(a, fb)

You can apply as and when you want. Easy to write. Easy to adapt. Not monadic,
but still takes advantage of the bind syntax. The only concern left would be
performance, right?

How about we try this and see how it goes?

---

Revisiting how I want to implement my show functions. The answer is via a
Format function. But I want it to evaluate arguments lazily. Should we have a
convention that you can implement ShowL and Show, one is lazy, the other
eager, and have Format take a list of ShowL and return a Show?

The show interface is still a bit up in the air, but I'm hoping to land on:

Oh. bummer. I still don't know where I want to land. Arg first or monad and
put interface first? :(

Let's look more from the Show point of view. Maybe it will give a better
perspective than the OStream point of view. Or at least a different one.

Ideal world, I could write as if it was pure strings, which I would write
like:

  Show@<Pair@> ShowPair = (Pair@ p) {
    Format|'(%s, %s)'[ShowA(p.a), ShowB(p.b)];
  };

  Show@<List@> ShowList = (List@ xs) {
    xs.?(empty: Empty);
    Format|'%s, %s'[ShowX(xs.items.head), ShowList(x.items.tail)];
  };

This has a problem of traversing the whole list before showing any part of it.
Probably we shouldn't traverse the second element of the pair before finishing
the first element. So, regardless of types, as soon as I want to be lazy, I
need to modify how I write the code. I suppose best would be something like...
    
  ShowL@<Pair@> ShowPairL = (Pair@ p, Unit@ u) {
    Format|'(%s, %s)'[ShowAL(p.a), ShowBL(p.b)](u);
  };

  Show@<Pair@> ShowPair = (Pair@ p) {
    ShowPairL(p, Unit);
  };

  ...

But if we do that, we always end up using the lazy version. So why not just
define the lazy version?

  Show@<Pair@> ShowPair = (Pair@ p, Unit@ l) {
    Format|'(%s, %s)'[ShowA(p.a), ShowB(p.b)](l);
  };

  Show@<List@> ShowList = (List@ xs, Unit@ l) {
    xs.?(empty: Empty);
    Format|'%s, %s'[ShowX(xs.items.head), ShowList(x.items.tail)](l);
  };

Key is that Format takes a list of the lazy version, but returns the non-lazy
version. I suppose you could, if you want, use Format in a non-lazy way?

  Show@<Pair@> ShowPair = (Pair@ p) {
    Format|'(%s, %s)'[ShowA(p.a), ShowB(p.b)];
  };

Which is kind of cool, to be honest. The types still work out. So you can
chose to be lazy or not. If you do list, you probably ought to be lazy. But
otherwise it doesn't matter.

Now, this tells us nothing about the actual interface we use for showing
things. All we know i s we have 'Empty' and 'Format' as our primitive
constructs. Empty could be implemented as Format|''[]. So maybe all we really
need is a primitive Format function that we can construct everything else out
of? Imagine some abstract type FormatM@. Uh, let's be explicit. FormatM@ and
FormatML@:

  @ FormatM@ = ...
  @ FormatML@ = (Unit@) { FormatM@; };
  <@>@ Show@ = <@ T@>(T@) { FormatML@; };
  (String@, List@<FormatML@>) { FormatML@; } Format = ...;

Now FormatM@ can be whatever I want. Including:
* String@
* (String@) { String@; }
* <<@>@ M@>(Monad@<M@>, Put@<Char@, M@>) { M@<Unit@>; }

But this only works if I provide the argument to show before the monad and the
put interface.

Thus, as far as I can tell, from the Show@ perspective, I really rather not
have to know anything about M@. Use Unit@ to make things lazy, not M@,
Monad@<@>, or Put@<Char@, M@>.

Cool? This gives me a nice way to implement show, that works efficiently and
can be reused directly everywhere I want, where I can chose easily at
implementation time if I want to traverse everything before showing anything
or if I want to do things incrementally.

Question: Can I do something fancy with Format and bind syntax?

  Show@<Pair@> ShowPair = (Pair@ p) {
    Formatted@ a <- ShowA(p.a);
    Formatted@ b <- ShowB(p.b);
    Format|'(%s, %s)'[a, b];
  };

But that's the wrong order. It would need to be more like:

  Show@<Pair@> ShowPair = (Pair@ p) {
    Unit@ _ <- Format|'(%s, %s)';
    Unit@ _ <- ShowA(p.a);
    Unit@ _ <- ShowB(p.b);
  };

Well, we could certainly do puts:

  Show@<Pair@> ShowPair = (Pair@ p) {
    Unit@ _ <- Put|'(';
    Unit@ _ <- ShowA(p.a);
    Unit@ _ <- Put|', ';
    Unit@ _ <- ShowB(p.b);
    Put|')';
  };

But that defeats the point of the Format function, which I would really love
to have.

  Show@<Pair@> ShowPair = (Pair@ p) {
    F@<A@> a <- Format|'(%s, %s)';
    Unit@ _ <- a(p.a);
    Unit@ _ <- b(p.b);
  };

No. I think what I had before was fine. What we need to pass Format is the
function to call to render each argument. Which is what FormatML@ gives me.

Here's what I want to be able to write:

  @ FormatM@ = ...
  @ FormatML@ = (Unit@) { FormatM@; };
  <@>@ Show@ = <@ T@>(T@) { FormatML@; };
  (String@, List@<FormatML@>) { FormatML@; } Format = ...;

  Show@<Pair@> ShowPair = (Pair@ p) {
    Format|'(%s, %s)'[ShowA(p.a), ShowB(p.b)];
  };

  Show@<List@> ShowList = (List@ xs, Unit@ l) {
    xs.?(empty: Format|''[](l));
    Format|'%s, %s'[ShowX(xs.items.head), ShowList(x.items.tail)](l);
  };

Let's go back to stdlib.io.txt discussion with this in mind: Show@ doesn't
want to know anything at all about M@, Monad@<M@>, or Put@<M@>. So there!

Wait. Then we can do that easily already, no? We can make it generic to any
type S@. If Format is the building block, then may as well output individual
characters at a time? Format seems like a somewhat complicated function to
build things on top of is the only problem. What we really need is...

  @ FormatM@ = <@ S@>(S@, (Char@) { S@; }, (S@, S@) { S@; }) { S@; };

It says: for all stream types S@, you are given a way to produce an empty
stream: S@, a way to output a single character: (Char@) { S@; }, and a way to
append two streams: (S@, S@) { S@; }. Or maybe bind should be:
(S@, (Unit@) { S@; }) { S@; }. Yeah. Which brings us back to the abstract
interface I suggested a while ago above. And this is easy to implement.

Could also be written as:

  @ ShowM@ = <@ S@>(ShowI@<S@>) { S@; };

It's not hard to define functions to convert these to String@ or output
stream.

Thus, it feels like, whatever I decide to do with output streams, I want Show
to be completely separate.

Let me try to be very concrete in the proposal now.

  # ShowI@ is an interface of stream types S@ that you can show stuff to.
  <@>@ ShowI@ = <@ S@> {
    S@ return,
    (S@, (Unit@) { S@; }) { S@; } do
    (Char@) { S@; } put
  );

  # ShowM@ is a computation that shows something.
  @ ShowM@ = <@ S@>(ShowI@<S@>) { S@; };

  # ShowL@ is a computation that shows something when you explicitly request
  # it to.
  @ ShowL@ = (Unit@) { ShowM@; };

  # Show@ is an interface to show a value of that type.
  <@>@ Show@ = <@ T@>(T@) { ShowL@; };
  
  # The preferred primitive operation for implementing show.
  (String@, List@<ShowL@>) { ShowL@; } Format = ...;

  # A helper function to show to string if you want, though presumably you'll
  # never use this?
  (ShowL@) { String@; } ShowToString = (ShowL@ s) {
    s(Unit, @(
      return: (String@ x) { x; },
      do: ((String@) { String@; } fa, (Unit@, String@) { String@; } fb, String@ tail) {
         fa(fb(Unit, tail))
      },
      put: Items<Char@>));
  };

It's feels a little weird in this case to have the Unit@ argument, but we need
that for the monadic implementation. If I want, I can define top level 'Do',
'Put', and 'Return' functions as well.

Okay? Cool? Decided? I have my proposal for Show, and it doesn't depend at all
on output streams anymore, but will also work fine with them once I figure out
what to do with them. But the interesting observation is, for this to work, we
take the less generic argument (T@ x) first, before the more generic arguments
S@, ShowI@<S@>.

Now, here's where things get interesting and I start going in circles again.
If I was willing to generalize S@, ShowI@, and ShowM@ every so slightly, so
that S@ now because M@<...>, return takes an argument, and do passes the
argument in between, then we now get something that looks just like a generic
OStream@ interface.

Maybe let's not do that. Not associated with Show anyway. We can have the
specialized version for Show and have it work fine with the generalized
version of OStream@. But there really is an important difference here: I
expect to mix puts to different output streams with the same generic monad M@.
I don't ever want to do that with Show@.

Okay? Good. Let's call this done before I change my mind. I can try to
implement this now. Make forward progress. Learn something. Then spin some
more on IO in general.

---

Switch over to new proposed show interface. This is going to be a slog, given
how much we use Show all over the place. Let's get started and hope for the
best.

First thing: am I happy with the names?

* ShowI@ - yes
* Show@ - yes
* ShowM@, ShowL@ ?? Not sure.

I'll basically never use the ShowM@ one. What name makes sense for what I
want? Focus on the name for ShowL@. Which is: a computation that shows a
value? Or maybe, a function that shows a value when given the argument Unit? A
deferred show?

Actually, looks like I won't end up using these much anywhere in code either.
We use Show@ to give the type of the Show function, and otherwise avoid
writing down the return type. So these function names don't matter that much.

How about: ShowM@ and ShowD@, 'D' for deferred? Yeah. Let's do so.

---

First questions to come up:
* /Std/Char/Foo%.Format should be for String@ or for ShowD@?
* /Core/Test%.Fail should take String@, or ShowD@?
 - Probably we want it to take ShowD@.

But now that means I'm starting to pass ShowD@ around more. Should I pick a
better name? Shown@? Showable@? Showed@? Output@?

I don't think any of those are better for now.

Other observations at time of implementation:
* Having a Do function exposed is useful for more explicit sequencing.
* I want to switch between ascii and String versions of 'String', depending on
  use case.
* It's a little tedious to write:
  Unit@ _ <- m.do(O.Show(out, Format|'...'[...]));
  This is one of those cases where I wish ShowM@ was a little more general and
  could return a value along with what it is showing. /Core/Test/Run%: we want
  to print the output and record whether the test is passing or failing.
  Otherwise I feel like I have to split it into two parts: 1. figure out all
  the log messages, 2. compute the results I'm actually interested in.

---

On further thought, there's no need to distinguish between ShowM@ and ShowD@.
We should use ShowM@ everywhere. If you're willing to know about the Unit@ arg
for ShowD, you should be willing to use 'Do' instead for sequencing. No need
for the extra sequencing.

Specifically: ShowList should use 'Do', not a single Format call. That will be
fine. Map should use 'Do' as it does now. Things like 'Pair' need not use do
and need not be lazy. Anything big like Map or List will already take care of
that.

It's slightly annoying that ShowM@ has kind <@>@ and that we can't use it with
kind @.

---

* It's nice that I don't have to deal with concat/reverse/items and other such
  structuring anymore when I switch over to the new show interface.
* ToString and Str are still useful for testing show by string comparison.
  That's a good example of a case where we create a string that we consume
  internally.
* Use of Format is really nice compared to Concat with text interleaved
  between shows.
* Might be nice to have 'Lines' and 'Join' and 'Seq' variations on ShowM@?

---

Okay! Everything is converted over to the new interface. Let me summarize my
feedback:

* For describing show implementations, this feels like an improvement
  - Format is really much nicer to use.
  - Explicit Do is great for explicit sequencing.
  - Nice not to deal with complex concat/reverse/items and other stuff I did
    to try and avoid performance issues with outputting to String@.
* ToString and Str still useful for testing show by string comparison.
* It would be useful to have helper functions like Lines, Join, and Seq for
  ShowM@.
* In practice, it was okay writing O.Show(out, ...). Doesn't seem worse than
  what we had before, which was O.PrintStr(out, ...).
* It's good to export Do, Show, Put as the building blocks.
* Slight annoyance of distinguishing between /Core/Show%.String and
  /Core/Char/Ascii/Show%.String. But makes sense to have. In once case we are
  talking literals, in the other strings - mostly file names, command line
  args, etc.
* This was a very invasive API change. I can't do this kind of thing again
  once people start using the standard library.
* Consider renaming to StringM@. Like, this is really a string builder kind of
  class. I was going to say distance ourselves from Haskell 'Show' class,
  which has additional semantics of producing valid haskell code. But I'm not
  suggesting changing the name of Show, just ShowM@.
* No issue with 'Show' being a pure kind of computation, that doesn't
  interleave with higher level things. ToString and O.Show functions work fine
  to lift it into higher level computation, which composes at that level.

---

Proposed improvements:
* Rename ShowM@ to Output@.
* Separate Output@ definition from Show@ definition.
* Remove String@ methods better suited for construction via Output@.

I'm thinking of an Output@ type specialized for outputting Char@ streams. But
the type could meaningfully generalize to other kinds of output streams. The
important point here is that Output@ is 'atomic' in the sense that it doesn't
get interleaved with other IO operations. You write an entire file at once.
You write an entire output.

There's plenty more to think about here on the IO side of things, but let me
focus instead right now on cleanup of String, Show and ShowM@.

Proposal:
* /Std/String/Lines% - keep as is, with Lines function.
* /Std/String/Escape% - keep as is.
* /Std/String/Format% - remove. Use output instead?
* /Std/Char/Ascii%
  - Str, StrE as before
  - add String, StringE
  - Replace Format, FormatE with Output@ variants.
* /Std/Char/Output%
   - Output@, Stream@, Return, Do, Put, String, Format (move to submodule?)
   - Concat, Lines, Join (string join or Output@ join?)
   - ToString
* /Std/Show%
   - Show@

Cool. Makes sense. Question is, who, if anybody, still uses the old String@
Format function? Let's get rid of that.

---

Cool. I think we are ready to transition Output@ and Show@ from /Core to /Std.
Unless I want to implement Lines an Join first? Okay. Sure. Lines at least.

---

Output@ and Show@ moved from /Core to /Std. I can start moving over
Show implementations next.

---

Next things to move over: Show for Char and String. But how do I want to show
them?

For example:  'a' or a? "hello" or hello or 'hello'?

Do I want to properly escape things? Like, 'don''t do that!' Maybe I want to
make it look like a literal? S|'don''t do that!', C|'.'?

It's supposed to be human readable, so probably just go with whatever is most
human readable? Uh... quotes if we need it, but not if we don't? Backslash
escapes for \' and \\? I suppose we could have Show and ShowE implementations
for showing things, so the caller can pick?

How about: show Char as a single element string. Show string as having single
quotes around it, and any internal single quote is shown double quoted. That
way you at least have an unambiguous interpretation of the output of show, and
you can see that something is a character or string consistently and easily.
Yes, let's go with that. And let's test that.

---

Show and Char are moved over. That's all the Shows for now. Let's call this
done.

---

* Remove /Std/Stream%.Do?
  Because we don't make use of the laziness of it anymore?
  Or keep it, as a convenient way to do a sequence of nested function
  application?

I use it all the time, right? Or did I used to and I don't as much anymore?

Here's an implementation of Join that uses it:

  (String@, List@<Stream@<Char@>>) { Stream@<Char@>; }
  Join = (String@ sep, List@<Stream@<Char@>> xs) {
    xs.?(empty: Return<Char@>);
    xs.items.tail.?(empty: xs.items.head);
    Unit@ _ := Do(xs.items.head);
    Unit@ _ := Do(String(sep));
    Join(sep, xs.items.tail);
  };

What would you have me write instead? In general, given two Stream@<A@>, I
want to sequence them. How?

  <@ S@>(S@ return, (A@, S@) { S@; } put) {
    a(b(return, put), put);
  };

Which we could implement as a Seq function.

  (String@, List@<Stream@<Char@>>) { Stream@<Char@>; }
  Join = (String@ sep, List@<Stream@<Char@>> xs) {
    xs.?(empty: Return<Char@>);
    xs.items.tail.?(empty: xs.items.head);
    Seq[xs.items.head, String(sep), Join(sep, xs.items.tail)];
  };

But it's not nice for making use of function bind. Imagine we had a plain Seq
function that takes two streams:

  (String@, List@<Stream@<Char@>>) { Stream@<Char@>; }
  Join = (String@ sep, List@<Stream@<Char@>> xs) {
    xs.?(empty: Return<Char@>);
    xs.items.tail.?(empty: xs.items.head);
    Seq(xs.items.head, {
      Seq(String(sep), {
        Join(sep, xs.items.tail);
      });
    });
  };

That's fine, but again, I want to make use of function bind for this. Or have
some other syntax (here we go again...) that let's me do application
sequentially in a statement.

If we had something more general, maybe:

  (String@, List@<Stream@<Char@>>) { Stream@<Char@>; }
  Join = (String@ sep, List@<Stream@<Char@>> xs) {
    xs.?(empty: Return<Char@>);
    xs.items.tail.?(empty: xs.items.head);
    Unit@ _ := Bind(Seq(xs.items.head));
    Unit@ _ := Bind(Seq(String(sep)));
    Join(sep, xs.items.tail);
  };

Have 'Bind' be a generic /Std/Func% thing, or part of Lazy type?

Except they don't mean the same thing, right? Use of 'Bind' requires we have
to evaluate the function being passed before outputting the first argument. We
can only properly change that later if we use the 'Do' api.

---

The implementation of /Std/Stream%.Do feels suspect:

<@ A@>(Stream@<A@>, Lazy@<Stream@<A@>>) { Stream@<A@>; }
Do = <@ A@>(Stream@<A@> ma, Lazy@<Stream@<A@>> f) {
  <@ S@>(S@ return, (A@, S@) { S@; } put) {
    ma(f(Unit, return, put), put);
  };
};

It's calling the second first. Getting the resulting value, then passing that
to the first.

We could change the type of stream to be:

  <@ S@>((Unit@) { S@; } return, (A@, S@) { S@; } put) {

Then pass the function through and it would make sense.

Anyway, I don't think the Do API is the problem. I think it makes sense and is
what I want everyone to be using. If anything is a problem, it's the
implementation. But I can change that later if/when I find a concrete instance
of it being a real problem.

