
Cmp@ = /Std/Cmp%.Cmp@;
Compared@ = /Std/Cmp%.Compared@;

Addr@ = /Hwdg/Addr%.Addr@;

Cmp@<Addr@> Ord = (Addr@ a, Addr@ b) {
    Compared@ r = /Std/Int/Ord%.Cmp(a.x, b.x);
    r.?(equal: /Std/Int/Ord%.Cmp(a.y, b.y));
    r;
};

@(Ord);
