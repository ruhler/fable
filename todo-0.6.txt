Fble 0.6 TODO
=============
Things to consider doing for the fble-0.6 release.

* Should /Std/Set% implement a Map function?

* Come up with a decent example use case for /Std/Stream/Test%.ToTest?

* Add tests for showing lists?

* SkipTest is super awkward to use.
 - Anything we can do about that?

* Add a note to String@ type when Stream@<Char@> is better
 - Namely, if it's going to be consumed once / output once, better to use
   Stream@<Char@>. Probably more efficient that way.

* How to capitalize ascii?
 - Ascii, ASCII, ascii?
 - I don't do it consistently.

* /Std/Stream/String% seems like a weird place for GetLine, GetText.

* Decide when to use @l in documentation
 - /Std/List/Show%: The @l{Show@} instance for @l{List@}.
 - /Std/Maybe/Show%: The Show@ instance for Maybe@.

* /Std/List/Search%.Span reads weird.
 - Span(Elems|ABCDE, Ge(Elem.C)) reads as: span elements of the list greater
   than C. But it's actually: span elements of the list C is greater than.
 - Anything worth doing there?

* /Std/List/Index%.Range feels like a weird name for that function.
 - Should it be sub, or sublist?
 - Should it take start and end instead of start and length?

* Should /Std/List/Sort% take Cmp@ as the last argument?
 - For better integration with function bind syntax?

* Change /Std/List/Sort%.Sort to merge sort?

* Should /Std/List/Count% be merged with /Std/List/Index%?
 - They have the same sets of dependencies?

* Should string show not show surrounding quotes if there is no punctuation?
 - Like, is the idea it is formatted as an fble word? Then we should be able
   to output things like hello without surrounding quotes, right?

* Decide when I should use @l{...} around type names or not in doc comments.
 - For example /Std/Cmp/Show% uses it around @l{Compared@}, but not in arg
   types?

* Should Tail and Init of empty lists be empty lists instead of undefined?

* The Cmp@ interface feels not much used
 - When should I provide Le, Ge, etc. and when not?

* The /Std/Stream/String% module is weird in what it defines.
 - Should it be reorganized some other way?

* Define sample makefiles for people who want to build compiled programs?
 - Integrate it into a tutorial somewhere?
 - Maybe as part of the modules tutorial or shortly thereafter?

* Mark /Std/Int/IntP%, et all docs as @dev.

* Add left side menu to generated website for tutorials?

* Update release notes.
* Double check things still work on github.
* Proof read tutorials.
* Make release.
