{ /Char%; @(Char@); };
{ /List%; @(S@, List@, S); };

@ Str@ = S@<Char@>;
<@>@ CharLit@ = List@<Char@>;

(CharLit@){S@<Char@>;} Str = (CharLit@ mkList) {
  S<Char@>(mkList);
};

@(Str@, CharLit@, Str);
