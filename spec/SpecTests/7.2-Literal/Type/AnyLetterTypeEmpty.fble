# @@fble-test@@ compile-error 8:1
Unit@ = *();

# The element type has to be a union type, even if the literal is empty.
Letter@ = (Unit@) { Unit@; };
L@ = +(*(Letter@ head, L@ tail) cons, Unit@ nil);
(L@) { L@; } f = (L@ l) { l; };
f|''; 
