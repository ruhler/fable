
* Fix issue with opengl with dummy driver.

Let's remind myself of what the original goal was and what the problem was.

Original goal: be able to test that an app like fble-invaders doesn't crash
when you launch it without having to open a window.

I don't remember what specific code coverage I was trying to get with this. If
you aren't interacting with the app, what's the point? How will the app end?

Here's the code I had:

    # TODO: This test fails, maybe because --driver dummy never supported GL?
    # test $::b/pkgs/invaders/fble-invaders.tr \
    #   "$::b/pkgs/invaders/fble-invaders" \
    #   "$::b/pkgs/invaders/fble-invaders --driver dummy --profile $::b/pkgs/invaders/fble-invaders.prof -- --test"

So there must have been an option to fble-invaders to do some test stuff and
then exit. That's vaguely reasonable.

How did it work though? Or how didn't it? What was the bug I ran into?

I had added code:

  if (window == NULL) {
    fprintf(stderr, "Unable to create window: %s\n", SDL_GetError());
    FbleFreeValueHeap(heap);
    FbleFreeProfile(profile);
    return FbleCliMainOtherStatus(FBLE_MAIN_OTHER_ERROR);
  }

So window must have been returned as NULL. Before that, maybe everything just
silently failed? I can try recreating that scenario.

Anyway, the code I'm trying to test here is not the .fble code. I can do that
with fble tests. I'm trying to test the type of the main function passed and
logic in app.c to interact with the app.

I see two approaches to take here:
1. Remove --driver, since it's not useful for anything. Give up on having a
way to test the integration of an app with fble-app.

2. Restructure fble-app to add a --test mode, or write a library to add a test
mode that abstracts out the back end and let's you switch between SDL and its
own TEST backend. Maybe the TEST backend lets you drive events manually. Write
it so we share as much code as we can between the real app and the test
driver. Maybe the test driver is a library you can instantiate to write C
based unit tests for your app.

But why does (2) really matter? We aren't testing the app. You can already do
that in fble. We are testing the type of the app and the app.c code. I
shouldn't need to give all apps a way to do that? Or: all apps should be able
to test the type of their app somehow, but only app.c needs to test itself.

---

Let's consider what code I'm trying to get coverage for:
1. The small bit of .fble code between what we can test in pure fble and the
   interface to fble-app. e.g. /Invaders/AppIO%.Main function.
 * In particular concern about forgetting to update every app if I change the
   type of the Main@ function.
 * This testing would need to be done per app.
 * Having a way to type check as part of compiling to an fble-app binary would
   cover the type part, but not any other wrapper logic.
2. The part of app.c that interacts with the app.
 * Only needs to be tested with a single app to make sure it works for all.
3. The part of app.c that interacts with SDL.
 * Needs SDL, but not necessarily graphics?
4. The part that connects up to the user.
 * Needs video, keyboard input, mouse input, etc.

I can test (2), (3), (4) in a manual way to day by manually running the demo
app. I can test (1) manually by manually running each specific app.

I can't test each app in a generic way beyond checking for their type. If I
really want to test each app, I need a way to simulate input and output and
validate that. So maybe I have some way to feed input into fble-app and record
a video output and compare that video output to what I expect.

I could write a specific test app and try to cover (2) and (3) by having it
draw some stuff and request some tick events. It's only limited coverage
though: we don't test the different input events.

So you see, it's all pretty scattered.

Brainstorm of concrete things I could do:
1. Add option at compile time to enforce main module type.
  e.g. fblemain $target.o $::b/bin/fble-compile "--main FbleAppMain -t /App/Native/Type% -m $path"
  Not sure how to do the same for raw use of fble-app in a generic way. Like a
  way to hard code inside fble-app binary the expected path for -t.
2. Remove --driver option to fble-app.
  This should be done regardless I vote. It's just a question of what to
  replace it with.
3. Implement an --test option to fble-app
  - That skips use of video
  - That maybe allows sending inputs and getting outputs for followup
    assertion.
4. Write a test app for fble-app that draws some stuff and does some tick
  events.

I want to play around more with (3). That kind of wants a (4) to start.

Let me write a simple app for (4). Have it draw something, run for, say 5
seconds, draw something else, then exit.

Then see if I can make it run via a --test option to fble-app that initializes
and uses SDL, but skips initialization of the window. Surely we can do
offscreen rendering somehow?

Then decide if any of that's useful and what next steps are.

(4) was easy enough. Let's play with (3).

The idea / question is: can I skip video initialization, but keep timer and
other code, and have it run through?

* I removed SDL_INIT_VIDEO from SDL_Init, but it still initialized and used
  video. I don't understand. Is video not needed? Is it initialized somewhere
  else along the way? Ah, yes. The call to SDL_CreateWindow initializes video
  automatically.

Things to explore:
* Can I create a window other than via video? Like SDL_CreateWindowFrom*?
* Can I create a GL context some other way? I assume no...
* What if I don't create a gl context. Do the gl functions still work?

I'm skeptical. 

---

Here's what I think I should do:
1. Add -t option to FbleMain. You pass a module path. It finds a .fble.@ (or
.fble) file to use to check the type of the main function.
 - Pass the expected path for fble-app and fble-cli at compile time.
 - Pass a default path at runtime for the app if using interpreted mode, but
   allow the user to override it to whatever they put for -m for the rare case
   where they are doing interpreted code without depending on the library the
   binary was compiled for.
 This is a step forward for solving our typing issue. Should make it a lot
 harder to run a module of the wrong type in fble-cli or fble-app.

2. Add a no-video option to fble-app, run my newly created test app as a test
case with no-video. Try to have no-video exercise as much of the code as
reasonable without video.
 This way I have some coverage of fble-app at least.

Continuing on with options for (2), let's see what happens if I don't create
any gl context at all or if I use SDL_CreateWindowFrom*.

It actually works fine to use a NULL window. I just need to initialize EVENTS
explicitly if I don't do it via video. Interesting. Shall I clean up my
changes and see what I can make of them?

Wow. It's very simple. Replace create window with NULL, and it basically does
exactly what I want. Cool. Let's go with that then.

---

Too bad I don't get keyboard events going in in no-video mode. Maybe leave
that as future work if desired.

Seems like you need the video driver set up to read events. Should I try
setting up a dummy driver if the user passes --no-video? That way we could get
key events at least? Or does that not make much sense?

Oh. You know what will happen. I'll get an error about the gl context. Let's
not worry about it for now. It doesn't make sense to want to send key input
without video enabled anyway.

---

While on this topic, let's see what I can do about:

* fble-app capture input so we don't see a bunch of it on the console after
  the game is done?

In theory it's supposed to work, looking at the sdl source code. Some
conditions must be failing.

Actually it does work fine, but not if I'm running screen. Interesting. Is
screen capturing the input somehow first? Or screen causes the keyboard muting
to be disabled? Or the muting method needs to be different in this case?

Sigh. It's not surprising that screen does funny stuff. It's not surprising
that sdl doesn't handle the case of running under screen in the linux console.
I shouldn't have to do special stuff in app.c to address this case.

Can I leave SDL out of it and experiment myself with changing muting?

For example, manually call the equivalent of:

  ioctl(state->console_fd, KDSKBMODE, K_OFF);

See if I can reproduce the behavior that it stops echoing of keyboard only
when not running under screen.

Maybe via: linux/keyboard.h? No. linux/kd.h

  console_fd = open("/dev/tty", O_RDONLY | O_CLOEXEC);

Let me try it.

Note to self: Don't turn off the keyboard when the only way you have to turn
the keyboard back on requires using the keyboard.

I can replicate the behavior. My guess is the ioctl to turn off the keyboard
doesn't work under screen.

Yup. We get: 'Inappropriate ioctl for device'.

Because /dev/tty is not the keyboard device.

Now it all becomes clear. SDL is using the linux console driver, but /dev/tty
isn't pointing to the console under screen, so the ioctls to control it don't
work.

Something else works though. How does SDL know to use the console driver, and
how is it getting input and stuff like that? Especially if the keyboard is
off?

Under screen, tty says '/dev/pts/<n>'

Without screen, tty says: '/dev/tty1'

I suppose the question is: could I use /dev/console or /dev/tty1 or something
other than /dev/tty to make the key off stuff work while under screen? Mostly
just out of curiosity, because it's not like I can change SDL.

/dev/console doesn't work either way.
/dev/tty1 works without screen, gives 'Operation not permitted' with screen.

It's kind of feeling like the answer is to just deal with the fact that K_OFF
isn't going to work when running apps under screen.

Even if I could do something to detect if I'm running under screen and stop
echoing characters, it doesn't make sense to do in general. Normally the
terminal and the window are two different apps. If you are typing into the
terminal, you want it to be echoing there (and not going to the app,
presumably).

If I really care for myself, I can redirect that echoed output to /dev/null?

No. That doesn't help.

Maybe: stty -echo ; ... ; stty echo

That oddly gets rid of the garbage from the arrow keys, but not spaces or q
characters.

Oh well. I spent enough time looking into this. I don't see an easy solution.
It's a rare case (linux console + screen + sdl), and it doesn't cause that
much harm to me to see the output on my console.

I suppose I could add an option to fble-app to no-echo specifically for my
case?

Sure, let's at least figure out how to implement that, even if we don't go
with it. Try via termios.

Apparently it's in posix. Not sure if it will work on windows. But then, it's
not needed on windows, right?

  #include <termios.h>
  #include <unistd.h>

  struct termios attr;
  tcgetattr(STDIN_FILENO, &attr);

  attr.c_lflag ^= ECHO;
  tcsetattr(STDIN_FILENO, TCSANOW, &attr);

I say, not worth the effort.

---

* Add -t option to compiler to pass expected type of main

Remember why: so I can specify the type as part of the build command,
independent of the main module. That's why we can't just ask the user to add
to their main program:

 Foo@ x = ...;
 x;

I think this should be straight forward, if slightly tedious to wire things
through.

Reviewing how I would add it, starting with fble-compile, which presumably is
the easier case?

* FbleLoadForModuleCompilation should take an extra argument?
* FbleCompileModule should take an extra argument?
 - Or do we put the type in the loaded program?

Okay, so it's going to be slightly more involved to update the APIs to support
this. We'll want to support it as part of FbleCompileModule and
FbleCompileProgram?

Or... should we just add a separate compilation mode? --typecheck? Because I
would normally only want this when using the --main option to fble-compile
otherwise.

But it's a bit sad to have to reload the entire module, yet again, just to
type check it.

How does the interpreter path look?

It uses FbleLoadForExecution, followed by FbleCompileProgram. Same kind of
structure as fble-compile, only different function to load the program in the
first place.

Perhaps we should defer this discussion until after spending a little more
time thinking about how to do true modular compilation.

