# @@fble-test@@ no-error
@ Unit@ = *();
Unit@ Unit = Unit@();

@ Bool@ = +(Unit@ true, Unit@ false);

# Basic abstract value.
@ Pkg@ = %(/SpecTests/'9.4-AbstractValue'/Basic/Basic%);
@ AbsBool@ = Pkg@<Bool@>;
AbsBool@ t = Pkg@(Bool@(true: Unit));
t.%.true;
