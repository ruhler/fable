Monad Composition
=================

This keeps coming up. I've had some thoughts and made some progress, but I'm
not convinced my current approach scales the way it should. So let's review.

I want to be able to write functions that work for any monad that supports a
specific interface. For example, any monad that supports random number
generation. Then I want to be able to reuse those functions with a variety of
different monad instances easily: a pure state monad, an Io monad, a
transformed Io monad, a monad that can also do input and output, etc.

See thoughts/fble-0.6/stdlib.random.txt for discussion of various approaches
and explanation of how I got to where I am today. The progression is roughly:

1. Write the function to use a concrete monad.
 - But then it doesn't compose well with variations on that monad.

2. Write a function that works forall M@ that have a Monad@ instance and an
Rng@ instance. Rng@ is separate from Monad@ in this case.
 - But then type inference doesn't work to infer the type M@ and we have to
   pass around 3 arguments everywhere: M@, m, r.

3. Write a function that works forall M@ and takes an RngM@ instance, where
RngM@ has an explicit M@ field to enable type inference of M@ and includes a
Monad@ subfield so we can pass around one argument for everything we need.
 - But now any time we want to say m.do or m.return, we now say something like
   rng.m.do, rng.m.return. Is that better than (2)?

I'm worried this doesn't scale up too well though. Like, what if I want to
compose an Rng@ with Io@. Am I given RngM@ and IoM@? Or I have to define a new
RngIoM@? How can I extract easily an RngM@ from this RngIoM@ to pass to a
function that only wants the Rng@ part of the interface?

Can we include the M@ field in all sub-structures so there is no need to
distinguish between Rng@ and RngM@? How about the Monad@ field? Any concern
about having two different named Monad@ subfields?

---

There's no problem having M@ everywhere. There's no problem having M@ in just
those places that we need it. For example, IoM@ doesn't need M@ if it
references MonadM@ and MonadM@ has M@. The type parameter ensures consistent
values for M@. No need to worry about the type inference problem.

The Monad@ subfield is more concerning, because how do you make sure it stays
consistent across different subfields?

Composition is great, except for introduction of extra .foo accessors needed.
Let's say I didn't care about that extra accessors. How would I structure
things?

Io has run method, and M@?
IoM has 'io' and 'monad' subfields.
 - Ignoring that io.io is annoying.

Rng has random method, and M@?
RngM has 'rng' and 'monad' subfields.

IoRngM has 'io', 'rng', 'monad' subfields.

What I don't really like here:
* Requiring an extra top level type to be defined: IoM, RngM, IoRngM.
* The wordiness of composed subfields. foo.m.return compared to m.return.
* Extra conversion needed to extra, e.g. RngM from IoRngM field.

These all seem slightly closely related issues.

I know the alternative though. Pass Io@, Rng@, Monad@ around everywhere. Now
we are passing 3 things at once instead of 1. If I'm really passing things
everywhere, that's pretty annoying.

It feels like what I want surface level is some notion of interfaces. Where
they are a list of fields always at the surface level, and we can compose them
together without intermediate name, or optional intermediate name. And if I
have an IoRngM, for example, I can pass that directly anywhere an IoM or RngM
is expected, because the expected methods are there.

It's like we just need a slightly nicer layer to give us something like
(multiple?) inheritance and this and implicit naming and implicit 'this'
arguments.

Like, what if we could generalize the concept of 'this' to support multiple
different this arguments to a function. We pass 'this' implicitly. We
reference it implicitly. We have some magic to make it all nice. Then you
don't have to worry about composition at all. You just define what you need,
it gets given to you, and it gets propagated implicitly anywhere it needs to.
Unless, of course, you want to provide an alternative explicit argument.

Maybe start thinking about single 'this' first. See if I can understand that.
And see if that can generalize. Because what is 'this' aside from an implicit
argument passed to functions?

How nice would it be if we could refer to 'do' and 'return' directly, and they
inferred the right value for m? And we didn't have to pass m and io and all of
that everywhere? Is there any reasonable rigorous way to do this?

---

See also discussion in thoughts/fble-0.6/object_oriented.txt, which I feel
like is trying to address the same underlying challenge.

Here's what I think the challenge is:

We want to define some set of axioms and parameterize a family of separately
defined functions by those axioms. Sample axioms: do, return, random, run.

To parameterize functions by the axioms, we pass the axioms as arguments to
the functions. Either individually, or grouped together in structures, or some
mix of approaches.

It's tedious to pass these things around everywhere:
* Annoying to have to access the axiom from whatever structure you pass it
  with, e.g. io.m.return, io.io.run
* Annoying to have to pass the axioms everywhere we want to call things, 
  e.g. Range(rng, m, Int|10, Int|20), Range(MkRng(iorng), m, Int|10, Int|20)

You reduce the cost of passing things everywhere by grouping them together
into one object, but that increases the cost of accessing the axioms.

How can we solve this?

First off, understand that the language as full support for making the names
of things nicer, if we are willing to use it. It's the same thing we do for
top level imports. Imagine writing at the top of each function body:

  % return = io.m.return;
  % do = io.m.do;
  % run = io.io.run;
  % Range = /Core/Int/Rand%.Range(rng);

Now the body of the function lets us write exactly what we want to write. The
only thing missing would be instead of saying things like Foo(a, ...), Foo(b,
..), we could write a.Foo(...), b.Foo(...), which for whatever reason I don't
fully understand, feels much nicer.

But top level imports are super tedious. I know it. Look at the above example.
I didn't bother figuring out the right kinds for things, because it's too much
effort when the compiler can tell you the right thing anyway.

What makes importing tedious:
* Having to figure out the kinds of things, when it feels like they don't
  matter at all in practice.
* Taking up so many lines: One line for each definition in practice. For a
  short function, this could easily take up as much space as the content of
  the function. It often takes up half of a module's definition just importing
  things.
* Module paths are long and slightly annoying to write and deal with.
* Notice how much duplication is in the above example: io is reused 3 times,
  return, do, run, Range all show up twice.

So here's the first potential solution idea: come up with a much nicer syntax
for importing things. That could help with monad composition here, but also
with top level module imports.

One challenge is that I really like the fact that you can search for any
variable name and jump to the definition. We don't ever define variables
implicitly. You can always track down where and how something is defined.

Example wild ideas for how to simplify top level definitions. Using the
examples from above, which capture a decent range of the different things I
might want:

  ^ io(.m(.return, .do), .io(.run));
  ^ /Core/Int/Rand%.Range(rng);

Is it much better? I'm not sure. Should I try using the above approach for
some of my functions, see how it feels? Key ideas:
* Skip writing the kind. The compiler knows that.
* Reuse names from the defining expression for the variable.
* Allow multiple imports from a single item at once.

  ^ @(m: @(return, do), io: @(run)) = io;

An alternative approach is to try to automatically figure out what someone
means when they refer to some name. Call it compiler magic. What kinds of
searches should we do?

* See if a name refers to a subfield of a variable in scope.
  For example, you say 'return'. That's not defined, but there is a local
  variable 'm', or 'io', where if we search, we find 'm.return' and
  'io.m.return' options for return.
* See if you can infer an argument for something.
  For example, you say 'Range', it takes an RngM@ as an argument, we happen to
  have an RngM@ in scope, so automatically pass that.

Might be nice, but also very scary to do that kind of magic. How do you figure
out what things are actually referring to?

Another style of approach could be have an: 'import everything from this
struct' syntax. Then the user could write helper functions. Actually, that's
pretty similar to the thing I was trying above.

  ^ io.m;   # Imports return and do fields of io.m.
  ^ io.io;  # Imports run field.
  ^ @(Range: /Core/Int/Rand%.Range(rng)); # Imports Range field

But we better provide a nice way to import a subset. And things start to get a
little messy.

Think about how some other languages do things.

C++:
* Classes give some fancy magic:
 - Member fields let you bundle together axioms associated with the class
   instance.
 - this.Foo() can be written just Foo: let's you pass all those axioms around
   implicitly.
* Essentially we are hiding the axioms in the class state, which is passed
  around explicitly and used for name resolution. And people can add functions
  to that class. But it's a closed list. If I was happy with a closed list, I
  could do everything that way too, pass around a single this option and use,
  e.g. this.do, this.return, this.random, this.run, this.Range everywhere.

Now I have a pretty good sense when writing a function what I'm taking as
import that is kind of an interface. I could easily separate out the interface
arguments from regular arguments. Let's say we have a notion of an interface
that you pass to a function. This is an environment. A set of name value
pairs.

When I call a function that expects some environment, let's say it inherits
from the current environment. And/or I can explicitly modify the environment
when calling that function if I want to override things.

Is it as simple as that? One key being: I can define more in my environment
than a callee requires, which is nice for composition. We try to put
everything at the top level of the environment, so I would have:
  do, return, random, run
instead of: m.do, m.return, rng.random, io.run.

When specifying your environment, you have to deal with name conflicts there.
So environments can maybe hold sub-environments.

In a sense, an environment is like a read only struct value, and we can pass
any struct value whose type is a superset of what is required by the user.

We could define an environment using a struct type. We could provide helpers
to inline things. For example:

  Monad@ = *(Do@ do, Return@ return);
  Io@ = *(Run@ run);
  Rng@ = *(Random@ random);
  IoM@ = *(Monad@ *, Io@ *);

Now I can have a function that expects Monad@, and I have an instance of IoM@,
and I can pass that IoM@ directly do the function where a Monad@ is expected,
because it has everything we need.

Now imagine we have a nice way to import everything in an environment. And
imagine we have a nice way to capture everything in the environment, or, we
pass the environment implicitly. Everything would be solved, right?

  # Generates a random integer uniformly in the range [0, n).
  <<@>@ M@>(RngM@<M@>, Int@) { M@<Int@>; }
  Of = <<@>@ M@>(RngM@<M@> ^, Int@ n) {

  # Generates a random integer uniformly in the range [lo, hi]
  <<@>@ M@>(RngM@<M@>, Int@, Int@) { M@<Int@>; }
  Range = <<@>@ M@>(RngM@<M@> ^, Int@ lo, Int@ hi) {
    Int@ x := do $ Of^(Incr(Sub(hi, lo)));
    return $ Add(lo, x);
  };

Where here I'm using magic syntax '^' to mean:
1. When used as a function argument, it says import everything from that
struct into the current environment.
2. When used after a function, it means extract everything you need for the
function argument struct directly from the environment.

That's interesting. This sounds readily doable aside from the fact that it
implicitly defines variables. Also, not sure how it works with type inference.

Maybe we could have:

  # Generates a random integer uniformly in the range [lo, hi]
  <<@>@ M@>(RngM@<M@>, Int@, Int@) { M@<Int@>; }
  Range = <<@>@ M@>(RngM@<M@> rng, Int@ lo, Int@ hi) {
    ^ rng(do, return, random);
    Int@ x := do $ Of^(Incr(Sub(hi, lo)));
    return $ Add(lo, x);
  };

Hmm... Part of the issue is it would be nice to pass things from rng to Of
without listing them explicitly. So not going through my current environment.

But it feels like what I'm suggesting is:
* Have a single interface with everything moved to the top level.
* Have a convenient way to import from the top level.
* We already have a convenient way to construct subinterfaces explicitly?
  @(foo, bar, ...);
* Maybe need a convenient way to define these flag structs?
  RngM@<M@> = @(Rng@<M@> *, Monad@<M@> *);

What if instead we had a convenient way to search for sub-sub fields?

  RngM@<M@> = @(Rng@<M@> rng, Monad@<M@> m);

  # Generates a random integer uniformly in the range [lo, hi]
  <<@>@ M@>(RngM@<M@>, Int@, Int@) { M@<Int@>; }
  Range = <<@>@ M@>(RngM@<M@> rng, Int@ lo, Int@ hi) {
    ^ rng(*.do, *.return);
    Int@ x := do $ Of(rng, Incr(Sub(hi, lo)));
    return $ Add(lo, x);
  };

What if we added support for two things:
1. An easy way to import a list of (sub) fields from a struct.
2. An easy way to construct a struct from a superset struct.

More simply, is it enough to provide a nice way to import from immediate
fields of a struct?

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<IStream@<M@>>>; }
  Read = <<@>@ M@>(IoM@<M@> io, String@ path) {
    do, return, run ^= io;
    Maybe@<File@> file := do(run(Open(path, Str|rb)));
    file.?(nothing: return(Nothing<IStream@<M@>>));
    return(Just(run(GetByte(file.just))));
  };

I guess the example I want should have three things:
1. Direct use of axioms.
2. Pass set of axioms to called functions.
3. Pass subset of axioms to called functions.

Using our imaginary IoRngM:

  <<@>@ M@>(IoRngM@<M@>, String@) { M@<Maybe@<IStream@<M@>>>; }
  ReadRand = <<@>@ M@>(IoRngM@<M@> this, String@ path) {
    do, return, m, rng, run ^= this;                # Import from subfields.
    String@ pathr := do(StrRand(@(m, rng), path));  # Pass subset of axioms.
    Maybe@<File@> file := do(run(Open(pathr, Str|rb)));
    file.?(nothing: return(Nothing<IStream@<M@>>));
    return(Just(run(GetByte(file.just))));
  };

Yeah. It's the pass subset of axioms that I don't have a good answer for. I
could do the nice import syntax maybe. Certainly without nesting. I'm not sure
about with nesting.

  <<@>@ M@>(IoRngM@<M@>, String@) { M@<Maybe@<IStream@<M@>>>; }
  ReadRand = <<@>@ M@>(IoRngM@<M@> this, String@ path) {
    do, return ^= this.m;    # Import from subfields.
    run ^= this.io;          # Import from different subfield.
    String@ pathr := do(StrRand(RngM(this), path));  # Pass subset of axioms.
    Maybe@<File@> file := do(run(Open(pathr, Str|rb)));
    file.?(nothing: return(Nothing<IStream@<M@>>));
    return(Just(run(GetByte(file.just))));
  };

Not sure. Keep thinking about it. Try out explicit import for now, see if that
makes things nicer or worse.

---

Let's say I didn't add any new language features. What are the decisions to
make about how to structure things?
1. Pass individual objects or structured?
  Foo(do, return, random, io)
  Foo(m, rng, io)
  Foo(iorngm)
2. Maintain structure, or flatten?
  iorngm.m.do, iorngm.rng.random
  iorngm.do, iorngm.random
3. What name to use for intermediate fields?
  iorngm.m.do, iorngm.rng.random
  iorngm.Monad.do, iorngm.Rng.random

In theory if we do all the importing up front, these decisions don't matter
for the body: we always have direct access to axioms and pre-applied composite
functions.

My gut feeling is the best way to describe things is with structure and full
names: iorngm.Monad, iorngm.Rng, etc. Maybe rename it to 'this':
  this.Monad, this.Rng, etc.

Because that gives us clear names, easy composition. Probably need helper
functions to decompose iorngm into iom and rngm though.

Which suggests the alternative of no composition might be just fine? Compose
via multiple arguments?

---

Experience switching /Core/Stdio/FFI% to explicit import style:
* The body of the function looks much more legible now.
* The import is just as annoying as I expected. Kinds in particular.
* I can't import a function under the same name as itself. This doesn't work:
  % Output = Output(io);
  Which is tedious. Now I need to come up with a different name.
  io.Output would be so nice though...
* I don't know if I should factor out the imports to just after the io arg, or
  if I should do it in the body every time. Probably it's faster to do it
  every time to avoid partial function allocation every time.

I do feel like things would be much nicer if I could just have a nice import
syntax. Can I come up with something that works well in general?

Requirements (targeting imports in general):
* Don't have explicit kinds.
* Allow multiple vars to be imported in a single statement. e.g. do, return.
* Allow for overwriting of existing names. e.g. Output: Output(io).
* Allow for application or other modification of things. e.g. Output(io).
* Allow for renaming of things. e.g. MyFoo = /Foo%.Foo;

First attempt:

 name, name, ... ^= <expr>;

Here <expr> is an arbitrary expression of struct type. We bring each name into
scope from an immediate field of whatever was returned from the expression.

Supports: no explicit kinds, multi vars at once. What about overwriting of
existing names, modification, and renaming of things?

 Output ^= @(Output: Output(io)); # Not great...

What if we supported generic kind? Like, inferred kind? Non-recursive?

 ^ Output = Output(io);
 ^ MyFoo = /Foo%.Foo;

Could we have the same syntax support defining a new name and importing a
field name all at once?

 .m.do, .m.monad ^= io.m;
 m ^= io.m;

Basically for these two cases, I want to provide my own name and an arbitrary
expression. No kind, no recursion allowed.

If we allow us to not specify kinds when importing things, why would we ever
want to specify kinds when importing things?

I've asked this question in the past. I should find what I decided before. But
also:
* So we know for some entity Foo if Foo(x) is doing a type inference.

That's the only reason I can think. To distinguish between <@>% and %, so we
know when type inference is being done. Is that a good reason?

Let me dig up the previous discussion on this if I can.

thoughts/fble-0.5/kinds.txt:
* For let's specifically I suggested better to have explicit kinds because: 

"It's really not that hard. Worse case you mess up, the compiler teaches you
something. It reinforces everywhere you import something how to think about it
in terms of polymorphism. If we can provide this extra documentation in code
without much effort, let's do so. That's consistent with the idea of a
language that takes advantage of the computer to help you write your code."

The interesting example is this one. Compare:

  @ Bool@ = /Core/Bool%.Bool@;
  <@>@ List@ = /Core/List%.List@;
  <<@>@>@ Monad@ = /Core/Monad%.Monad@;
  <@>@ Maybe@ = /Core/Maybe%.Maybe@;
  <<@>@>@ IStream@ = /Core/Stream%.IStream@;
  <<@>@>@ OStream@ = /Core/Stream%.OStream@;
  @ String@ = /Core/String%.String@;

With:

  @ Bool@ = /Core/Bool%.Bool@;
  @ List@ = /Core/List%.List@;
  @ Monad@ = /Core/Monad%.Monad@;
  @ Maybe@ = /Core/Maybe%.Maybe@;
  @ IStream@ = /Core/Stream%.IStream@;
  @ OStream@ = /Core/Stream%.OStream@;
  @ String@ = /Core/String%.String@;

And I suppose now with something like:

  Bool@ ^= /Core/Bool%;
  List@ ^= /Core/List%;
  Monad@ ^= /Core/Monad%;
  Maybe@ ^= /Core/Maybe%;
  IStream@, OStream@ ^= /Core/Stream%;
  String@ ^= /Core/String%;

It's okay. I think it's okay. Nicer without anything than with % versus @. We
know that info from the name. No reason to put something else there.

Do we ever need an explicit kind in a let expression? How about just get rid
of that entirely, always infer the kind?

But we do need a way to have recursive non-typed definition like:

  @ L@ = +(*(T@ head, L@ tail) items, Unit@ empty);

I'm fine writing it without the kind:

  L@ = +(*(T@ head, L@ tail) items, Unit@ empty);

But it means we have legitimate use case with and without recursion. That's
the case in general, right? Like, the case where I want to shadow, versus the
case where I don't. That comes up in practice when making modifications to
things.

I suppose I'm suggesting we support the following. See if this is enough:
1. Recursive, explicit type.
  e.g.  (Int@) { Int@; } Factorial = (Int@ n) { Mul(n, Factorial(Decr(n))); };
2. Non-recursive, explicit type.
  e.g.  State@ s &= s.@(dir);
3. Recursive, implicit type.
  e.g. L@ = +(*(T@ head, L@ tail) items, Unit@ empty);
4. Non-recursive, implicit type.
  e.g. Output &= Output(io);
5. Multi struct field import.
  e.g.
  do, return ^= Monad;
6. Function bind syntax.
  e.g.  Int@ x := do(...); ...;

For example:

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    <@>% do = io.m.do;
    <@>% return = io.m.return;
    <@>% run = io.io.run;
    % output = Output(io);
    
    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

Becomes:

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    do, return ^= io.m;
    run ^= io.io;
    Output &= Output(io);
    
    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

You know? I'm not convinced that's any better. It's still almost four lines of
code that I'm going to ignore as a reader. But now it has funny ^= and &=
which is not really intuitive at all for me.

How about instead:

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    do = io.m.do, return = io.m.return, run = io.io.run, output = Output(io);
    
    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

Hmm... Anyway, one thing coming out of this: Think how much nicer it would be
to not write explicit kinds at all? Except... does it hide polymorphism, which
we really ought not to hide?

---

Getting rid of kinds in let feels like it makes sense to me. The compiler
clearly doesn't need them. I clearly have no notion when writing code what the
actual kinds of things are. The only possible use for them is documentation,
but I ignore that anyway. I don't think it's worth it.

Think how much simpler let expressions would be from a language point of view
if we said the type is optional? Not: type or kind. Just optional type. Think
how much nicer it would be to include things? No more tedious reliance on the
compiler to tell me the kind of things. I think it's worth a try. See how it
feels. See if I think I'm missing something or not.

Now then, consider if we flattened the hierarchy for all my interfaces. Monad@
is M@, do, return. Rng@ is M@, do, return, random. Io@ is M@, do, return, run.
If we had that, it would be convenient to define things like:

  <<@>@ M@>(Io@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(Io@<M@> m, String@ path) {
    Maybe@<File@> file := m.do(m.run(Open(path, Str|w)));
    file.?(nothing: m.return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(m, file.just),
      close: m.run(Close(file.just))
    );
    m.return(Just(ostream));
  };

We just need helper functions when defining the Io@ interface type to
construct it from sub components and deconstruct it if desired. If we wanted
to, we could have Rng@ be M@, do, return, random, and Monad. Thought that
feels a bit excessive to me.

Another idea: use of function bind syntax to import things in a single line.
If we have a common set of functions, we could do, for example:

Uh... I guess we need explicit types for that. So nevermind.

I just can't convince myself on multi struct import. It feels too limited.

Well, let's see. Here are the imports for /Core/Int/Tests%, for example:


  @ Bool@ = /Std/Bool%.Bool@;
  % Not = /Std/Bool%.Not;

  <@>% AssertEquals = /Std/Eq/Assert%.AssertEquals;

  % Ord = /Std/Cmp%;
  @ Compared@ = Ord.Compared@;

  <@>% List = /Std/List%.List;

  @ String@ = /Std/String%.String@;
  % Str = /Std/Char/Ascii%.Str;
  % String = /Std/Char/Ascii/Output%.String;

  @ Test@ = /Std/Test%.Test@;
  % Test = /Std/Test%.Test;
  % TestSuite = /Std/Test%.TestSuite;
  % Pass = /Std/Test%.Pass;
  % Fail = /Std/Test%.Fail;

  @ Unit@ = /Std/Unit%.Unit@;

  @ Int@ = /Std/Int%.Int@;
  % Neg = /Std/Int%.Neg;

  % Sqrt = /Std/Int/Sqrt%.Sqrt;

  % Lt = /Std/Int/Ord%.Lt;
  % Le = /Std/Int/Ord%.Le;
  % Gt = /Std/Int/Ord%.Gt;
  % Ge = /Std/Int/Ord%.Ge;
  % Min = /Std/Int/Ord%.Min;
  % Max = /Std/Int/Ord%.Max;
  % IOrd = /Core/Int/Ord%.Ord;
  % IsZero = /Std/Int/Sign%.IsZero;
  % Abs = /Std/Int/Sign%.Abs;

  % Bin = /Std/Int/Lit%.Bin;
  % Oct = /Std/Int/Lit%.Oct;
  % Int = /Std/Int/Lit%.Int;
  % Hex = /Std/Int/Lit%.Hex;

That's 42 lines of imports. If we get rid of kinds, it's as many lines, but
now looks like:

  Bool@ = /Std/Bool%.Bool@;
  Not = /Std/Bool%.Not;

  AssertEquals = /Std/Eq/Assert%.AssertEquals;

  Ord = /Std/Cmp%;
  Compared@ = Ord.Compared@;

  List = /Std/List%.List;

  String@ = /Std/String%.String@;
  Str = /Std/Char/Ascii%.Str;
  String = /Std/Char/Ascii/Output%.String;

  Test@ = /Std/Test%.Test@;
  Test = /Std/Test%.Test;
  TestSuite = /Std/Test%.TestSuite;
  Pass = /Std/Test%.Pass;
  Fail = /Std/Test%.Fail;

  Unit@ = /Std/Unit%.Unit@;

  Int@ = /Std/Int%.Int@;
  Neg = /Std/Int%.Neg;

  Sqrt = /Std/Int/Sqrt%.Sqrt;

  Lt = /Std/Int/Ord%.Lt;
  Le = /Std/Int/Ord%.Le;
  Gt = /Std/Int/Ord%.Gt;
  Ge = /Std/Int/Ord%.Ge;
  Min = /Std/Int/Ord%.Min;
  Max = /Std/Int/Ord%.Max;
  IOrd = /Core/Int/Ord%.Ord;
  IsZero = /Std/Int/Sign%.IsZero;
  Abs = /Std/Int/Sign%.Abs;

  Bin = /Std/Int/Lit%.Bin;
  Oct = /Std/Int/Lit%.Oct;
  Int = /Std/Int/Lit%.Int;
  Hex = /Std/Int/Lit%.Hex;

If I had multi import, it might look something like:

  Bool@, Not .= /Std/Bool%;
  AssertEquals .= /Std/Eq/Assert%;
  Ord = /Std/Cmp%;
  Compared@ .= Ord;
  List .= /Std/List%;
  String@ .= /Std/String%;
  Str .= /Std/Char/Ascii%;
  String .= /Std/Char/Ascii/Output%;
  Test@, Test, TestSuite, Pass, Fail .= /Std/Test%;
  Unit@ .= /Std/Unit%;
  Int@, Neg .= /Std/Int%;
  Sqrt .= /Std/Int/Sqrt%;
  Lt, Le, Gt, Ge, Min, Max .= /Std/Int/Ord%;
  IOrd = /Core/Int/Ord%.Ord;
  IsZero, Abs .= /Std/Int/Sign%;
  Bin, Oct, Int, Hex .= /Std/Int/Lit%;

Not bad, right? 16 lines only. No need for lines in between things. A little
bit dense to parse? Actually, looks much harder for me to parse without the
kinds for some reason.

  <<@>@ M@>(Io@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(Io@<M@> m, String@ path) {
    do, return, run .= m;

    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: Output(m, file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

I think, somehow, I like the indicator at the beginning of the line to
indicate a definition of something. Or some syntax highlighting of .= to make
it stand out a little more.

  <<@>@ M@>(Io@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(Io@<M@> m, String@ path) {
    do, return, run, Output .= Import(m);

    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: Output(file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

Anyway, I think this gets us real close, without sacrificing my goal of
explicit variable definitions.

In summary:
1. Remove kinds from let expressions.
2. Inline interfaces to top level for Io@, Rng@, etc.
3. Add struct field .= import syntax.

Do one at a time. See how it feels.

---

So far, removing kinds, I have zero regrets.
* Language spec is simpler.
* Implementation is cleaner.
* fble code looks nicer.
 - I like how variable definitions put the name in the left column now, no
   longer indented. That makes the subsequent indents, if any, stand out much
   better and aligns all the variable definitions much better.

This feels like a good change. It's just a bit on the big side, because now I
have to rewrite literally all of my code. Oh well. The compiler will help me
get it done.

---

The kind transition is done. Still no concerns, but I also feel like I need to
do a bunch of fble programming before I can really evaluate any potential
negative impacts of the change. I don't want to introduce multiline import
yet. I need more space before making another language change. Maybe
tentatively punt multiline import to a future release. See what I can
accomplish without it for now.

Which means, regrouping, next thing to try is, perhaps, get rid of distinction
between RngM and Rng, IoM and Io. Try it out. See how it feels. See if it
feels better or worse. See how painful it is. Primary goal is to find
something that I feel good about to include in the standard library.

---

Experiment 1:
* Change RngM + Rng to just Rng with everything at top level.
* Reference methods via m.foo everywhere.

Experience report:
* Consider State vs. StateM too, in addition to Rng and Io.
* It's not particularly harder to construct an Rng than the previous RngM.
 - Just have to remember what order do and return are declared in.
* As a user of Rng, feels pretty nice.
* Biggest issue so far is annoyance of having to copy paste do and return
  declarations from Monad@.
* It's nice use Rng@ directly instead of RngM@.

I would say, for Rng@, this feels like an improvement. It's much nicer to use.
It's not any harder to construct. It's only very slightly annoying to define
because of duplicating do and return declarations.

---

Experiment 2:
* Change StateM + State to just State with everything at the top level.
* Reference methods via m.foo everywhere.

Experience report:
* Defining pure state monad instance is actually easier without the extra
  layers of indirection.
* 'P' is a weird variable name to use for a pure state monad.
* Maybe IStream@<M@> should include do, return too?
 - Which starts to get trickier, because I expect to have access to the monad
   type separately for that...
* I'm doing @(P.return, P.do) a bunch to extract the Monad@ implementation
  from a State@. Is that something I should be concerned about?
 - Actually, it needs to be Monad@(P.return, P.do).
 - Actually, it needs to be Monad@<P.M@>(P.return, P.do)
* Now I see cases where having M@ as part of Monad@ would be nice.
* When I want to create two separate state views for the same monad, in
  /Games/BenchmarkGame/Game%, things are getting messy.

Unlike the Rng case, I'm not at all convinced this is an improvement. What's
bad is, the Rng case would run into the same issues if it was used via as much
composition as StateM is being used in. For Rng@, it's like there's one
interface that everyone uses, and so it's fine. For State@, it's like we are
passing around lots of different variations of views of state monads to
different things, so we have lots of different instances that we want to
construct and pass around. That's making things unpleasant.

I'm going to back out this change. Go back to the drawing board. Use StateM@
as my driving example, because I clearly do more interesting things with that
than with Rng@.

---

Let's think conceptually. Conceptually Monad@, M@ give us composition of side
effects. State@, Rng@, Io@ give us the side effects. Any one of State@, Rng@,
Io@ is really useless without the Monad@, because we can't compose things
together.

What if we go the other direction. Get rid of StateM@. Always pass things
separately? But I often want to return State@ + Monad@.

Let's review:
* Problem with just State@ - awkward to return that plus Monad@.
* Problem with just StateM@ - feels like it doesn't compose well.
* Problem with combined - s.m prefixes everywhere.

/Games/BenchmarkGame/Game% is the interesting one. Where I make a StateM@,
then provide two alternative State@ and bundle those separately to IStream and
OStream.

The Istream and OStream functions are interesting, because they currently take
State@ before Monad@, so type inference fails. That could be fixed by
switching order, or add M@ to State@ interface too.

What would be best for the /Games/BenchmarkGame/Game% case?
* Define a pure state monad: Monad@ + State@
* Define State@ variations for in, out
 - Bundled with Monad@ or not though?

I want the best of all worlds:
* Grouped together, so we can return as a single thing.
* Everything top level, so we can access without p.m layers.
* Easy to extract and build from Monad@ so we can recompose easily.

Straw Proposal:
* Have separate Foo@, FooM@
 - Foo@ is the raw interface without Monad@
 - FooM@ is Foo@ + Monad@
* Have functions consume FooM@
 - But what if they don't want to define a special FooM@ to consume?
* Have FooM@ put all methods at top level.
* Provide helper functions to construct/deconstruct FooM from parts.

That doesn't feel right. What really feels right to me is have separate
Monad@, Foo@ only. Don't define top level interfaces. Pass as separate
arguments to functions. Include M@ in all the interfaces to help with type
inference. But define top level things for things like StateM@ if we are
returning it?

There seems to be a conflict between: "group together because it always goes
together" and "keep separate, so we can easily recombine things separately".

Random idea I want to get in the notes: We could provide a constructor for,
e.g. StateM@, that takes a Monad@ and has put/get undefined. Then to
recombine, we use something like:

  StateM(m).@(put: ..., get: ...);

---

I'm feeling stuck here. Let's step back, try again, see where we are.

Immediate goal: be able to start transferring things like IoM@, Rng@ over to
/Std.

Monadic classes I anticipate having in /Std for this release:
* Monad(do, return)
* State(get, put)
* Rng(random)
* Io(run)
* IStream(get)
* OStream(put, close)

For the most part, I expect to have all the combinations with Monad:
* Monad, State + Monad, Rng + Monad, Io + Monad, IStream + Monad, OStream +
  Monad.

I can imagine having more complicated compositions, like OStream + OStream +
Monad for stdout, stderr. I'm not sure if I currently use something like that.
I guess Invaders game uses Monad + State + App. The benchmark games use 
Monad + IStream + Put.

I know I want to be able to construct, for example, Rng + Monad, and State +
Monad and return those from functions. It's not clear to me how important /
convenient it is to be able to pass Rng + Monad, State + Monad as single
arguments as opposed to two separate arguments to a function. But I'm sure as
things scale up I'll want to be able to collect interfaces into a top level
type and pass that all at once.

My gut tells me:
* I should preserve hierarchy. Monad is a thing. State is a thing.
  Treat Monad + State as Monad + State, not do + return + get + put.

My goal here is about coming up with a standard convention used in the
standard library for how to do these kinds of compositions. Conventions in
terms of:
* How to name types. e.g. State@ versus StateM@.
* How to name fields. e.g. m versus Monad.
* When to group multiple things into a single type
  e.g. StateM versus State, Monad

I'm pretty sure I want to have raw interfaces pre combination:
* State, Rng, Io, IStream, OStream?
* StateI, RngI, IoI, IStreamI, OStreamI?
* I think I like the first better. Without the 'I'.

I know there are cases where I need to group things into a single type.
Specifically when constructing and returning composite Monad + State, Monad +
Rng.
* StateM, RngM sound decent to me?
* MonadicState, MonadicRng are too long
* MState@, MRng@? Doesn't feel as good as StateM@, and RngM@.

For more complex types, perhaps it makes sense to avoid a convention and just
go with what works. For example, instead of IStreamOStreamOStreamM, we have
something like StdioM@. In this case, Stdio@ should be a collection of
interfaces without Monad? Which suggests the following convention:
* Foo@ is the name of a collection of interfaces without Monad@.
* FooM@ adds Monad@ to that collection of interfaces.

In terms of naming fields, that relates partly to how I anticipate importing
things into the local scope.

Let's consider the case where we pass the composite thing to start.

A. Reference composite arg everywhere:

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    Maybe@<File@> file := io.Monad.do(io.Io.run(Open(path, Str|w)));
    file.?(nothing: io.Monad.return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: Output(io, file.just),
      close: io.Io.run(Close(file.just))
    );
    io.Monad.return(Just(ostream));
  };

B. Import first:

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    do = io.Monad.do;
    return = io.Monad.return;
    run = io.Io.run;
    output = Output(io);

    Maybe@<File@> file := do(run(Open(path, Str|w)));
    file.?(nothing: return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(file.just),
      close: run(Close(file.just))
    );
    return(Just(ostream));
  };

C. Bring into top level?

  <<@>@ M@>(IoM@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(IoM@<M@> io, String@ path) {
    m = @(
      do: io.Monad.do, return: io.Monad.return,
      run: io.Io.run,
      Output: Output(io));

    Maybe@<File@> file := m.do(m.run(Open(path, Str|w)));
    file.?(nothing: m.return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: m.Output(file.just),
      close: m.run(Close(file.just))
    );
    m.return(Just(ostream));
  };


(C) is interesting because it seems to sort of solve the multiple import
desire and renaming all at once? Sort of? The downside is we have to construct
a new object every time we call the function, which doesn't sound great from a
performance point of view.

D. Don't pass composite argument:

  <<@>@ M@>(Monad@<M@>, Io@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(Monad@<M@> m, Io@<M@> io, String@ path) {
    output = Output(m, io);

    Maybe@<File@> file := m.do(io.run(Open(path, Str|w)));
    file.?(nothing: m.return(Nothing<OStream@<M@>>));
    OStream@<M@> ostream = @(
      put: output(file.just),
      close: io.run(Close(file.just))
    );
    m.return(Just(ostream));
  };

What's nice about (D) is we can use full field names for composite types like
StateM@ at construction time, and then the function can use whatever short
name it likes.

For performance, it would be nice if we could pass all the interfaces
separately. If we did it consistently, then that should be effective.
Something like:

  <<@>@ M@>(Monad@<M@>, Io@<M@>, String@) { M@<Maybe@<OStream@<M@>>>; }
  Write = <<@>@ M@>(Monad@<M@> m, Io@<M@> io) {
    output = Output(m, io);

    (String@ path) {
      Maybe@<File@> file := m.do(io.run(Open(path, Str|w)));
      file.?(nothing: m.return(Nothing<OStream@<M@>>));
      OStream@<M@> ostream = @(
        put: output(file.just),
        close: io.run(Close(file.just))
      );
      m.return(Just(ostream));
    };
  };

That way we do all the rebinding once rather than for every call to a
function. We need to be consistent about it for it to work though.

Contrast this with the other option, which I've tried in the past, to pass the
interfaces to the modules. Like, the modules are functions which take the
interface as input. That definitely forces us to pass things ahead of time and
let's us simplify lots of code, but it was awkward to use in the past because
you would have to re-import a module for every separate function where it was
used. It has a contagion like thing, where it forces every module to take the
interfaces it needs as input before you can get anything out of the module.

I vote next thing to try is change Rng@ back to Rng@ + RngM@(Monad, Rng),
restructure code as above to propagate interfaces all first, separately from
other arguments, and see how it feels. Consider renaming RngM@ to PureRng@ or
whatever better name I can find for Pure and associate it with the
construction function instead of the interface. Pass Rng@ and Monad@ around
everywhere separately. Get a solid feel for it.

---

Proposal:
* Foo@ does not include Monad@.
* Pass Monad@ separately from other interfaces.
* We can group together FooA@, FooB@, etc. however we like, using whatever
  field names we like. In practice I don't expect to want to do so for
  anything in the standard library.
* Do not inline anything to the top level.
* Always pass Monad@ as the first argument to a function, then any other
  interfaces FooA@, FooB@.
  - We pass Monad@ first because I expect FooA@, FooB@ are all tied to the
    specific Monad@ instance, but not the other way around. For example, I
    would expect to call Output(m, stdio.err, ...) and
    Output(m, stdio.out, ...), but not Output(n, stdio.err) for that same
    stdio instance.
  - We pass other interfaces next so that we can instantiate and capture
    anything we need as static values rather than have to pass things around
    every time a function is called.
* Monad@ will have M@ as a field. The other interfaces don't need to.
* Cli Main@ should separate Monad@ and Io@ args to follow this convention,
  even if it is a tiny bit more effort on the c side.
* Do whatever you like best in terms of passing things around when you
  implement a function. It's implementation detail, easy to change any time,
  not something I bake into the spec.
* There is no such thing as FooM@ type.
* For returning a pure state monad instance or a pure rng monad instance, we
  can define a helper type that combines Monad@ with State@/Rng@, but that's
  just to return the value. Pass it to any functions in split argument form.
  If it's convenient, don't even define a name for that composite type.

I think this gives us the best we can hope for:
* Composition that scales up without concern.
* Avoid ambiguity in naming between Foo@ and FooM@.

The cost is just the (hopefully minor?) tediousness of passing two arguments
everywhere instead of potentially one. But I'm hoping function level imports
and defines can help there.

Let's try this out. Start migrating things over to use it. See how it feels in
practice. What I like, what I don't like.

I guess let's go from the ground up?
* IoM ==> Io@.
* State@ cleanup.
* Rng@ cleanup.
* IStream@, OStream@ cleanup.

There is some tedious backpedaling to do, but don't worry too much about that.

---

Experience:
* It doesn't feel so bad going back to Monad@ + Io@.
* It's nicer to do m.return, m.do than io.m.return and io.m.do.
* Concern with Cli Main@ putting Monad@ first: does it make it harder to apply
  io and get to a pure monad function thing? Or doesn't matter at all?
* I honestly think the benefits of m.do instead of io.m.do outweight the costs
  of Stdout(m, io) versus Stdout(io). Since we tend to call m.do a lot and
  Stdout(m, io) rarely.

Yeah, I see no issue going back to Monad@ + Io@. Let's keep going on aligning
things all with the new convention.

---

For pure state monad, I'm ending up doing something like this to import
things:

  P = Pure<String@>;
  M@ = P.M@;
  M = P.Monad;
  S = P.State;

Seems okay? I end up doing this a lot. I think it's better than returning a
single object and then using P.M@, P.Monad, and P.State directly?

Is this where I should try a helper monadic bind thing? But that would require
I write down the types explicitly, which I don't want to do.

This might be where multi import helps. I could do just:

  M@, M, S .= Pure<String@>;

How nice would that be?

Anyway, I don't feel too bad about having switched state over to the new
conventions. What's next? Rng@?
