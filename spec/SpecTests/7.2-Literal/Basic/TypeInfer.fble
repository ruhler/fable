# @@fble-test@@ no-error
@ Unit@ = *();
Unit@ Unit = Unit@();

<@>@ L@ = <@ T@> { +(*(T@ head, L@<T@> tail) cons, Unit@ nil); };

<@ T@>(L@<T@>) { T@; } f = <@ T@>(L@<T@> l) {
  l.cons.tail.cons.tail.cons.head;
};

@ Enum@ = +(Unit@ A, Unit@ B, Unit@ C);

# Use of a literal expression with a polymorphic function where type inference
# is applied.
Enum@ e = f|BAC;
e.C;
