Minimum viable product
======================

Also known as: what next?

Status:
* I can show rails and trains, and we can detect train collisions.
* I have a proposal for how to represent data using trains and the initial
  basic operations.

Let's say next goal is to implement an inverter? You send it a sequence of
Boolean trains, it ouputs a sequence of inverted boolean trains.

Input is selector for constant 0=>1, 1=>0. Maybe I can loop the output back to
the input, and end up with a toggling train. I can initialize it by manually
placing a train on the loop.

U->0 -> 1
U->1 -> 0 ->------->--\
    /-> s -> <sink>   |
    \-------------<---/

Gates needed:
* U - unit car.
* 0 - construct 0 union
* 1 - construct 1 union
* select
* sink

---

Implementation Strategy

Make it so that there is a train car associated with every action. We create a
schedule of actions to execute in a turn by listing the train cars. Cars are
listed in train-atomic order. The actions associated with the cars in the
schedule are executed in order, resulting in state updates, including
potential updates to the schedule.

A gate consists of one or more locations, each of which may or may not have a
car on it. Gate action is only instigated from the locations of the gate that
have cars. There is optional state associated with each gate.

A turn is implemented as:
1. Enumerate the cars in train-atomic order, forming the schedule.
2. Execute the action associated with each location in the schedule, in order,
until there is nothing left in the schedule.
3. Wait for the next time event to refresh the display and go back to step (1).

Rail Gate:
* Single location. Single action.
* Has a direction associated with it.
* State: none
* Initially empty.
* Action: If the location the rail points to is empty, move the car to that
  location.

U Gate:
* Single location. Single action.
* State: none
* Initially populated with a unit car.
* Has a direction associated with it.
* Action: If the location the gate points to is empty, move the car to that
  location. Then populate the existing location with a new unit car.

0/1 Gates:
* Two locations: input and output. Two actions.
* Has a direction associated with the output.
* State: whether it is waiting or feeding.
* Initially populated with a 0/1 car on the output and in the 'waiting' state.
* Output Action:
  If in the feeding state and the output points to an empty location, move car
  to that location. If input is empty, put a new 0/1 car on the output and go
  to the 'waiting' state.
* Input action:
  If in the feeding state and the output is empty, move the car to the output.
  If in the waiting state, join the output car to the front of the input train
  and go to the feeding state.

Sink Gate:
* Single location. Single action.
* Action: remove the car at the given location.

Select Gate:
* 3 input. Three action?
* State W,0,0A,0B, or W,1A,1B
  Where 'W' means waiting for next select input.
  0 means 0 was selected abut nothing has been output or eaten yet.
  0A means 0 was selected and output has been passed through, but not unused.
  0B means 0 was selected and unused has been eaten, but not output.
  1 means 1 was selected abut nothing has been output or eaten yet.
  1A means 1 was selected and output has been passed through, but not unused.
  1B means 1 was selected and unused has been eaten, but not output.
* Input S:
   W: if car is 0, go to state 0.
Hmm... this is tricky. It's almost like I want three separate gates that
communicate.

Summary of state queries to support:
* Get the location where a direction points.
* Check if a car exists in a given location.

Summary of state changes to support:
* Add a new car to a new train.
* Move an existing car from a singleton train to the front of an existing
  train.
* Remove a car.
* Move a car to a different location.

---

How to interpret select gate?
1. Block-waiting: waits for select, sends one input through per select, blocks the
unused input.
2. Block-eating: waits for select. sends one input through per select. Eats
the unused input.
3. Non-blocking: input goes through whatever was last selected. The unused
input blocks and queues up. Can toggle switch, but only in between input
trains.

For the proposed MVP example, (3) is no good. We need to block. But we could
do it via a separate synchronizer block if we want. Wouldn't that be better?
More explicit? More interesting? (1) versus (2) doesn't matter here because if
we eat it, we'll just construct another train right away. I could imagine you
want to do a speculative switch, in which case you ought to eat the unused
input.

I think it will be best to start to think of it as: you present all three
inputs, it sends out the two outputs. That doesn't mean you have to wait for
all three inputs, it means you have to consume all three inputs entirely
before going to the next set of inputs. If the select and output are early,
you are fine to output the output while waiting for the unused. If the select
and unused are early, fine to eat the unused first.

---

Rewriting existing app with new approach. What do I need?
* Gates: Rail only.
* State:
  - Schedule
  - Grid of actions
  - List of trains, where a train is a list of car ids
  - Map from CarId to Car coord and value.

Or something like that. Maybe start minimal and add things as we go as we
need.

What state do we really need to keep track of the cars?
* I need a list of trains, to be able to form the schedule.
* A train is a list of cars: x, y, data value.

Options:
* Store (x, y, data) directly on the list of trains. If I want to move a car,
  I linear search through the trains to find and replace it.

Good enough for now. Just factor out the operation to move a car.

For schedule, how about we store the car, including its value? Save us the
effort of looking that up again.

Remind me in what case a gate messes with the schedule? A sink removes the
current car. Do we ever want to remove another, distant car? Let's start no. I
can rewrite the code to support that when we have a specific case for it.

---

Rail gates are working now. Let's add something else fun. Source and sink
maybe? Better to start with sink probably. Or source. One or the other.

---

Sink and source and tag gates are done. Last gate needed for mvp 1 should be
select gate. How did we decide it works? We said one car passes per select,
eaten otherwise, right?

There are 3 parts to the gate: 0, 1, S. 0 and 1 should point to the same
output destination. S should point to a different output destination. Ideally
S knows where 0 and 1 are and can check and update their state?

The states we have are:
* 0, 1: waiting or feeding or sinking
* S: waiting or feeding

S waits for 0, 1, S all waiting, then advances to feeding, sinking, feeding as
appropriate. 0 and 1 do their state as either feeding or sinking. All three
reset to waiting when they read the caboose. Sounds like a common operation we
can factor out for feeding, sinking? We can have 0, 1, S all be Dir@, except
that S needs to know the location of 0 and 1 to be able to read and write
their states.

---

Change in plans for select. Let's do a simpler select gate: 2 input, 2 output.
Inputs are data and select. The data goes to one of the two outputs based on
the select. Have it eat the select train.

Maybe have the select train stay on the select gate until the input train has
passed through? Seems simple.

States for select part of the gate are waiting and sinking?
waiting: the train doesn't advance
sinking: sink the select train. On caboose, switch to waiting.

For the input part of the gate:
* If select is waiting and has a 0 or 1 train, forward to the appropriate
  output. On caboose, flip the select to sinking.
* Otherwise block.

No extra state needed for the input gate?

Using directions to identify the outputs are a little awkward. I would like a
2x2 square with select and input gates adjacent. Maybe specify the target
destinations directly? Let's call it 'select' and 'switch' gates? Select has
waiting vs. sinking state. Switch has three dests: sink, 0, and 1. What are
all the combos?

  W0 SW 1S 01
  S1 10 0W WS

  W1 SW 0S 10
  S0 01 1W WS

  S0 WS 1W 01
  W1 10 0S SW

  S1 WS 0W 10
  W0 01 1S SW

Diagonal support too?

Let's just give explicit targets for 0 and 1 outputs.

Or, does diagonal make more sense? So that input doesn't make a train go
diagonal?

  W0 W1 * 4 rotations
  1S 0S

Or:  SW0
      1

Maybe that's the right way to do it. W has 3 directions: 0, 1, S. And S is
just S. Sounds reasonable. Let's try it.
