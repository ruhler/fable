
@ Compared@ = /Std/Cmp%.Compared@;

<@>@ Show@ = /Core/Show%.Show@;

% String = /Core/Char/Ascii/Output%.String;

Show@<Compared@> Show = (Compared@ a) {
  a.?(less: String|Less, equal: String|Equal, greater: String|Greater);
};

@(Show);
