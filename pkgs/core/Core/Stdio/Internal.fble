
@ Int@ = /Core/Int%.Int@;

<@>@ Maybe@ = /Core/Maybe%.Maybe@;

@ String@ = /Core/String%.String@;

@ Unit@ = /Core/Unit%.Unit@;

# Package type for access to internals of Stdio@ abstract type.
@ _@ = %(/Core/Stdio%);

# Stdio@ --
#   Internal interface for standard input and output operations.
<<@>@>@ Stdio@ = <<@>@ M@> { *(
    # Reads the next byte from stdin.
    M@<Maybe@<Int@>> in,
    (String@) { M@<Unit@>; } out,
    (String@) { M@<Unit@>; } err
  );
};

@(_@, Stdio@);
