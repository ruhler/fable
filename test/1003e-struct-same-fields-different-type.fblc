
// Test that two struct types that are declared with the same fields are still
// treated as different, incompatible types.

struct Unit();

struct A(Unit x, Unit y);
struct B(Unit x, Unit y);

/// Expect: ERROR
func main( ; A) {
  B(Unit(), Unit());
};
