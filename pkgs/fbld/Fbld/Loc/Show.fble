
Format .= /Std/Char/Ascii/Stream%;
Show@ .= /Std/Show%;

Loc@ .= /Fbld/Loc%;

Show@<Loc@> Show = (Loc@ x) {
  Format|'%s:%s:%s'[
    /Std/Stream/String%.String(x.file),
    /Std/Int/Show%.Show(x.line),
    /Std/Int/Show%.Show(x.col)];
};

@(Show);
