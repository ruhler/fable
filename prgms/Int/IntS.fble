
{ /Int/IntP%; @(IntP@); };
{ /Unit%; @(Unit@, Unit); };

# Non-negative Integers.
@ IntS@ = +(
  Unit@ 0,
  IntP@ p
);

(IntP@) { IntS@; } IntP = (IntP@ p) {
  IntS@(p: p);
};

IntS@ 0 = IntS@(0: Unit);

@(IntS@, IntP, 0);
