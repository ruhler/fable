
@ Unit@ = /Std/Unit%.Unit@;
% Unit = /Std/Unit%.Unit;

@ Char@ = /Std/Char%.Char@;
% Str = /Std/Char/Ascii%.Str;

<@>% List = /Std/List%.List;
<@>% Append = /Std/List%.Append;

@ String@ = /Std/String%.String@;

# Experimental code. Ignore me.

<<@>@>@ I@ = <<@>@ M@> { *(
  <@ A@>(A@) { M@<A@>; } return,
  <@ A@>(M@<A@>)<@ B@>((A@) { M@<B@>; }) { M@<B@>; } bind,
  (Char@) { M@<Unit@>; } put
  );
};

<@,<@>@>@ ShowM@ = <@ R@, <@>@ M@>(I@<M@>) { M@<R@>; };

(Char@) { ShowM@<Unit@>; }
Put = (Char@ c)<<@>@ M@>(I@<M@> i) {
  i.put(c);
};

<@ A@>(A@) { ShowM@<A@>; }
Return = <@ A@>(A@ x)<<@>@ M@>(I@<M@> i) {
  i.return(x);
};

<@ A@>(ShowM@<A@>)<@ B@>((A@) { ShowM@<B@>; }) { ShowM@<B@>; }
Bind = <@ A@>(ShowM@<A@> ma)<@ B@>((A@) { ShowM@<B@>; } mb)<<@>@ M@>(I@<M@> i) {
  A@ a <- i.bind(ma(i));
  mb(a, i);
};

(String@) { ShowM@<Unit@>; }
PutStr = (String@ s) {
  s.?(empty: Return(Unit));
  Unit@ _ <- Bind(Put(s.items.head));
  PutStr(s.items.tail);
};

<@>@ DbgM@ = <@ A@> { A@; };

I@<DbgM@> DbgI = @(
  return: <@ A@>(A@ x) { x; },
  bind: <@ A@>(A@ ma)<@ B@>((A@) { B@; } f) { f(ma); },
  put: (Char@ c) { 
    /Core/Debug%.TraceLn(Append(Str|'Put ', List[c]));
  });

Unit@ _ = PutStr(Str|'Hello')<DbgM@>(DbgI);

@(Put, Return, Bind, PutStr);
