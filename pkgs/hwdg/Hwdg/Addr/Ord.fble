
<@>@ Cmp@ = /Std/Cmp%.Cmp@;
@ Compared@ = /Std/Cmp%.Compared@;

@ Addr@ = /Hwdg/Addr%.Addr@;

Cmp@<Addr@> Ord = (Addr@ a, Addr@ b) {
    Compared@ r = /Core/Int/Ord%.Ord(a.x, b.x);
    r.?(equal: /Core/Int/Ord%.Ord(a.y, b.y));
    r;
};

@(Ord);
