
@ Int@ = /Int%.Int@;
% Int = /Int/Lit%.Int;
<@>% Just = /Maybe%.Just;
@ Result@ = /Sat%.Result@;
% Solve = /Sat%.Solve;
@ Unit@ = /Core/Unit%.Unit@;
% Unit = /Core/Unit%.Unit;

Unit@! Bench = {
  Result@ solved := !(Solve(/Sat/Aim%, Just<Int@>(Int|400)));
  !(Unit);
};

@(Bench);
