
import @ { Unit@StdLib; Nat@UBNat@UBNat; };

union Color(
  Unit black,
  Unit red,
  Unit green,
  Unit yellow,
  Unit blue,
  Unit magenta,
  Unit cyan,
  Unit white
);

struct Draw(Nat x, Nat y, Nat w, Nat h, Color color);
