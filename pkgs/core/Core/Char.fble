
@ Int@ = /Std/Int%.Int@;

@ Char@ = *(Int@ pos).%(@/Core/Char%);

# Char from unicode code point.
(Int@) { Char@; } Char = (Int@ pos) {
  @(pos);
};

# Unicode code point of the character.
(Char@) { Int@; } Pos = (Char@ c) {
  c.pos;
};

@(Char@, Char, Pos);
