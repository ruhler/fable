Fble 0.6 TODO
=============
Things to consider doing for the fble-0.6 release.

* Revisit handling of wchar_t vs. unicode, locale, and all that.
 - Remove old text based IO.
 - Switch string conversion to use utf8.
 - finish implementing utf8 decoding in fble.

* Inline /Std/Io/File/Utf8%.Text fields into the module directly?
 - Is the extra indirection of 'Text' worth having?

* It's hard to find where fble functions live
 - Anything I can do about that?

* Add Monad for Stream on stream type itself?
 - Reuse that in the new impl of Text based file IO?

Standard Library:
* Add back character type functions isspace, isalnum, etc?
* Add Lt, Le, Gt, Ge, etc for Char@?
* Reconsider if Read, Write, Append should report error messages?
* Think about api to break out of a ForEach loop mid way through.
 - Because this comes up a lot, and I tend to just write 'TODO' in my code
   where I should probably implement a proper interface.
* Should we have a generic Container interface?
 - As a way to standardize ForEach and perhaps other loop constructs across
   all containers? Would that add anything of value?
* Add function to show an integer base 16? Base 2? Base anything?
* Reorder arguments in body function passed to ForEach?
  See thoughts/fble-0.6/stdlib.list.txt for more discussion.

Tutorials:
* Add left side menu to generated website for tutorials?
* Write a poker solitaire tutorial outside the repo?

Documentation:
* Double check all the old documentation referring to Output@, which is now
  called Stream@?
* A lot of the /Std/* documentation isn't very helpful.
  Consider rewriting it to be an explanation for how to use the functions
  instead of a description of what they are.
* Figure out how to properly document List@ type
 - Mix of poly, struct, and union all in one.
* How to document a poly? @func? @value?
* How to document methods of interface structs?
* How to document an interface function type, like Eq@?
* /Std/List/Eq% docs could be more precise and include examples.
* How to document internal functions, e.g. /Std/List/Sort%.Insert?
  Make sure this doesn't get shown in reference documentation.
  e.g. /Std/Int/Lit% internal helper functions.
* Should I add @example as a doc comment field? Seems useful?

Other:

* Update all the sample apps for latest features, styles, idioms.

* Figure out why we see the same errors reported many times when we build.
 - And fix it.
* Add -t option to compiler to pass expected type of main
 - Use it in fble-cli and fble-app for better type checking.
* Be able to typecheck body of a let where the variables aren't all well
  typed, to get more error messages at once.

* Revamp the standard library.
 - Test and document (and tutorialize?) all functions.
 - Implement rosettacode.org tasks under pkgs/rosetta to test it out.

* Clean up program loading
 - see thoughts/fble-0.5/modular_compilation.txt for context.
 - Clarify the spec with respect to builtins referring to other builtins and
   the recursive module requirements in case of difference between builtins,
   .fble files, and header files.
 - Add tests for:
   a. Try to compile a builtin module.
   b. builtin A -> builtin B, .fble C -> builtin B.
   c. builtin A, A.fble.@ -> B.fble, B.fble -> /C%,
      C.fble has bugs, C.fble.@ doesn't. This should not fail to execute.
 - Update the implementation to pass the tests.
 - Consider separating the FbleModule* for execution and for type for a given
   module path.
 - Add option for loading modules from .so files?
 - Have fble-cli check that the function has the right type?
 - Load minimum dependencies needed for type checking when possible?
  
* Implement deflate algorithm.
 - Change --profile to produce gzip compressed profiles (doesn't need to be
   compressed yet, can use uncompressed blocks).
 - Change fble-pprof to decompress profiles.

* Figure out a way to test foreign function not found?
 - Interpreted gives runtime error.
 - Compiled gives link time error not an any particular line number.
 - We would need some extra spec infra support to describe this.
 - Add spec test for Foreign value not found in interpreted case.

---

* Fix failing unicode tests in pkgs/std on windows build
 - See thoughts in thoughts/fble-0.6/stdlib.string.txt.
 - issues with utf-8 command line args
 - issues with setlocale

