
* Add option to select cars from the menu too
 - including an 'erase' car option.

* Clean up menu code.

* Figure out how to write tests for ui
  Really. Honestly. Do it now!

* Click on menu to select gate or car.
* Add key or right mouse click option to deselect.

* Change DrawGate to take Coord@ instead of a pair of Int@
* Remove Draw prefix from DrawGate, DrawCar, DrawModel.
  - Consider renaming Graphics% to Draw%.

* Fix indentation in source files, which seems to have gotten funny.
  And figure out why it got funny so that doesn't happen again.

* Pull Eq, Show for everything into appropriate submodules
  Instead of dumping it all in Model%.

* Highlight that gates are rotatble by making the gate brighter when the mouse
  is over it?

* Change zoom control to ctrl + and ctrl -.

* add special case zoom all the way out option that shows 1 pixel per gate?
* Show on screen whether the game is paused or running.

* Implement model save to file.
* Implement model read from file.
* Sketch out UI design.

* Pick more black/whitish colors for zero and one, so it's more intuitive?
  At least more intuitive to me?

* Finish mvp2 milestone.

* Add note to fble thoughts:
  - It's super tedious to do in place updates of deeply nested state elements,
    such as model.s.cars when placing a car.
