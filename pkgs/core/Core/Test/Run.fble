
@ Bool@ = /Std/Bool%.Bool@;
% True = /Std/Bool%.True;
% False = /Std/Bool%.False;

<@>% Concat = /Std/List%.Concat;
<@>% ForEach = /Std/List%.ForEach;

<<@>@>@ Monad@ = /Std/Monad%.Monad@;

<<@>@>@ OStream@ = /Core/Stream%.OStream@;

@ String@ = /Std/String%.String@;
% Str = /Std/Char/Ascii%.Str;
% FormatE = /Std/Char/Ascii/Output%.FormatE;

@ Test@ = /Core/Test%.Test@;
@ TestResult@ = /Core/Test%.TestResult@;

@ Unit@ = /Std/Unit%.Unit@;
% Unit = /Std/Unit%.Unit;

<<@>@ M@>(Monad@<M@> m, OStream@<M@> out) {
  % O = /Core/Stream/OStream%(m);
  % PutStr = O.PutStr;
  % PutLine = O.PutLine;

  # Returns True if the tests all pass, false otherwise.
  (String@, Test@) { M@<Bool@>; } RunTests = (String@ prefix, Test@ test) {

    (Test@, String@, Bool@) { M@<Bool@>; }
    Run = (Test@ test, String@ prefix, Bool@ passing) {
      test.?(
        case: {
          String@ name = Concat[prefix, test.case.name];
          Unit@ _ <- m.do(PutStr(out, Concat[Str|'@[', name, Str|']: ']));
          TestResult@ tr = test.case.test(Unit);
          tr.?(
            pass: {
              Unit@ _ <- m.do(PutLine(out, Str|'@PASS'));
              m.return(passing);
            },
            fail: {
              Unit@ _ <- m.do(O.Show(out, FormatE|'@FAIL: %s\n'[tr.fail]));
              m.return(False);
            },
            xfail: {
              Unit@ _ <- m.do(PutLine(out, Str|'@EXPECTED_FAIL'));
              m.return(passing);
            });
        },
        suite: {
          String@ nprefix = Concat[prefix, test.suite.name, Str|'.'];
          ForEach(test.suite.tests, m.return(passing),
            (Test@ test, M@<Bool@> mp) {
              Bool@ passing <- m.do(mp);
              Run(test, nprefix, passing);
            });
        });
    };

    Run(test, prefix, True);
  };

  @(RunTests: RunTests);
};
