# @@fble-test@@ compile-error 7:1

@ Unit@ = /SpecTests/'9.2-PrivateType'/Private%.Unit@;
% F = /SpecTests/'9.2-PrivateType'/Private%.F;

# The type of F is private, so we can't use it as a literal.
F|BAC;
