
@ Char@ = /Char%.Char@;

<@>@ List@ = /List%.List@;
<@>% List = /List%.List;

<@,@>@ Lit@ = /Literal%.Lit@;

@ Str@ = List@<Char@>;
<@>@ CharLit@ = Lit@<Char@>;

(CharLit@) { List@<Char@>; } Str = (CharLit@ lit) {
  List<Char@>(lit);
};

@(Str@, CharLit@, Str);
