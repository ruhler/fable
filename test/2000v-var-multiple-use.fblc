
// Test that a variable can be used in multiple places to share a value.

struct Unit();
struct A(Unit x, Unit y);

func foo(Unit v ; A) {
  A(v, v);
};

/// Expect: A(Unit(),Unit())
func main( ; A) {
  foo(Unit());
};

