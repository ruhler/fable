
@ Rat@ = /Rat%.Rat@;

<@>@ Show@ = /Show%.Show@;

% Str = /Core/String%.Str;
% Strs = /Core/String%.Strs;

Show@<Rat@> Show = (Rat@ r) {
  # TODO: Reduce the ratio before showing?
  Strs[/Int/Show%.Show(r.n), Str|' / ', /Int/Show%.Show(r.d)];
};

@(Show);
