
@ Bool@ = /Std/Bool%.Bool@;

<@>@ Cmp@ = /Std/Cmp%.Cmp@;
% Less = /Std/Cmp%.Less;
% Greater = /Std/Cmp%.Greater;
% Equal = /Std/Cmp%.Equal;

Cmp@<Bool@> Ord = (Bool@ a, Bool@ b) {
  a.?(
    true: b.?(true: Equal, false: Greater),
    false: b.?(true: Less, false: Equal));
};

@(Ord);
