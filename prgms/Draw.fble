{ /Unit%; @(Unit@); };
{ /Int/Int%; @(Int@); };

@ Color@ = +(
    Unit@ black,
    Unit@ red,
    Unit@ green,
    Unit@ yellow,
    Unit@ blue,
    Unit@ magenta,
    Unit@ cyan,
    Unit@ white
    );

@ Draw@ = *(Int@ x, Int@ y, Int@ w, Int@ h, Color@ color);

@(Color@, Draw@);
