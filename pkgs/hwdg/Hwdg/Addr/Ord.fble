
Cmp@, Compared@ .= /Std/Cmp%;

Addr@ .= /Hwdg/Addr%;

Cmp@<Addr@> Ord = (Addr@ a, Addr@ b) {
    Compared@ r = /Std/Int/Cmp%.Cmp(a.x, b.x);
    r.?(equal: /Std/Int/Cmp%.Cmp(a.y, b.y));
    r;
};

@(Ord);
