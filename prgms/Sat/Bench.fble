
@ Int@ = /Int/Int%.Int@;
% I = /Int/Int/Lit%.I;
% D = /Int/Int/Lit%.D;
<@>% Just = /Maybe%.Just;
@ Result@ = /Sat%.Result@;
% Solve = /Sat%.Solve;
@ Unit@ = /Unit%.Unit@;
% Unit = /Unit%.Unit;

Unit@! Bench = {
  Result@ solved := !(Solve(/Sat/Aim%, Just<Int@>(I(D|70))));
  !(Unit);
};

@(Bench);
