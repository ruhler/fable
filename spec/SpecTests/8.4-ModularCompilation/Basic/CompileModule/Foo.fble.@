# @@fble-test@@ none
# Module used in CompileModule test.

Unit@ = *();
Unit@ Unit = Unit@();

Bool@ = +(Unit@ true, Unit@ false);
Bool@ True = True;
Bool@ False = False;

@(Unit@, Unit, Bool@, True, False);
