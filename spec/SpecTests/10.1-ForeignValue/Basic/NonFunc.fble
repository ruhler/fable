# @@fble-test@@ no-error

@ Unit@ = *();

@ Bool@ = +(Unit@ true, Unit@ false);

# Basic use of a non-function foreign value.
# The foreign value 'False' for this module must be supplied by the test
# driver somewhere. It should behave as a boolean False value.
Bool@ F = Bool@|False;
F.false;
